# Transparency Coverage

Current main-branch status: the model-facing `mining.reach_depth` objective compiles one internal step from each fresh snapshot. Stone mining, supported large resource-clump removal, auto-combat, natural debris pickup, food recovery, ladder/shaft descent, and mandatory exit are implemented. An isolated ordinary-mine run closed levels 96 through 98 and terminal exit with five verified rows. A separate isolated Skull Cavern run verified native shaft descent from 130 to 134 with exact health cost and one training row. The Quarry Mine side branch has a separate `mining.acquire_golden_scythe` objective. Volcano Dungeon has a separate `volcano` transparent domain and `volcano.reach_caldera` rolling planner; switch-touch, forward-warp, watering-can cooling, stone removal, container removal, and supported vanilla melee combat compile to independent native-input executors. Ordinary mines (`1..120`), Skull Cavern (`MineShaft` area `121`, level `>120`), Quarry Mine (`MineShaft` sentinel `77377`), and `VolcanoDungeon` are four non-interchangeable families. Full-objective durations remain unknown and therefore fail the day time-budget gate instead of using guessed constants.

| required input/output | snapshot/contract path | source/status | completeness gate | missing/runtime boundary |
|---|---|---|---|---|
| Mine kind, level, area, generated/loaded identity, ordinary/Skull/quarry/dangerous classification | `mining.current_mine` | live `MineShaft.mineLevel`, `getMineArea()`, `GetAdditionalDifficulty()`, area flags | required by `mining.reach_depth` | unavailable outside loaded `MineShaft` |
| Player tile, collision context, exit/entry/elevator/ladder/shaft/Golden Scythe altar tiles | `mining.tiles` | loaded `GameLocation.map`; cached compact collision rows from read-only `IsTileBlockedBy`; exact action tokens including `GoldenScythe` | required and covered for the loaded floor | unavailable only outside a loaded `MineShaft`; current player is excluded from its own collision row |
| Breakable stones, ore/resource nodes, containers, placed staircases, relevant objects | `mining.objects` | live `MineShaft.objects`; `IsBreakableStone`; stone `MinutesUntilReady`; `BreakableContainer` runtime type/private net health | required and covered for the loaded floor | unknown non-stone/non-container objects remain identified but are not guessed as mining nodes |
| Large MineShaft resource clumps | `mining.resource_clumps` | live anchor/footprint/type/health plus exact decompiled Axe/Pickaxe upgrade gates and per-hit damage | required and covered for supported vanilla indexes | unsupported parent-sheet indexes, missing tools, and insufficient upgrades fail closed |
| Monsters with type, tile, bounding box, health, damage, behavior facts | `mining.monsters` | current live `Monster` rows; no AI/drop mutation | required and covered for current state | future AI path is intentionally not predicted; executor must re-read and replan after movement/combat changes |
| Floor objectives/gates affecting descent or the Quarry Mine reward | `mining.floor_objectives` | live mine flags, kill-all rule, enemy/stones/ladder facts, Golden Scythe applicability/claim flag/reward identity, private treasure-room net field, exact stone ladder formula and seeded per-stone roll | required and covered for the loaded floor | future monster-drop ladder uses live after-state instead of reading/mutating global RNG |
| Player mining resources | `mining.player_resources` | live player health/energy/levels/tools/melee weapons/loaded slingshots/typed bomb slots/stairs/food/time/buffs/deepest mine level; melee and slingshot formula inputs include Attack, luck, added speed, professions, blessing state, player enchantments, equipped trinket identities, attached ammo, and consumable stacks | required | exit deadline is only a model/compiler constraint, not observed state; slingshot projectile travel/interception and live bomb placement/escape still require isolated runtime calibration |
| Group completeness and forbidden calls | `mining.completeness`, snapshot `unavailable_fields` | adapter metadata | required recursively | `complete` on a loaded MineShaft; fails closed with `not_loaded_mineshaft` |
| Purpose-limited snapshot | `/v1/snapshot?profile=mining` | profile cache plus `world/player/menus/options/mining` domains | required for mining runtime/training | avoids unrelated farm/social/fishing and generic location scans; mining itself supplies loaded-floor collision and occupants; cached for the existing 30-tick profile window |
| Model option surface | `mining.reach_depth` parameters | target depth/location family, latest exit time, reserve health/energy, resource policy | compiler validates | model does not emit low-level mining/combat actions |
| Quarry Mine reward option | `mining.acquire_golden_scythe` | fixed family `quarry_mine`, sentinel floor `77377`, reward `(W)53`, optional reserve/deadline parameters | compiler validates and rejects use through `mining.reach_depth` | single-floor duration calibration and native runtime evidence pending |
| Candidate generation | availability `event_candidates[]` | `MiningReachDepthCandidateBuilder` | requires all mining groups recursively complete | missing nested groups, invalid target, missing elevator unlock facts, and unknown mining cost fail closed; negative ticks/energy are blocked unknown-cost sentinels, not estimates |
| Compiled queue envelope | `action_queue.items[].normalized_command.parameters` | `ActionQueueCompiler` | preserves the high-level objective and adds the current internal `execution_option_id` plus exact targets | executable one step at a time; full-objective timing remains unknown and blocked by the day budget |
| Per-floor mechanical selection | `MiningFloorStepPlan` | exact compact-grid BFS over the transparent snapshot | covered offline | selects ladder, required monster, or stone; does not predict future monster movement |
| Native stone execution | internal `executor.mine_stone` result | collision-safe walking plus native `BeginUsingTool`/`EndUsingTool` | one-stone E: covered | records positive native swing count and terminal health zero; not exposed to the small model |
| Native resource-clump execution | internal `executor.break_resource_clump` result | rectangle-perimeter BFS, exact live clump rebinding, native Axe/Pickaxe `BeginUsingTool`/`EndUsingTool`, natural removal verification | implementation and static compilation complete | no direct `performToolAction`, clump collection removal, health mutation, position write, or warp; isolated runtime test intentionally deferred |
| Semantic reach-depth execution | `mining.reach_depth` | rolling-horizon current-floor execution | ordinary-mine 96-to-98 loop and Skull Cavern one-shaft step runtime verified | two native stone steps, two native ladder descents, terminal native exit, and Skull Cavern native shaft 130-to-134 passed with fresh changed after-states; arbitrary-depth timing remains pending |
| Golden Scythe side-branch execution | `mining.acquire_golden_scythe` | rolling-horizon Quarry Mine combat/clearance, move, `GoldenScythe` claim, and native mine exit; bounded isolated loop uses only `profile=mining` | implementation, static compilation, debug fixture, and loop orchestration complete | runtime test intentionally deferred; no runtime evidence claimed |
| Volcano live level state | `volcano.current_level`, `volcano.tiles`, `volcano.connectors`, `volcano.gates`, `volcano.objects`, `volcano.monsters`, `volcano.debris`, `volcano.player_resources` | live `VolcanoDungeon`, `DwarfGate`, map, warp, water/cooling, occupant, object health, monster resilience/miss/support, inventory, and exact tool fields | read-side and completeness gate implemented | unavailable outside a loaded `VolcanoDungeon`; no generation or mutation calls |
| Volcano Caldera objective | `volcano.reach_caldera` | rolling current-level BFS over exact live collision, switches, coolable tiles, objects, monsters, and forward warps | current switch, connector, cooling, stone, container, and supported melee step compiles to a native-input executor | full repeated level loop not tested; Spiker and custom monsters fail closed |
| Volcano lava cooling | `executor.cool_volcano_lava` | exact target/stand/watering-can slot; native `BeginUsingTool`/`EndUsingTool`; live `cooledLavaTiles` verification | implementation and static compilation complete | no direct `CoolLava`, `performToolAction`, dictionary mutation, or position write; isolated runtime test intentionally deferred |
| Volcano obstacle removal | `executor.break_volcano_stone`, `executor.break_volcano_container` | exact target/stand/tool slot and deterministic hit budget; native pickaxe or heavy-hitter input; live object absence verification | implementation and static compilation complete | no direct `performToolAction` or object removal; moving threat and target drift fail closed; runtime test intentionally deferred |
| Volcano supported melee | `executor.combat_volcano_monster` | exact runtime identity/type/name, weapon slot, health/resilience/miss fields; native WASD and melee input; health-zero verification | implementation and static compilation complete | `Spiker.takeDamage` immunity and custom monster semantics fail closed; no direct damage/removal; runtime test intentionally deferred |

Validation status: the last full regression remains PASS Core 917 / Backend 49. Hidden/silent isolated E: `runtime-mining-reach-depth-20260716-183347` started at 96, visited 96/97/98, verified five of five compiled primitives, produced five fresh changed after-states and five training rows, then verified terminal exit. Hidden/silent isolated E: `runtime-skull-cavern-shaft-20260716-221731` verified Skull Cavern area 121, level 130-to-134, exact health 140-to-128, native shaft/fall dialogue lifecycle, fresh changed after-state, and one training row. The resource-clump executor, Golden Scythe bounded loop, and all currently implemented Volcano primitives compile successfully with deployment disabled; their scripts were intentionally not run by user instruction. These results do not remove any duration gate.
