# Risks

| risk id | risk | impact | mitigation | status |
|---|---|---|---|---|
| RISK-015 | Ordinary `(BC)130` current-map storage placement is built and runtime verified, and rolling cross-map continuation is statically covered. Stone, Big, Junimo, Mini-Fridge, Mini-Shipping, live inventory-`Chest`, and full connector-to-placement runs remain unverified. Acquisition, purpose/demand policy, explicit sharing, and relocation are not part of this slice. | A native special branch may expose a post-state identity difference, or a target-map transition may invalidate inventory/location assumptions. Treating one ordinary smoke as universal storage training eligibility could produce false failures or poor layouts. | Keep broad storage training eligibility blocked. Run isolated per-branch smokes and verify exact target `Chest`, special type/capacity, inventory decrement, new transparent access point, unchanged protected connectivity, and fresh training output. Run one full cross-map continuation smoke before enabling remote placement. | narrowed_open |
| RISK-013 | The material graph reads all persistent player chests and classifies actor-owned, shared global, shared unowned, and other-player nodes. Shared/other nodes remain visible but default-denied. Save/player-scoped material reservations persist goal ownership, bind exact slots through candidate/compiler revisions, and are rechecked immediately before machine-crafting runtime input. Explicit shared grants are not implemented. | The AI no longer silently consumes another player's current resources or materials reserved for another controller goal through the implemented machine-crafting path. Intentional human sharing cannot yet be authorized, and future material-consuming executors must opt into the same dispatch gate. | Keep default deny. Require every new material-consuming executor to bind reservations and use typed dispatch readiness. Add versioned sharing grants and run a two-farmer isolated matrix before enabling shared nodes. | narrowed_open |
| RISK-014 | The dedicated host reaches the overnight `ShippingMenu`, but host-local draw suppression also prevents the nested native `SaveGameMenu.draw()` call which alone sets `hasDrawn=true`. Decompiled `SaveGameMenu.update()` refuses to start `SaveGame.Save()` until that flag is set, so the live server stopped after the native OK-button path at `Context: before save`; Bridge snapshots then timed out and the host-AI retry budget was exhausted. | The AI can finish a day but cannot persist or enter the next playable day while full draw suppression is enabled. The server container remains alive and CPU-bound while its API is unhealthy. | Keep the native ShippingMenu input path and native `SaveGame.Save()` coroutine. In dedicated-host draw-suppressed mode only, a Harmony prefix now restores the draw lifecycle signal by setting public `SaveGameMenu.hasDrawn=true` before native update. Do not claim recovery until a disposable overnight run proves save files advance, the menu closes, Bridge health returns, and the next action is verified. | static_fix_complete_deployment_and_runtime_proof_pending |
| RISK-012 | Machine demand classifies each learned machine dynamically and consumes current backlog, fleet timers, live crop waves, or active versioned outdoor-crop commitments. The shared ledger now also owns exact material reservations; both classes are save/player scoped, persistent, revision checked, auditable, cancellable, and compiler rebound. | Crop commitments still exclude greenhouse/Island/modifier/layout feasibility. Recurring animal output, future mining/smelting demand, machine placement/route cost, and full service execution can still change the answer. Treating this slice as complete long-horizon utility would underbuild or overbuild. | Preserve ledger revision/IDs through compilation and fail closed on drift. Upstream excludes only when no task, no proven covered-horizon deficit, and no first-craft requirement. Before broad training, add recurring supply projections, native placement/service chains, modifier/location commitments, dispatch-time ledger freshness, and craft/place/load runtime smokes. | narrowed_open |
| RISK-011 | Farmhouse purchases are irreversible and the new native Carpenter state machine has not yet been exercised in a disposable in-game save. | Dialogue timing could fail after native payment, or postcondition verification could report a false block while construction has already started. | Candidate, compiler, and runtime independently rebind endpoint, Robin presence, competing construction, house level, countdown, exact money/material tuple, and stand/path. Runtime uses only native map-action/dialogue callbacks and verifies money, material, unchanged immediate level, and countdown 3. Do not run on a human save; add isolated level-0 and level-1 fixtures before marking runtime complete. | open |
| RISK-007 | Runtime fishing replaced `Game1.input` with a custom `ControlledInputState`, but SMAPI 4.5.2 casts it to internal `SInputState` every update. | The game froze on the first cast frame, logs grew unboundedly, the executor request never completed, and no valid fishing training sample was produced. | Global/OS input replacement was removed. Runtime now reflects only the existing SMAPI `SInputState.OverrideButton(SButton.MouseLeft, ...)` entry point during `UpdateTicking`, preserving native charge, cast, hook, BobberBar physics, and cleanup. Source guards plus hidden ordinary and Lava Eel smokes passed with zero SMAPI errors. | closed |
| RISK-008 | High-difficulty fish are not mechanically guaranteed at low character fishing level even with optimal input and legal advanced equipment. | Treating every cast as certain could bias planning and training rewards; treating executor losses as crashes would also be wrong. | Preserve observed success/failure labels and transparent fishing level, rod, bait, tackle, fish difficulty, and outcome distribution. The Lava Eel smoke proves a successful native BobberBar path, but planner utility must retain the remaining character/equipment-dependent risk. | narrowed_open |
| RISK-001 | The new after-snapshot freshness wait was not exercised against a live E: SMAPI session in the first pass. | A live bridge/runtime timing edge could still produce `after_snapshot_fresh=false`, blocking trusted model training data. | Completed hidden/silent live runtime validation with applied and blocked movement episodes; both final artifacts have `after_snapshot_fresh=true` and `state_hash_changed=true`. | closed |
| RISK-002 | Historical applied movement artifact has equal before/after hashes even though executor changed `player.tile`. | Existing historical episode artifacts should not be used as final training examples for movement outcome learning. | Treat prior equal-hash movement artifacts as diagnostic only; regenerated final applied and blocked artifacts under `artifacts\hidden-training-final-*`. | closed |
| RISK-003 | Crop planting is only verified as a narrow single-tile executor slice, not as broad planting policy. | If a future planner exposes layout/crop-choice planting before candidate generation, seed acquisition, exceptions, and next-day outcomes are covered, the model could choose poor or impossible planting plans. | `farm.crop_catalog`, `player.seed_inventory`, and `current_location.planting_context` are transparent. `executor.plant_seed` now requires exact tile/seed `hard_rule_allows_planting=true` and has one isolated runtime smoke. Keep broad planting candidates/policy blocked until layout choice, buying missing seeds, fertilizer, exception maps, harvest/growth outcomes, and training output recording are covered. | narrowed_open |
| RISK-004 | Broad behavior/policy training is not meaningful while hard-rule domains are incomplete. | Model may learn from incomplete or invalid candidate spaces and overfit to smoke/calibration artifacts. | Training is paused. Resume only after `hard-rule-transparency-audit.md` blocked domains have transparent fields, Wiki confirmation, local decompile evidence, compiler gates, and tests. | open |
| RISK-005 | Shop opening and one safe money-currency purchase were runtime-verified, but broad shop behavior is not complete. | A future planner could still choose a shop/action branch outside the verified slice, or assume all shops/items are safe because Blacksmith copper ore passed. | Keep route/shop candidates gated by `locations.route_gate_context`, owner/service time, and `current_location.shop_action_tiles`. The verified slice is isolated route-to-shop plus `executor.buy_shop_item` for entries with `executor_purchase_enabled=true`; do not generalize to unsupported branches, selling, upgrades, or special shops. | narrowed_open |
| RISK-006 | Purchase `actionWhenPurchased` and UI held-item behavior can mutate state beyond simple inventory add. | A purchase executor could lose items, start tool upgrades, consume special currency, learn recipes, open new menus, or leave items in `ShopMenu.heldItem` if modeled too simply. | `menus.shop_stock` exposes affordability/trade/inventory/side-effect risk fields, and the runtime purchase executor only accepts safe whitelisted stock entries. Blacksmith `(O)378` single-item purchase is verified; callback-heavy, trade-currency, limited/synced stock, upgrades, buyback, and selling remain blocked until separately modeled and smoked. | narrowed_open |
| RISK-009 | A random floor-119 breakable container selected before descent exhausted the existing container swing budget during the first Skull Key end-to-end attempt. | Long ordinary-mine objectives could stop on an incidental container even though the target-specific floor-120 claim chain was correct. | Closed as executor calibration. Decompiled `BreakableContainer.performToolAction` confirms heavy hitters decrement live health and clubs decrement it twice. The executor now uses native left-click input for melee heavy hitters, native tool input for non-melee tools, and charges the swing budget only after the native action animation completes. Deterministic container smoke `runtime-container-lifecycle-20260717102239` observed `(W)63`, health `3 -> 0`, one completed swing, and removal. Full 119-to-120 run `runtime-skull-key-20260717102345` independently selected and removed a random container before claiming the key and exiting. | closed |
| RISK-010 | Joja membership and project purchases are irreversible and the new native runtime state machine has not yet been exercised in a disposable in-game save. | A dialogue timing or menu-transition mismatch could block after opening Morris dialogue, or report failure after a native payment already settled. | The compiler and runtime independently rebind route, endpoint, event/greeting, balance, project button/price/mail IDs, and pending-order state. Runtime mutates progress only through native Joja callbacks and verifies money/mail postconditions. Do not run against a human save; add a dedicated disposable Joja fixture and smoke membership plus every project before marking runtime complete. | open |
| RISK-016 | A server interruption can leave a compiler-owned native transaction open while volatile tick/state hashes continue changing. | Exact state-hash retry detection misses the deadlock, causing high CPU, internal traffic, rotated logs and useless artifacts. | Resume only explicitly modeled exact transaction states. For every other non-executable semantic queue, use exponential no-progress backoff with a deployment cap and reset immediately when an executable item appears. Keep rolling artifact retention. | static_fix_complete_deployment_and_runtime_proof_pending |
| RISK-017 | The deterministic C# structured provider and checkpoint round-trip now exist, but the standard E-drive production paths still contain no real policy trajectories, horizon observations, formal manifest or checkpoint. Only five bounded option families are admitted, while the dedicated host remains a sparse execution/data-quality probe. | Calling the synthetic model tests, an empty production path, or a five-family rollout "full training" would produce a false completion claim and a policy that cannot represent the full 21-point route. | Keep `--require-structured-policy` failure-closed and never fabricate a production checkpoint. Expand five-gate admission by authoritative-dictionary dependency order, collect real verified trajectories and closed horizons, build the hash manifest, train the V1 provider, then evaluate on independent saves and a full third-year 21-point rollout before freezing the perfect-policy baseline. | narrowed_open |
| RISK-018 | The reported new training laptop has a Ryzen 9 9955HX, 32 GB system RAM, and an RTX 5070 Laptop GPU with 8 GB VRAM; the exact laptop, driver, TGP, cooling, storage, and sustained-load behavior are not yet measured. | Treating the laptop GPU like a higher-memory desktop part could cause OOM, RAM paging, thermal throttling, unstable game/training concurrency, or a model choice that delays the required formal baseline. | Make the C# structured ranker the required V1 and use streaming datasets with bounded parallelism. Treat a 0.6B-class 4-bit constrained model as optional, 1.7B as a measured boundary experiment, and 3B+ training as off-device. Require `nvidia-smi`, checkpoint round-trip, storage, thermal, memory, and throughput acceptance before formal training. | open |
