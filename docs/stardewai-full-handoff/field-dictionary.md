# StardewAI Field Dictionary

This dictionary scopes fields to AI decision, execution, training, and safety needs. It is not a complete Stardew Valley data model.

Status values:

- `covered_for_read`: transparent read-side field exists.
- `covered_for_gate`: compiler/candidate gate consumes the field.
- `blocked_for_execution`: executor remains disabled or unimplemented.
- `needs_runtime_regen`: field needs a fresh runtime snapshot after next Bridge deploy.
- `needs_wiki_secondary`: local decompile evidence exists, but player-facing Wiki confirmation is still pending.

Current installed full-snapshot schema checkpoint: **151 required / 135 readable with provenance / 16 contextual / 0 blocking** (EVD-304).

## Calico Jack Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.club_coins` / `player.has_club_card` / `player.calico_jack.access` / `shop_rows[]` / `action_tiles[]` | Exclude inaccessible or irrelevant casino play before scoring and bind the exact low/high table | live player/team state, Club map actions and shop data; locked 1.6.15 `Club.checkAction` | Casino page confirms Qi coins and the Rarecrow shop dependency | covered_for_read_and_gate_EVD_304 | Automatic demand exists only while `(BC)126` is absent and the Deluxe Scarecrow dependency remains open. |
| `next_round.seed` / `initial_cards[]` / `hidden_dealer_card` / `future_draws[]` / `decision_trace[]` / `expected_result` / `expected_coin_delta` | Replay the complete next native round without consuming production RNG and keep all card mechanics out of model output | locked `CalicoJack` constructor, `getRandomCard`, hit bias, dealer branches and settlement; shared pure cursor model | Wiki gives player-facing rules but not the complete RNG/dealer implementation | covered_for_read_candidate_compile_output_EVD_304 | Fresh compilation rejects seed/card/decision/bet/result drift. Random stream derives from play count, day count and unique ID. |
| `luck_conflict` / `daily_luck` / `luck_level` / `qi_fruit_999_chance` | Preserve the exact scope of luck rather than accepting a broad secondary-source statement | locked Qi fruit 999 branch reads `DailyLuck + LuckLevel`; normal card flow does not | Wiki broadly states luck does not affect Calico Jack | decompile_authoritative_conflict_recorded_EVD_304 | Luck is not injected into ordinary card choice. It remains transparent because the native special reward branch consumes it. |
| `active_game.*` / `native_contract` | Verify table/dialogue/minigame identity, current cards, hit/stand sequence, bet, settlement and single-round quit | live reflected `CalicoJack` private state plus locked native input lifecycle | n/a | covered_for_native_runtime_and_receipt_EVD_304 | Production may call native input methods only; direct card, RNG, ClubCoins, result or statistic writes are forbidden. |

## Island Field Office Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `world_progress.island_field_office.location_id` / `is_current_location` / `north_cave_opened` / `professor_available` / `intro_received_or_pending` / `mutex_locked` / `menu_clear` | Exclude locked, busy, unavailable or wrong-map donations before scoring and drive one-connector rolling routes | locked 1.6.15 `IslandFieldOffice.performAction`, `resetLocalState`, `safariGuyMutex`; live location/mail/menu state | n/a | covered_for_read_and_gate_EVD_302 | Current-map terminal execution requires the professor, Desk and clear mutex/menu; remote execution retains only exact continuation identity. |
| `desk_action_tiles[]` / `pieces[]` / `donated_piece_count` / `donation_candidates[]` / `projection_status` | Bind one inventory fossil to the exact native display slot, stack transition, set identity and fresh compiler request | locked `totalPieces=11`, `FieldOfficeMenu.receiveLeftClick` and item-to-holder order | n/a | covered_for_read_candidate_compile_EVD_302 | `(O)823` resolves 0 then 2; `(O)826` resolves 7 then 6. Changed slot/item/runtime/count/reward projection fails closed. |
| `center_skeleton_restored` / `snake_restored` / `bat_restored` / `frog_restored` / `golden_walnuts_found` / `uncollected_rewards[]` / `finale_ready` / `finale_received_or_pending` | Predict and verify exact native set settlement without direct mutation | locked `IslandFieldOffice.donatePiece`, `TransferDataFromSavedLocation`, collected-nut/mail logic | n/a | covered_for_read_compile_runtime_output_EVD_302 | Includes center `(O)73 x6 + (O)69`, snake `(O)73 x3 + (O)835`, ordinary bat/frog walnuts and 130-found fallback `(O)TentKit`/`(O)926`. |
| `survey_action_tiles[]` / `plants_restored_left` / `plants_restored_right` / `has_failed_survey_today` / `next_survey_kind` / `next_survey_answer` / `survey_candidates[]` | Bind the unique next question, locked numeric answer, native dialogue keys and same-day sequencing | locked `FieldOfficeSurvey`, `answerDialogueAction`, `DayUpdate`; fixed answers 22 and 18 | n/a | covered_for_read_candidate_compile_runtime_output_EVD_303 | Wrong answers are excluded from production and verified only by debug native input; failed-today, complete, busy, locked, stale and unreachable states fail closed. |
| `survey_candidates[].walnut_debris_*` / `golden_walnuts_found_*` / `output_delivery` / `expected_finale_*` | Separate transient native walnut debris creation from shared magnet pickup and durable walnut/finale settlement | locked `ApplyPlantRestoreLeft/Right`, `MarkCollectedNut`, `createItemDebris`, survey finale branch | n/a | covered_for_exact_transient_and_durable_receipt_EVD_303 | Existing walnut debris requires pickup upstream. Below 130, spawn count is one, final debris returns to baseline and GoldenWalnutsFound increments one; at 130 all three deltas are zero. |

## Fruit Tree Harvest Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.terrain_features[].is_fruit_tree` / `fruit_tree_id` / `growth_stage` / `stump` / `max_shake` | Bind one exact harvestable base FruitTree and reject stale or unsafe states upstream | locked 1.6.15 `FruitTree.performUseAction` and `FruitTree.shake`; live terrain feature | Fruit Trees page confirms maturity and harvest lifecycle | covered_for_read_and_gate_EVD_298 | Custom runtime types, immature trees, stumps, empty fruit and active shake fail closed before planning. |
| `fruit_tree_fruit[]` / `fruit_tree_expected_outputs[]` / `fruit_tree_expected_output_quantity_total` | Preserve exact live item identity, quality and quantity for compiler and runtime receipts | live `FruitTree.fruit`, `GetQuality()`, `struckByLightningCountdown`, and shake debris branches | Wiki confirms up to three accumulated fruit, age quality and temporary lightning coal production | covered_for_read_compile_output_EVD_298 | Existing fruit remains harvestable regardless of current season; lightning output is `(O)382`, quality 0, one per fruit slot. |
| `fruit_tree_harvest_status` / `fruit_tree_projection_status` / `fruit_tree_native_contract` / expected after-state and XP delta | Make candidate exclusion and native-only execution auditable | locked native call chain and zero-XP shake implementation | n/a | covered_for_gate_runtime_receipt_EVD_298 | Runtime calls `GameLocation.checkAction` only and verifies fruit count 0, all output deltas and Foraging XP delta 0. |

## Fish Pond Management Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `farm.buildings[].fish_pond.management_*` | Publish exact management readiness, safe/restore slots, supported operations and native contract | locked 1.6.15 `FishPond.doAction`, `PondQueryMenu` and `FishPond.ClearPond`; live pond state | Fish Pond page confirms empty/reset semantics and loss of Golden Animal Cracker | covered_for_read_and_gate_EVD_297 | Output must be empty before management because native output collection has priority over the right-click menu. |
| `netting_style` / `management_cycle_expected_netting_style_after` | Bind the only cosmetic pond management transition | `PondQueryMenu.receiveLeftClick` increments and applies modulo four | n/a | covered_for_read_compile_output_EVD_297 | Explicit player command only; never an autonomous value candidate. |
| `management_empty_expected_*` | Bind exact destructive clear reset and preservation receipt | `ClearPond` fish debris loop and every assignment; negative evidence for untouched max occupants, completion flag, sign, output and netting | Wiki secondary confirms all fish are removed and the cracker is lost | covered_for_read_compile_output_EVD_297 | `empty_pond` needs both general PlayerCommand authorization and `confirm_empty_pond=true`. |
| `menus.menu_specific_state(kind=fish_pond_query)` | Expose bound pond identity, confirmation state and public button bounds | live `PondQueryMenu._pond`, `confirmingEmpty`, and public controls | n/a | covered_for_runtime_diagnosis_EVD_297 | Runtime verifies exact object identity before either menu click. |
| `fishing.manage_fish_pond` | Keep visual/destructive pond management separate from productive pond service | governance, candidate, DailyPlan, compiler and runtime catalogs | n/a | five_gate_closed_player_command_only / excluded_from_training | Reuses fish-pond movement and native interaction infrastructure; does not duplicate `fishing.service_fish_ponds`. |

## Fair Wheel Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.fair_wheel_spin` | Publish the complete active Fall 16 wheel context or an explicit contextual-inactive state | locked 1.6.15 `Event`, `NumberSelectionMenu` and `WheelSpinGame`; live event/player/menu state | Fair page confirms green/orange wagering, 73.3% base green rate and Luck effects | covered_for_read / contextual_outside_festival | Full snapshot keeps the field required. Active menu and wheel private fields are read live; fixed formulas come from locked source. |
| `interaction_tiles` / `dialogue_key` / `response_key` / `native_contract` | Bind Buildings 308/309 and the exact `wheelBet -> Green -> NumberSelectionMenu -> WheelSpinGame` lifecycle | locked `Event.checkAction`, `Event.answerDialogue` and `Event.betStarTokens` | n/a | covered_for_gate_and_output_EVD_296 / fresh_projection_required | Runtime must click the real response and numeric menu; direct menu creation or result mutation is prohibited. |
| `base_zero_luck_distribution` / `random_contract` / `effective_luck_level` | Expose all 30 zero-luck constructor outcomes, deceleration, win arcs and LuckLevel retry gates | exhaustive locked constructor enumeration plus `WheelSpinGame.update` | Fair page independently reports 22/30 green and the Luck/15 green correction | covered_for_read_and_compile_EVD_296 | `DailyLuck` is not consumed by this code path. Random win/loss remains feedback, not an execution-success invariant. |
| `wager_policy` / `zero_luck_kelly_fraction` / `remaining_star_token_demand` | Size a green wager without overshooting the automatic Stardrop demand | exact `22/30` source distribution and even-money Kelly derivation `2p-1=7/15`; current tokens and shared Fair demand | Fair page independently reports a 46.7% current-token wager | covered_for_candidate_and_compile_EVD_296 | Wager is `min(demand, floor(score*7/15))`. Demand below two is excluded; exact one reuses the free strength game. |
| `active_wheel` / `active_number_selection` | Record native wager input, velocity, rotation, timer, done/result state and settlement | live reflected fields bound to exact runtime types | n/a | covered_for_read_and_receipt_EVD_296 | Production writes only the visible native number textbox and sends menu clicks; RNG, rotation and festivalScore are never written. |
| `festival.spin_wheel` / `executor.spin_fair_wheel` | Separate economic scheduling from one stochastic native wheel session | candidate/DailyPlan/compiler/capability registry; shared movement and native menu executor | n/a | strategy_value_allowlisted_policy_confirmed / executor_calibration_only | Both native outcomes are accepted only after exact score delta, result text, cleanup and fresh replan receipts. |

## Fair Strength Game Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.fair_strength_game` | Publish the complete active Fall 16 StrengthGame context or an explicit contextual-inactive state | locked 1.6.15 `Event` and `StrengthGame`; live event/player/minigame state | Fair page confirms the free strength minigame and fixed top/bottom one-token result | covered_for_read / contextual_outside_festival | Full snapshot keeps the field required. Fixed rules come from locked source; power, direction, speed, timers and result state come from the live instance. |
| `interaction_tiles` / `stand_tile_x` / `native_contract` | Bind Buildings 540, required player TilePoint.X 29, exact menu creation, swing callback, score and result exit | locked `Event.checkAction`, `StrengthGame.receiveLeftClick`, `FarmerSprite.animateOnce`, `StrengthGame.afterSwingAnimation` | n/a | covered_for_gate_and_output_EVD_295 / fresh_projection_required | Runtime must enter through the real festival action and may not bypass the original click/result lifecycle. |
| `power_contract` / `active_minigame` | Publish power 0..100, changeSpeed 3/4, direction reversals, animation 168/80ms/8, end timer and live clicked/result state | locked constructor/update/click/callback paths plus reflected live private fields | n/a | covered_for_read_and_receipt_EVD_295 | Production predicts the state after nine native updates and clicks once at a projected power of at least 99; no field or result writes are allowed. |
| `shop_rows[]` / `remaining_star_token_demand` | Reuse the complete Fair shop while bounding this fixed one-token action to an exact Stardrop top-up | live shop stock; locked conditions; current mail/tokens and Grange projection | Fair shop table confirms `(O)434` costs 2000 Star Tokens | covered_for_candidate_and_compile_EVD_295 | A deficit other than exactly one is excluded upstream because this action has a fixed one-token return. |
| `festival.play_strength_game` / `executor.play_fair_strength_game` | Separate the exact-one-token scheduling decision from one native maximum-power attempt | candidate/DailyPlan/compiler/capability registry; shared movement and native minigame executor | n/a | strategy_value_allowlisted / executor_calibration_only | The executor handles movement, timing and one click; only the high-level action enters strategy training. |

## Fair Slingshot Game Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.fair_slingshot_game` | Publish the complete active Fall 16 TargetGame context or an explicit contextual-inactive state | locked 1.6.15 `Event`, `TargetGame`, `Slingshot`, shop data; live event/player/minigame state | Fair page confirms the 50g slingshot minigame and Star Token shop | covered_for_read / contextual_outside_festival | Full snapshot keeps the field required. Fixed rules come from locked source; current targets/projectiles/timers come from the live instance. |
| `interaction_tiles` / `dialogue_key` / `native_contract` | Bind Buildings 501/502, `slingshotGame`/`Play`, the exact menu click path and native festival return | locked `Event.checkAction`, `DialogueBox.receiveLeftClick`, `Event.answerDialogue`, `TargetGame.unload` | n/a | covered_for_gate_and_output_EVD_294 / fresh_projection_required | Runtime must use the real festival response and exact 50g receipt. |
| `target_sequence[]` / `active_minigame.targets[]` | Bind all 79 scheduled targets plus every live target's type, rectangle, speed, motion, pause and spawn state | locked TargetGame constructor/update and live reflected private state | n/a | covered_for_read_and_receipt_EVD_294 | Predictive intercept consumes live state. The compiler binds the exact schedule count and contract rather than accepting a partial projection. |
| `scoring_contract` / `temporary_loadout` | Publish target values, shot counters, accuracy denominator, multiplier thresholds, reward branches, four timers, temporary slingshot/ammo and projectiles | locked `TargetGame`, `BasicProjectile`, `Slingshot`; live minigame state | Fair page confirms score/accuracy converts to Star Tokens | covered_for_read_and_output_EVD_294 | Runtime recomputes the full result and never writes score, accuracy, tokens, money, timers, targets or inventory. |
| `shop_rows[]` / `remaining_star_token_demand` | Reuse the complete Fair shop and bound repeat demand to the unacquired Stardrop | live shop stock; locked conditions; current mail/tokens and Grange projection | Fair shop table confirms `(O)434` costs 2000 Star Tokens | covered_for_candidate_and_compile_EVD_294 | Shared with Fair fishing. Other shop rows remain transparent but do not automatically request repeated minigames. |
| `festival.play_slingshot_game` / `executor.play_fair_slingshot_game` | Separate economic scheduling from one complete native 50-second TargetGame | candidate/DailyPlan/compiler/capability registry; shared movement and SlingshotAimPatch | n/a | strategy_value_allowlisted / executor_calibration_only | The ordinary-mine slingshot and Fair TargetGame share one aim override; no second projectile system exists. |

## Fair Fishing Game Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.fair_fishing_game` | Publish the complete active Fall 16 fishing-game context or an explicit contextual-inactive state | locked 1.6.15 `Event`, `FishingGame`, `BobberBar`, shop data; live event/player/minigame state | Fair page confirms the 50g fishing minigame and star-token shop | covered_for_read / contextual_outside_festival | Full snapshot keeps the field required. Missing world/player data is unavailable; a non-Fall-16 context is explicitly contextual, not missing. |
| `interaction_tiles` / `dialogue_key` / `native_contract` | Bind Buildings 503/504, `fishingGame`/`Play`, the exact menu click path and native festival return | locked `Event.checkAction`, `DialogueBox.receiveLeftClick`, `Event.answerDialogue`, `FishingGame.unload` | n/a | covered_for_gate_and_output_EVD_293 / fresh_projection_required | The runtime uses the real clickable response after transition and safety timers settle; ordinary `GameLocation.answerDialogue` is not a valid festival substitute. |
| `scoring_contract` / `active_minigame` | Publish raw fish points, perfection bonus, three-perfect multiplier, reward formula, private timers, native loadout and live rod state | locked `Event.caughtFish`, `Event.perfectFishing`, `FishingGame.update`; live reflection over exact private timers | Fair page confirms score converts to Star Tokens | covered_for_read_and_receipt_EVD_293 | Random fish/perfection outcomes remain feedback. Runtime recomputes the exact result and never writes score, perfections, tokens or money. |
| `shop_rows[]` / `remaining_star_token_demand` | Expose every current Fair star-token shop row while bounding automatic repeat demand to the unacquired Stardrop | live `ShopBuilder.GetShopStock`; locked shop conditions; current mail, tokens and Grange projection | Fair shop table confirms the Stardrop costs 2000 Star Tokens | covered_for_candidate_and_compile_EVD_293 | Current tokens and projected unclaimed Grange prize reduce the demand. Other shop rows are strategy inputs, not automatic repeat targets. |
| `festival.play_fishing_game` / `executor.play_fair_fishing_game` | Separate economic scheduling from one complete native 100-second session | candidate/DailyPlan/compiler/capability registry; shared movement and predictive legal-input controller | n/a | strategy_value_allowlisted / executor_calibration_only | Input runs before native physics. Perfection ratio is recorded as stochastic output; native lifecycle/formula mismatches fail closed. |

## Grange Display Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.grange_display` | Publish the complete active Fall 16 shared-display context or an explicit contextual-inactive state | locked 1.6.15 festival event and `FarmerTeam`; live event/team state | Grange Display page confirms Fall 16, nine slots and shared multiplayer display | covered_for_read / contextual_outside_festival | Full snapshot keeps the field required; outside the festival it is contextual, not missing. |
| `display_rows[]` / `rows[]` | Bind every display slot and every eligible inventory unit with exact identity, quality, category, actual sell price and item points | live `grangeDisplay`, inventory and `sellToStorePrice(-1L)`; locked scoring code | Wiki confirms actual sell price, quality and category diversity affect score | covered_for_read_and_gate_EVD_292 / fresh_projection_required | Mayor shorts are published but excluded from the optimizer because native score becomes -666. |
| `current_projected_score` / `best_available_score` / `scoring_contract` | Reproduce the exact base, item-count, category and item scoring and prove whether first place 90 is reachable | locked judging branches and deterministic optimizer | Wiki confirms 90/75/60 prize thresholds and nine-item scoring | covered_for_read_and_compile_EVD_292 | The optimizer selects at most nine concrete units and prefers preserving equivalent items already on display. |
| `next_operation` / `projection_fingerprint` | Bind exactly one place/remove operation and reject stale inventory, display, judging or scoring state | transparent projection plus action compiler drift checks | n/a | covered_for_gate / one_fresh_snapshot_mutation_only | Before judging it converges on the best available set; after judging it retrieves occupied slots. It never initiates judging. |
| `interaction_tiles` / `mutex_*` / `native_contract` | Bind the live Buildings interaction endpoint, shared lock and original menu lifecycle | locked `Event.checkAction`; live map tiles and `FarmerTeam.grangeMutex` | Wiki confirms the display is shared in multiplayer | covered_for_gate_and_output_EVD_292 | A lock held by another player, missing endpoint, open menu, busy player or non-nine-slot display fails closed. |
| `festival.manage_grange_display` / `executor.manage_grange_display` | Separate strategic festival scheduling from one mechanical native menu mutation | candidate/DailyPlan/compiler/capability registry; shared movement and `StorageContainer` executor | n/a | strategy_value_allowlisted / executor_calibration_only | Production does not directly write display, inventory, score or judging state. |

## Warp Totem Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.warp_totem.rows[]` | Bind every exact base Farm, Mountain, Beach, Desert and Island Warp Totem slot and its row-specific destination | locked 1.6.15 `Data/Objects`, `Data/CraftingRecipes`, `Object.performUseAction`; live inventory/map state | Warp Totem pages confirm the five destinations and consumption | covered_for_read / fresh_projection_required | Multiple variants may coexist; each row owns its route so a selected slot cannot bind another totem's destination. |
| `destination_route` | Publish Farm map-property/fallback coordinates, fixed destinations, map-width correction, active festival entry and ordered passive replacements | locked `Object.totemWarpForReal`, `Game1.warpFarmer`, festival data; live farm/festival/map state | Beach page confirms festival-sensitive use | covered_for_read_and_gate_EVD_291 | Pre-start consume-without-warp, multiplayer ReadyCheck and exact-destination no-op fail upstream. Single-player festival entry and passive replacement remain native-supported routes. |
| `native_animation_contract` | Bind facing, 2000ms animation, 1000ms callback, deterministic sprite counts and visual-only random domains | locked `Object.performUseAction`, `totemWarp`, `totemWarpForReal` | n/a | covered_for_read_and_gate_EVD_291 / native_runtime_verified | Initial effects are 3 item sprites plus 65 sprinkles; later poofs/trails do not affect routing. |
| `executor.use_warp_totem` | Consume one exact selected totem through native object use and verify destination, festival route and restored player state | shared `UseInventoryObjectNative`; delayed native receipt | n/a | five_gate_closed_EVD_291 / executor_calibration_only | Upstream owns travel value and destination choice. No direct warp, position, inventory, visibility, invincibility or audio write is permitted. |

## Treasure Totem Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.treasure_totem.rows[]` | Bind every exact base `(O)TreasureTotem` inventory slot and common native-use state | locked 1.6.15 `Data/Objects`, `Object.performUseAction`; live inventory/player state | Treasure Totem page confirms recipe, outdoor use and surrounding spots | covered_for_read / fresh_projection_required | Each row retains slot, stack, item identity and the shared object-use gate; subclasses and stale slots fail closed. |
| `player.treasure_totem.spawn_projection` | Publish all 16 rounded-distance-3 tiles and every native membership predicate | locked `Object.treasureTotem`; live map/object/layer/bush/tile-property state | Treasure Totem page confirms up to 16 tillable outdoor spots | covered_for_read_and_gate_EVD_290 | Each tile records placement, occupancy, `AlwaysFront`/`Front`, bush, Diggable and winter-grass results. Visual RNG is excluded because it cannot change spot membership. |
| `player.treasure_totem.ring_contract` | Bind center, exact scan/ring rule, output `(O)590`, current spot counts and `TreasureTotemsUsed` transition | locked `Object.treasureTotem`; artifact-spot deterministic seed consumers | Artifact Spot page confirms the world counter affects later fixed drops | covered_for_read_and_gate_EVD_290 / native_runtime_verified | The native `name.Equals("Forest")` operand is the item name and is false for the base item; the projection preserves this exact behavior. |
| `executor.use_treasure_totem` | Consume one exact totem through native object use and verify the exact generated set plus counter | shared `UseInventoryObjectNative`; native map/counter receipt | n/a | five_gate_closed_EVD_290 / executor_calibration_only | Upstream owns location/value selection. Generated spots flow into existing `ArtifactSpots` transparency and `executor.clear_obstacle`; no duplicate digging executor is allowed. |

## Return Scepter Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.return_scepter.rows[]` | Bind every exact base `(T)ReturnScepter` `Wand` slot, runtime type, nonstackable identity, instant-use and sound flags | locked 1.6.15 `Data/Tools`, `Tool.cs`, `Wand.cs`; live inventory | Return Scepter page confirms non-consumable use | covered_for_read / fresh_projection_required | Stack remains 1; no inventory reduction is synthesized. |
| `destination` | Publish the current player's home location/type and exact native Farm front-door tile | `Utility.getHomeOfFarmer`; `FarmHouse.getFrontDoorSpot`; `Cabin` branch | Return Scepter page confirms farmhouse versus own cabin destination | covered_for_read_and_gate_EVD_289 | The destination location is native `Farm`, but the tile is resolved from the player's own home every time. Missing home and exact-destination no-op fail upstream. |
| `native_wand_gate` / `executor_base_gate` | Separate Wand's bathing/bridge rules from the stable executor invocation boundary | locked `Wand.DoFunction`; player tool input path; live player/menu state | Wiki history confirms the former bathing-suit freeze fix | covered_for_read_and_gate_EVD_289 | Stable execution additionally excludes active tool/menu/dialogue/minigame/event/fade/swim/sit/mount/walk-only transients. |
| `animation_contract` / `state_transition_contract` | Bind 12 poofs, 17 trail sprites, bounded random domains, 1000ms callback, 2000ms freeze and exact instant-tool ordering | `Wand.DoFunction`; `Tool.beginUsing`; `Game1.toolAnimationDone` | Wiki confirms the scepter skips the Warp Totem animation | covered_for_read_and_gate_EVD_289 / native_runtime_verified | `Tool.beginUsing` restores `CanMove=true` after `Wand.DoFunction`; `freezePause` remains the effective hold. Random coordinates/delays are domains, never predicted values. |
| `executor.use_return_scepter` | Execute one exact native immediate-tool use and verify own-home landing, settled player state and unchanged Wand identity | native `Farmer.BeginUsingTool`; delayed `wandWarpForReal` callback | n/a | five_gate_closed_EVD_289 / executor_calibration_only | Upstream owns whether returning home has route/time value. No direct warp, position, invincibility, visibility, movement or inventory mutation is permitted. |

## Rain Totem Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.rain_totem.rows[]` | Bind every exact base `(O)681` inventory slot, runtime type, stack transition and temporary visibility | locked 1.6.15 `Object.performUseAction`; live inventory | Rain Totem page confirms consumed-on-use | covered_for_read / fresh_projection_required | Successful native caller behavior consumes exactly one item; blocked planning branches consume none. |
| `context_routing` | Separate source context, configured affected context, native decision context and actual weather-state owner | `LocationContextData.AllowRainTotem`; `RainTotemAffectsContext`; `Object.rainTotem` | Modding:Location contexts confirms both data fields and Desert-to-valley routing | covered_for_read_and_gate_EVD_288 | For a non-Default redirected mod context, native code still writes the current location weather instance; the owner field therefore need not equal the decision target. |
| `weather_transition` | Publish immediate weather write plus tomorrow date and final effective weather after known new-day normalization | `Object.rainTotem`; `Game1.getWeatherModificationsForDate`; `Data/PassiveFestivals`; live date/weather | Rain Totem and Weather pages confirm regional weather, festivals and first-of-season caveats | covered_for_read_and_gate_EVD_288 / native_runtime_verified | Default-context overrides are excluded before consumption. Independent non-Default contexts preserve the native Rain write for the coming day. |
| `animation_contract` | Bind deterministic facing, 2000ms animation, cloud/item counts and sound timing | locked 1.6.15 `Object.rainTotem` | n/a | covered_for_read_and_gate_EVD_288 | Random sprite coordinates are not predicted or advanced by policy code. |
| `executor.use_rain_totem` | Execute one valid routed Rain Totem through native use and verify inventory, weather owner, dialogue and control settlement | shared `UseInventoryObjectNative`; exact native `Farmer.canMoveNow` callback recovery | n/a | five_gate_closed_EVD_288 / executor_calibration_only | Upstream owns weather value and timing. The primitive is not an autonomous candidate or policy-training option. |

## Horse Flute Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.monster_musk.rows[]` | Bind every exact base `(O)879` inventory slot, runtime type, stack transition and temporary visibility | locked 1.6.15 `Object.performUseAction` plus live inventory | secondary comparison pending | covered_for_read / fresh_projection_required | Successful native caller behavior consumes exactly one item. |
| `player.monster_musk.buff_contract` / `active_buff` | Publish Buff 24 data and the current replaceable instance without guessing elapsed time | `DataLoader.Buffs("24")`; `Buff`; `BuffManager.Apply`; live `AppliedBuffs` | secondary comparison pending | covered_for_read_and_gate_EVD_287 | Duration is 600000ms, max -1, non-debuff, icon 24, glow `#2000203F`, empty effects/actions. Reapply removes and replaces the same ID. |
| `player.monster_musk.spawn_semantics` / `animation_contract` | Bind all deterministic downstream and timing semantics while leaving sprite X motion as a random domain | `MineShaft.populateLevel`; `VolcanoDungeon.GenerateContents`; `Object.MonsterMusk` | n/a | covered_for_read / covered_for_gate / native_runtime_verified | Buff 24 doubles ordinary-mine and volcano monster chance. Ordinary mine retains the native Buff 23 interaction. Use faces down, freezes 1750ms and applies at the 750ms callback. |
| `executor.use_monster_musk` | Consume one item through native use, wait for complete settlement, and verify a new full-duration Buff 24 instance | shared `UseInventoryObjectNative`; no direct production Buff, sprite, sound or inventory mutation | n/a | five_gate_closed_EVD_287 / executor_calibration_only | Upstream combat/resource goals decide whether increased monster density is useful. This primitive is not an autonomous candidate or policy-training option. |
| `player.horse_flute.rows[]` | Bind every exact base `(O)911` inventory slot, runtime type, stack and temporary-visibility state | locked 1.6.15 `Object.performUseAction` common gate and live inventory | secondary comparison pending | covered_for_read / fresh_projection_required | Temporary invisibility is part of the projection fingerprint; the reusable item reports equal before/after stacks. |
| `native_base_use_gate` / `horse_warp_restrictions` / `summon_rectangle` | Publish all knowable start restrictions and native error precedence | `Object.performUseAction`; `Utility.GetHorseWarpRestrictionsForFarmer` | secondary comparison pending | covered_for_read_and_gate_EVD_286 | Restriction bits are no-owned-horse=1, indoors=2, no-room=4 and in-use=8; native error precedence is preserved. The delayed callback rechecks the same native function at execution time. |
| `owned_horse` / `expected_result` / timing and facing fields | Bind the exact globally found owned horse and distinguish adjacent success from delayed summon | `Utility.findHorseForPlayer`; `FarmerTeam.OnRequestHorseWarp`; live Horse identity/location/rider/mutex | n/a | covered_for_read / covered_for_gate / native_runtime_verified | Remote use faces direction 2 and schedules 1500ms; adjacent use leaves position and facing unchanged. Mutex state is observable but not invented as a native start restriction. |
| `executor.use_horse_flute` | Execute the complete native reusable-flute branch and verify exact horse, landing, facing and inventory receipts | native `Object.performUseAction` plus caller `reduceActiveItemByOne`; no direct production warp/event/position mutation | n/a | five_gate_closed_EVD_286 / executor_calibration_only | Strategy may choose this movement accelerator, but does not learn or emit the mechanical team-event implementation. |

## Firework Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.firework_placement.supported_variants[]` / `rows[]` | Publish every exact base inventory firework and bind `(O)893/(O)894/(O)895` to type `0/1/2` and source X `256/272/288` | locked 1.6.15 `Object.placementAction` switch branch and live inventory | secondary comparison pending | covered_for_read / full_profile / fresh_projection_required | The field is computed only for the current loaded location and only scans placement ranges when inventory contains a supported firework. |
| `rows[].locations[].static_legal_tile_ranges` / `temporary_sprite_blocked_tiles` | Separate static native placement legality from the branch's exact transient sprite-position rejection | live `canBePlacedHere` probe and `location.temporarySprites` exact `target*64` comparison | n/a | covered_for_gate_EVD_285 | Compiler and runtime both reject the exact transiently occupied tile. |
| `random_outcome_contract`, rocket ID and acceleration domains | Publish every knowable random domain without consuming or guessing shared RNG | locked `Game1.random.Next()` calls in the firework branch | n/a | covered_for_read_and_output / no_rng_advance | ID is `20..30`; Y acceleration is `-0.36..-0.27` in `0.01` steps. Exact next values exist only after native execution. |
| `executor.use_firework` | Execute one explicit three-color firework through the shared native placement caller | adjacent movement, shared `CanPlaceInventoryObjectNative` / `PlaceInventoryObjectNative`, strict five-sprite receipt | secondary comparison pending | five_gate_closed_EVD_285 / player_command_only | No direct sprite broadcast, audio call, inventory mutation, autonomous candidate or policy-training admission is permitted. |

## Secret Note Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.secret_note_candidates.note_catalog[]` | Publish every live `Data/SecretNotes` row, raw content hash, journal classification and native display shape | locked 1.6.15 `DataLoader.SecretNotes` and `LetterViewerMenu(int)` | secondary comparison pending | covered_for_read / complete_live_catalog | Raw content is also published; SHA-256 is the stable compiler/runtime identity. |
| `rows[].unseen_note_ids_native_order_json` / `selected_note_id` | Bind the exact normal-note seeded choice or minimum journal-scrap choice | locked `Utility.GetUnseenSecretNotes` and `Object.performUseAction` | secondary comparison pending | covered_for_read_and_compile / fresh_projection_required | Normal seed inputs are game id, player multiplayer id and unseen count times 777. |
| `rows[].expected_quest_id` and before/after flags | Verify native note 10 quest 30 and note 23 quest 29 side effects | locked `Object.performUseAction` cases 10 and 23 | secondary comparison pending | covered_for_gate_and_output_EVD_284 | Mail `qiCave` and event `2120303` suppress the respective additions exactly as vanilla does. |
| `menus.menu_specific_state.secret_note_image` / `which_bg` | Verify the native LetterViewer image/text branch after use | live `LetterViewerMenu` public fields | n/a | covered_for_output_EVD_284 | Text notes require non-empty native pages; image notes bind the parsed image index. |
| `executor.read_secret_note` | Consume one exact note through the shared native inventory-object use caller | shared `UseInventoryObjectNative`; strict note/quest/menu/inventory receipt | secondary comparison pending | five_gate_closed_EVD_284 / executor_calibration_only | No direct seen-note, quest, menu or inventory mutation is permitted in the executor. |

## Grass Placement Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.grass_placement.rows[]` | Publish every inventory normal/blue grass starter, exact native result identity, and current loaded map legal tile ranges | locked 1.6.15 `Object.placementAction`; live inventory, object/terrain maps and native `canBePlacedHere` probe | secondary comparison pending | covered_for_read / full_profile / fresh_projection_required | `(O)297` binds type 1; `(O)BlueGrassStarter` binds type 7; both bind four initial weeds and `dirtyHit`. |
| `current_location.terrain_features[].grass_type` / `.number_of_weeds` | Verify the exact native post-state instead of inferring success from runtime return alone | live `Grass.grassType` and `Grass.numberOfWeeds` net fields | n/a | covered_for_read / output_gate_EVD_283 | The runtime receipt and next fresh snapshot must agree. |
| `grass_layout_reason`, exact target/stand and passable layout projection | Keep purpose and layout decisions upstream while restricting the executor to one mechanical placement | placement compiler and shared `StoragePlacementLayoutProjection` | n/a | covered_for_compile_rebind / fail_closed_on_drift | No autonomous grass candidate is generated by this primitive. Time, livestock-feed layout, protection and expansion remain planning responsibilities. |
| `executor.plant_grass` | Execute one rebound grass starter through the shared native placement path | adjacent movement, `CanPlaceInventoryObjectNative`, `PlaceInventoryObjectNative`, strict runtime receipt | secondary comparison pending | five_gate_closed_EVD_283 / executor_calibration_only | Production code does not directly add Grass, set its fields, or edit inventory. |

## Invocation Source Governance

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `OptionSpec.InvocationPolicy` / `OptionCapabilityDeclaration.InvocationPolicy` | Separate executable actions from actions the policy model may autonomously select | `OptionContracts.cs`; generated capability registry v3; governance catalog | n/a; project governance is authoritative | covered_for_registry / covered_for_generation / fail_closed_on_mismatch | Values are `PolicyOrAutonomous` and `PlayerCommandOnly`. Governance and generated capability declarations must agree during registry initialization. |
| `OptionAvailabilityCandidate.InvocationSource` | Prove that a player-command-only request actually came from the explicit player command surface | availability evaluator and `SafetyPolicyGate` | n/a | covered_for_gate / explicit_player_command_required | `Unknown` and `Policy` are rejected for `PlayerCommandOnly`; only `PlayerCommand` may continue to the existing confirmation and runtime gates. `InternalCompiler` is not a bypass. |
| `TrainingAdmissionExclusionReason.PlayerCommandOnly` | Prevent executable decoration/layout actions from contaminating strategy training | capability registry v3, KnowledgeCompiler manifest, dataset admission filter | n/a | covered_for_training_exclusion / typed_reason_required | Runtime evidence remains useful for executor verification but its training evidence scope is `not_admitted`. Current IDs cover building appearance, House Plant rotation, Singing Stone playback, furniture placement, sign display assignment and text editing. |

## Farm Computer Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.objects[].farm_computer_report` | Publish the complete location-dependent Farm Computer state without requiring policy code to open or parse a menu | locked 1.6.15 `Object.CheckForActionOnFarmComputer`, `ShowFarmComputerReport` and native aggregate getters; live object/location/farm state | Official Farm Computer page confirms the report is location-dependent and the object can be placed outside the Farm | covered_for_read / root_location_exact / localized_digest | Publishes crops, open hoe dirt, ready/unwatered crops, conditional greenhouse crops, forage, ready machines, hay/capacity, cave readiness, exact native timing and localized report SHA-256. |
| `farming.read_farm_computer_report` | Compile one explicit player request into shared movement and exactly one native delayed report action | DailyPlan/compiler, `NativeObjectInteractionMovement`, runtime harness | Official player-facing report behavior used only as secondary confirmation | covered_for_compile_rebind / five_gate_closed_EVD_280 / training_excluded_player_command_only | Compiler rebinds every mechanical and report field from a fresh snapshot. Runtime waits for the native `DialogueBox`; production does not synthesize a report or mutate the menu. |

## Flute Block Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.objects[].flute_block_tuning` | Expose exact persistent pitch state, next step and independent playback entry | locked 1.6.15 `CheckForActionOnFluteBlock`, `getFluteBlockSoundFromHeldObject`, `farmerAdjacentAction` | secondary comparison pending | covered_for_read / deterministic_25_state_cycle | Publishes raw/parsed/current/next pitch, `0..2400 step 100`, base cue, held-object override boundary, shake/scale and safe stands. |
| `world.tune_flute_block` | Execute one explicit native tuning step without direct state mutation | DailyPlan/compiler, shared movement, runtime harness | secondary comparison pending | five_gate_closed_EVD_281 / training_excluded_player_command_only | Empty/tool safe slot forces base `flute` cue; adjacent playback remains separate and is never used as tuning. |

## Drum Block Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.objects[].drum_block_tuning` | Expose exact persistent tone state, next step, matching sound cue and independent playback entry | locked 1.6.15 `CheckForActionOnDrumBlock` and `farmerAdjacentAction` | secondary comparison pending | covered_for_read / deterministic_7_state_cycle | Publishes raw/parsed/current/next tone, `0..6 step 1`, `drumkitN`, shake/scale and safe stands. |
| `world.tune_drum_block` | Execute one explicit native tuning step without direct state mutation | DailyPlan/compiler, shared Note Block runtime | secondary comparison pending | five_gate_closed_EVD_282 / training_excluded_player_command_only | Compiler rebinds all fields from the fresh projection. Runtime shares one native state machine with Flute but retains exact Drum identity and tone validation. |

## Singing Stone Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.objects[].singing_stone_interaction` | Distinguish exact base `(BC)94` from same-name furniture and expose every mechanical input without advancing shared RNG | locked 1.6.15 `Object.checkForAction` / `CheckForActionOnSingingStone`; live object/location/player state | n/a; decompile and native runtime are authoritative for hidden RNG semantics | covered_for_read / player_command_only / exact_next_pitch_unavailable | Publishes `crystal`, uniform `0..2300 step 100`, 24 outcomes, `Game1.random_shared_unread`, expected `shakeTimer=100`, native return, permanent identity and safe stands. |
| `world.play_singing_stone` | Compile one explicit player request into shared movement and exactly one native location action | DailyPlan/compiler, `NativeObjectInteractionMovement`, runtime harness | n/a | covered_for_compile_rebind / five_gate_closed_EVD_274 / training_excluded_player_command_only | Compiler never substitutes a different stone. Production never calls sound/RNG directly or writes the timer; runtime receipt is executor evidence only. |

## Feed Hopper Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.objects[].feed_hopper_withdrawal` | Expose every current input to exact native Feed Hopper withdrawal | locked 1.6.15 `Object.checkForAction` / `CheckForActionOnFeedHopper`; live Animal House, root location, player inventory and object state | n/a; decompile and native runtime are authoritative | covered_for_read / covered_for_gate / five_gate_closed_EVD_276 | Publishes exact base identity, root location ID, silo hay, animal count/limit, placed hay, unfed animals, expected native quantity and post-state, exact-stack inventory acceptance, safe stands and native contract. |
| `animals.withdraw_feed_hopper_hay` | Compile one useful animal-feeding withdrawal into shared movement and exactly one native location action | candidate, DailyPlan/compiler, typed runtime and conservation receipt | n/a | covered_for_compile_rebind / autonomous_when_unfed / training_admitted_EVD_276 | Upstream excludes zero unfed animals and all native failure states. Production never writes silo or inventory directly; receipt requires equal opposite deltas and unchanged hopper identity. |

## Auto-Grabber Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.objects[].auto_grabber_collection` | Publish the exact native `(BC)165` held-Chest transaction without opening the menu or mutating inventory | locked 1.6.15 `Object.checkForAction` / `CheckForActionOnAutoGrabber`; live object, Chest and player inventory | n/a; decompile and native runtime are authoritative | covered_for_read / covered_for_gate / five_gate_closed_EVD_278 | Publishes all source stack identities and unit-state hashes, a cumulative inventory-capacity simulation, transferable and remaining partitions, quantities, safe stands and the native menu contract. |
| `animals.collect_auto_grabber_contents` | Compile useful collection into shared movement plus exact native `ItemGrabMenu` clicks | candidate, DailyPlan/compiler, typed request, runtime fixture/smoke and conservation verifier | n/a | covered_for_compile_rebind / training_admitted_EVD_278 | Empty/all-rejected/no-stand states are excluded upstream. Runtime never writes inventory or Chest directly and requires strict partition identity, quantity conservation, inventory receipts, unchanged object/Chest identity and restored selected slot. |

## Cut-Content Compatibility Placeholders

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `executor.toggle_lantern` / `executor.use_raft` | Preserve recognized removed native actions for possible mod compatibility without corrupting the vanilla denominator | locked 1.6.15 source/content scan; compatibility placeholder catalog; generated inventory | Stardew Wiki modding pages identify both as cut tools | compatibility_placeholder / excluded_from_vanilla_denominator_and_training | `Data/Tools` contains Lantern and omits Raft, but neither has a normal save acquisition route. A reachable mod adapter must provide a new independent closure before activation. |

## Slime Ball Collection Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.objects[].slime_ball_collection` | Select one exact natural ready Slime Hutch output without guessing identity, RNG or output | locked 1.6.15 `SlimeHutch.DayUpdate`, `Object.checkForAction`, `CheckForActionOnSlimeBall`; live object/location/player state | n/a; hidden RNG and native dispatch are decompile/runtime authoritative | covered_for_read / covered_for_candidate / native_runtime_verified | Requires exact base `SlimeHutch`, exact base Object, `(BC)56`, `Fragility=2`. Publishes generation contract, day/game/tile seed, exact `(O)766` and `(O)557` quantities, stands and native action contract. |
| `farming.collect_slime_ball` | Compile one high-level collection goal into shared movement plus one native object interaction | DailyPlan and ActionQueue compiler; shared BFS; runtime harness | n/a | covered_for_daily_plan / covered_for_compile_rebind / five_gate_closed_EVD_272 / training_admitted | The compiler rebinds the exact requested tile and never substitutes another ball. Production does not remove the object or create debris directly. |
| `conserved_output[(O)766]` / `conserved_output[(O)557]` | Verify native generation even if some debris enters inventory before the receipt is sampled | live inventory plus current-location debris multiset | n/a | covered_for_output_receipt / shared_pickup_handoff | Success requires exact deltas across inventory and debris. Remaining debris belongs to the existing `executor.pickup_debris`; no second pickup implementation exists. |

## Object Trap Recovery Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.object_trap_recovery` | Detect the exact native four-cardinal object trap without guessing from failed movement | locked 1.6.15 `Object.checkForAction`; live `Game1.player.TilePoint`; current-location object table and virtual `isPassable` results | n/a; decompile is authoritative for the hidden branch | covered_for_read / covered_for_gate / live_snapshot_schema_verified | Publishes each N/E/S/W tile, object presence/passability, qid, runtime/object type, `checkForAction` declaring type, interaction-state gates and the destructive null-tool contract. |
| `player.object_trap_recovery.destructive_native_fallback_enabled` | Prevent the original anti-trap branch from silently destroying a placed asset | base `Object.performToolAction(null)` removes the exact object and emits cosmetic radial debris without returning the asset | n/a | covered_for_read / hard_disabled | Always false. Subclass-specific null-tool behavior is not generalized into a recovery executor. |
| `recovery.escape_object_trap` | Turn one fully mechanical recovery command into a deterministic safe machine-removal action | strict recovery compiler; candidate and daily-plan bindings; existing `executor.remove_machine` contract | n/a | covered_for_candidate / covered_for_daily_plan / covered_for_compile / policy_authorization_required / dedicated_runtime_calibration_pending | If no target is supplied, the compiler selects the first direction-ordered adjacent row whose exact machine projection is currently safe. It then reuses the sole machine-removal runtime. No second mutation path exists. |

## Tent Sleep Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.large_terrain_features[type=Tent]` sleep contract | Bind one exact loaded base Tent to its native interaction geometry and overnight lifecycle | `Tent.performUseAction`, `Tent.getBoundingBox`, `Tent.isPassable`, `Tent.dayUpdate`, `Tent.tickUpdate`; live runtime type/health/anchor | n/a; locked 1.6.15 decompile plus runtime are authoritative | covered_for_read / covered_for_gate / native_runtime_verified | Publishes sleep location, anchor, native 3x2 bounds, canonical interaction tile, canonical stand `anchor+(0,1)`, direction up, health, passability, question `SleepTent`, confirm action `SleepTent_Yes`, native gates and overnight destruction contract. |
| `player.temporary_sleep` | Distinguish a temporary-bed transaction from farmhouse sleep and verify the exact wake/reset lifecycle | `Farmer.isInBed`, `sleptInTemporaryBed`, `lastSleepLocation`, `lastSleepPoint`, `timeWentToBed`; `GameLocation.doSleep/CanWakeUpHere`; `SaveGame` and client reset | n/a | covered_for_read / covered_for_gate / cross_day_receipt_verified | Before execution the flag must be false. Runtime must observe it true after native confirmation, then exactly one new day, the same sleep location/point, visible farmer and the flag reset after save settlement. |
| `menus.tent_sleep_prompt_context` | Verify that native Tent interaction opened the exact prompt before confirmation | `Game1.activeClickableMenu as DialogueBox`; `GameLocation.lastQuestionKey`; `SleepTent_Yes` response branch | n/a | covered_for_read / covered_for_gate / native_runtime_verified | Carries prompt-open state, exact question/action keys and whether confirmation is currently legal. It is separate from `menus.sleep_prompt_context` for a farmhouse bed. |
| `recovery.sleep_in_tent` | Compile and execute the exact terminal temporary-bed transaction without model-generated mechanical input | strict Tent compiler; shared adjacent movement/input; native `GameLocation.checkAction`; shared sleep/shipping/save settlement | n/a | covered_for_compile / terminal_only / native_runtime_verified / evaluation_only | Production code does not write date, player position, temporary-bed state, Tent health or terrain-feature tables. Interrupted or stale requests fail closed and require a fresh snapshot/replan. |

## Sign Placement Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.sign_placement.sign_catalog` | Enumerate every live vanilla display-item sign and text sign without hardcoded IDs or counts | `Game1.bigCraftableData`; exact `sign_item` context tag; exact `TextSign` identity; detached `ItemRegistry.Create<Object>` probes | n/a; locked decompile plus runtime are authoritative for execution | covered_for_read / all_live_catalog_rows_verified | Publishes raw data, qid, branch kind, expected runtime type, passability and empty payload/text contract for all four live rows. |
| `player.sign_placement.rows[].locations[].static_legal_tile_ranges` | Bind one exact inventory sign to current-map native legality, reachable adjacent stand and route-safe blocking layout | `Utility.playerCanPlaceItemHere`; `Object.canBePlacedHere`; `Object.placementAction` sign branches | n/a | covered_for_read / covered_for_gate / current_location_rebind / native_runtime_verified | Only the current loaded map is scanned. Fingerprints include live sign data, exact inventory state and current sign topology. Cross-map plans must rebind after arrival. |
| `current_location.objects[].sign_state` | Verify the exact native sign subtype and payload while exposing separate placement, display assignment and text actions | live `Sign.displayItem/displayType`; direct display-item serialization; base Object `signText/showNextIndex`; `isPassable` | n/a | covered_for_read / native_post_state_verified | Placement cannot smuggle either payload. Display assignment and text editing remain separate native actions and are both verified. |
| `current_location.objects[].sign_state.display_assignment` | Bind one exact base `Sign`, every non-null source inventory item and any previous payload before native assignment or replacement | direct `SaveSerializer` over the live source and previous display item; locked `Sign.checkForAction` type branch; no read-side `getOne()` | n/a | covered_for_read / covered_for_gate / native_runtime_verified | Publishes source state SHA-256, expected display type, unchanged-source contract, previous payload identity/hash, replacement requirement and one target fingerprint. The actual display-copy runtime type/state is read only after native execution. |
| `current_location.objects[].sign_state.text_editing` | Bind one exact base text sign and its previous raw/display text before native write, replacement or clear | direct target serialization; `Object.signText/SignText/showNextIndex`; locked `CheckForActionOnTextSign`, `TitleTextInputMenu`, `NamingMenu` and `TextBox` contract | n/a | covered_for_read / covered_for_gate / native_runtime_verified | Publishes target fingerprint, previous raw/display state, replacement requirement, 60-code-unit keyboard limit, filter/trim/token/display pipeline and empty-display index rule. Runtime records the actual native-normalized result. |

## Furniture Placement Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.furniture_placement.furniture_catalog` | Enumerate the complete live furniture data surface and canonical runtime factory result without hardcoded item or subtype lists | `Data/Furniture`; `Furniture.GetFurnitureInstance`; `FurnitureData`; locked 1.6.15 furniture subclasses | n/a; locked decompile plus runtime are authoritative for execution | covered_for_read / all_live_catalog_rows_read | Publishes all 645 live rows, raw data fields, qualified identity, canonical runtime subtype, furniture type, dimensions, rotations, passability, placement restriction and source rectangles. Catalog reading never calls source-mutating `Furniture.getOne()`. |
| `player.furniture_placement.rows[].rotations[].static_legal_tile_ranges` | Bind one exact inventory furniture unit and virtual rotation to current-map native legality, rectangular layout safety and its exact native endpoint | `Furniture.canBePlacedHere`; `CanPlaceThisFurnitureHere`; `placementAction`; virtual `rotate`; `InitializeAtTile`; `GameLocation.CanFreePlaceFurniture` | n/a | covered_for_read / covered_for_gate / current_location_rebind / native_runtime_verified | Only the current loaded map is scanned. Each tile uses a fresh detached factory probe initialized at that tile. Rows carry wall-anchor correction, footprint, passability, free-placement/proximity contract, location restriction, `location_furniture` or `table_held_object`, and exact empty-table identity. |
| `current_location.furniture` | Verify native result and expose current furniture topology, storage and table payloads to later planning | live `location.furniture`, furniture virtual geometry/state, `heldObject`, storage items and mutex | n/a | covered_for_read / native_post_state_verified | Exposes runtime subtype, qid, tile, rotation, furniture type/restriction, footprint/bounds, passability, on/flipped state, held object, storage content and mutex. Production placement does not write these fields directly. |

## Flooring Placement Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.flooring_placement.floor_path_catalog` | Enumerate every live floor/path identity and complete placement/removal/movement metadata without a hardcoded item list | `Game1.floorPathData`; `Flooring.GetFloorPathItemLookup`; `FloorPathData` locked 1.6.15 type | n/a; locked decompile plus runtime are authoritative for execution | covered_for_read / runtime_all_catalog_rows_verified | Publishes data key/id, item/qid, connect/shadow type, placement/removal/footstep sounds, debris, corner size, farm speed buff and canonical lookup status. |
| `player.flooring_placement.rows[].locations[].static_legal_tile_ranges` | Bind one exact inventory slot to a current-map native-legal target and same-floor eight-neighbor connection mask | `Object.canBePlacedHere`; `Object.placementAction(IsFloorPathItem)`; `Flooring.gatherNeighbors/OnAdded` | n/a | covered_for_read / covered_for_gate / current_location_rebind | Only the current loaded map is scanned. Any existing TerrainFeature makes the target illegal. Random floors bind the complete `whichView=0..15` domain; other floors bind constructor value zero. |
| `current_location.terrain_features[].flooring_state` | Verify native result identity, data key, view domain, passability and derived connection topology | live base `Flooring` fields plus `GetData`, `isPassable`, `getFootstepSound` | n/a | covered_for_read / native_post_state_verified | Placement never replaces existing terrain features. Removal remains a separate Axe/Pickaxe/damage action. |

## Fence Placement Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.fence_placement` | Bind one enclosure/layout intent to an exact inventory fence, loaded persistent location, native-legal tile, neighbor topology and route-safe stand | `Object.IsFenceItem`, `Object.canBePlacedHere`, `Object.placementAction`; `Fence.GetFenceLookup`, constructor, `ResetHealth`, `getDrawSum`, `countsForDrawing`, `toggleGate`, `isPassable` | n/a; locked decompile plus runtime are authoritative for execution | covered_for_read / covered_for_gate / native_runtime_verified / evaluation_only | Reads every live `Data/Fences` row rather than a hardcoded identity list. Legal ranges are split whenever expected draw sum or gate functionality changes. Compiler additionally virtual-blocks the exact closed placement and preserves the whole reachable domain, map endpoints and existing storage access. |
| `current_location.objects[].fence_state` | Verify the placed object's exact post-state and expose later fence service decisions | live base `Fence` net fields plus `GetData`, `getDrawSum`, `isPassable` | n/a | covered_for_read / native_post_state_verified | Exposes exact runtime support status, gate flag/position, draw sum, health/max health, repair queue, passability and data key. Gate conversion/repair, removal and toggle/open are intentionally not folded into placement. |

## Crab Pot Placement Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.crab_pot_placement` | Bind one capacity/layout intent to an exact inventory pot, persistent loaded location, legal water tile and tile-specific production context | `Object.canBePlacedHere` and `placementAction` `(O)710` branches; `CrabPot.IsValidCrabPotLocationTile`, `placementAction`, `NeedsBait`, `DayUpdate`; `GameLocation.GetCrabPotFishForTile`; `Data/Fish` trap rows | n/a; locked decompile and runtime are authoritative for execution | covered_for_read / covered_for_gate / native_runtime_verified / evaluation_only | Publishes compressed legal water ranges grouped by production signature, owner and Mariner/Luremaster state, fish area/habitats, base/effective junk chance, native-order eligible catches and fallback trash domain. Compiler freezes exact slot/stack/location/target/stand/fingerprint/signature/reason; runtime rechecks the loaded tile and initial state. |
| `current_location.objects[].crab_pot_*` / `executor.load_crab_pot_bait` / `fishing.collect_crab_pots` | Continue from placement through native bait, production and collection without a second lifecycle implementation | `CrabPot.NeedsBait`, `performObjectDropInAction`, `GameLocation.checkAction`, EVD-258 bait runtime and EVD-209 collection chain | n/a | covered_for_read / covered_for_gate / native_placement_bait_and_collection_verified / evaluation_only | EVD-257 verifies placement and initial state. EVD-258 publishes and binds every live native-probe-accepted `Category=-21` inventory bait row, then verifies native bait/owner/inventory deltas for five vanilla bait identities. EVD-209 remains authoritative for ready output, Book of Crabbing doubling, inventory receipt, Fishing XP, collection and ready/bait reset. |

## Cookout Kit Placement Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.cookout_kit_placement` | Bind a same-day cooking plan to one exact inventory kit, persistent loaded location, legal layout tile and native result | `Object.placementAction` `(O)926` branch; `Utility.playerCanPlaceItemHere`; `Utility.tryToPlaceItem`; `Torch.checkForAction` | n/a; locked decompile and runtime are authoritative for execution | covered_for_read / covered_for_gate / native_runtime_verified / evaluation_only | Source is `(O)926`; placed object is `StardewValley.Torch` / `(BC)278`, `Fragility=1`, `destroyOvernight=true`. The projection publishes compressed legal ranges and a topology fingerprint; the compiler requires exact slot/stack/location/target/stand/reason and the runtime rechecks the loaded tile. |
| `player.cooking.sources[source_kind=cookout_kit]` | Hand a newly placed kit to the existing cooking chain instead of inventing a second cooker | native `Torch.checkForAction` opens `CraftingPage(cooking:true)`; EVD-253 cooking source projection | n/a | covered_for_read / native_handoff_verified | EVD-256 verifies the next full snapshot exposes `cookout:Farm:62,15`; recipe materials, output and `recipesCooked` remain owned by EVD-253. |

## Mining Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `mining.tiles.collision_context.blocked_rows` | Exact loaded-floor BFS and dynamic obstacle input | `GameLocation.isTilePassable` / `IsTileBlockedBy` | n/a | covered_for_read / runtime_verified | Compact row-major `1=blocked,0=passable`; excludes the current farmer and includes other live occupants. Level 99 runtime verified a 50x50 grid. |
| `mining.objects[].health_or_hits_remaining` | Perfect executor mining cost and target ordering | `Object.MinutesUntilReady`, `Pickaxe.DoFunction`, `BreakableContainer.health` | n/a | covered_for_read / runtime_verified | Level 99 runtime verified 79 breakable stone durability rows and 17 reflected containers. |
| `mining.objects[].mining_experience_*` / `luck_experience_*` | Rank a stone action by permanent skill progress without confusing Mining XP with conditional Luck XP | `GameLocation.breakStone`; `MineShaft.checkStoneForItems`; `Farmer.gainExperience` | pending secondary comparison | covered_for_read / covered_for_candidate_bounds / covered_for_compile / needs_runtime_regen | Direct resource nodes expose exact or strict bounded Mining XP, including random gem-node, Geologist, and conditional coal branches. Generic MineShaft stones expose separate `0..5` Mining and area-derived Luck bounds. Plans carry Mining as the primary and Luck as the secondary skill result; no stochastic branch is converted to an expected value. |
| `mining.resource_clumps[]` / `executor.break_resource_clump` | Distinguish large clumps from ordinary obstacles, expose exact tool/upgrade/hit/output gates, and remove a supported clump through native tool input | `ResourceClump.performToolAction/destroy`, `Pickaxe.DoFunction`, `GameLocation.performToolAction`, live clump/tool/debris fields | n/a | covered_for_read / covered_for_candidate / covered_for_compile_rebind / native_executor_runtime_verified | Exact base `ResourceClump` indexes 148/622/672/752/754/756/758 expose footprint, health, Pickaxe gate, selected slot, base/additional/effective power, damage, hit count, deterministic core-output item-state multiset, and bounded optional secret-note status. The planner binds an outside perimeter stand plus occupied hit tile; the compiler rebinds every live identity/tool/health/output field. Runtime uses cross-frame native input, verifies natural removal and exact debris/inventory output deltas, and records optional secret notes separately. Clean allowlisted smoke `runtime-mining-resource-clump-smoke-20260729-191339` passed all seven branches with projected hits exactly matching observed native swings. |
| `mining.objects[].is_container` / `executor.break_container` | Treat mine crates/barrels as a separate heavy-hitter action and release contents for normal pickup | `BreakableContainer.performToolAction`, private health field, `Tool.isHeavyHitter()` | n/a | covered_for_read / covered_for_candidate / covered_for_compile / runtime_verified | Planner selects nearby containers separately from stones. Runtime uses native left-click input for melee heavy hitters and native tool input for non-melee tools, counts only completed native attack animations, verifies object removal, restores the requested slot, and leaves generated debris to the normal pickup chain. Hidden isolated PASS: `runtime-container-lifecycle-20260717102239`, `(W)63`, health `3 -> 0`, one completed swing; full 119-to-120 PASS: `runtime-skull-key-20260717102345`. |
| Mining native movement input | Preserve normal walk animation and collision while moving to stones, containers, monsters, debris, ladders, shafts, exits, farm-tool targets, and beds | Game update consumes SMAPI `W/D/S/A` overrides on the following tick | n/a | runtime_verified / regression_guarded | No executor writes `Farmer.Position` or calls `MovePosition`. Stuck detection observes position across update ticks; combat movement budget also counts cross-tick tile changes. Hidden isolated stone/container/combat smokes passed after conversion. |
| `combat_intent` / shared dungeon combat policy | Keep safety/route combat distinct from farming, task, and progression kills without duplicating dungeon executors | `TrainingCombatIntentRules`, MineShaft/Volcano planners and compilers, common runtime combat-intent adapter, structured execution result and feature row | n/a | covered_for_compile / native_runtime_disengagement_verified / recorded_for_training | `target_defeat` preserves the original terminal requirement. `transit_self_defense` stops beyond four tiles. `transit_route_clearance` stops only after leaving both the safety window and original blocking area. MineShaft and Volcano retain separate pathing and input state machines but share these thresholds and budget rules. Hidden isolated runs `runtime-mining-transit-combat-20260730-r2` and `runtime-mining-combat-intent-regression-20260730` cover zero-attack disengagement plus 50/50 rolling descent actions. |
| `route_objective_id`, `route_target_*`, `blocked_route_cell_*`, `blocker_attribution_status`, `expected_connectivity_gain` | Prevent unrelated reachable entities from being mislabeled as route clearance | mining/Volcano static and dynamic collision comparison; exact runtime identity/object origin; compiler parameters | n/a | covered_for_compile / regression_tested | Quarry and Volcano fail closed on zero or multiple blocker matches. Generic move repair separately requires every clear to reduce the 0-1 BFS minimum clear count to the target. |
| Opportunistic `mining.debris[]` pickup | Collect nearby drops before another fight or while advancing a reach-depth objective | live `MineShaft.debris` chunks and natural game collection | n/a | covered_for_read / covered_for_candidate / covered_for_compile / runtime_executor_existing | Planner prioritizes debris dropped by the current target and otherwise picks reachable debris within three tiles when no immediate threat exists. The pickup executor moves with native input and lets the game collect chunks naturally. |
| `mining.objects[].ladder_preview` | Prefer exact ladder-producing stone without guessing danger | `MineShaft.checkStoneForItems`, `Utility.CreateDaySaveRandom` | n/a | covered_for_read / runtime_verified | Runtime verified all 79 breakable stones carried a preview; the adapter never advances game RNG or calls ladder creation. |
| `mining.monsters[]` | Current combat/path replanning facts | live `Monster` type, bounds, health, damage | n/a | covered_for_read / runtime_verified | Level 99 runtime serialized seven monsters. Future AI path is not guessed; every dynamic change requires a new snapshot. |
| `mining.monsters[].slingshot_attack_projections[]` | Compare ranged combat without asking the small model for aim or ammo mechanics | loaded `Slingshot`, attached ammo, decompiled projectile damage, vanilla receiver state | n/a | covered_for_read / covered_for_candidate / pending_runtime_validation | Carries exact discrete direct-projectile damage only when vanilla receiver/global-enchantment composition is complete. Planner requires enough ammo, a clear current projectile line, and at least four tiles of separation. Explosive-ammo area side effects and projectile interception remain outside duration ranking. |
| `mining.monsters[].bomb_damage_semantics` | Prevent area-clear planning from assuming every monster receives or can be finished by a bomb | live vanilla runtime type and receiver state | n/a | covered_for_read / covered_for_candidate / covered_for_compile / native_executor_implemented / runtime_pending | Armored Bugs explicitly reject bomb damage; Spikers and age-789 Bats remain immune; closed ordinary Rock Crabs lose their shell; reviving Mummies compile to a targeted bomb finisher, while standing Mummies first compile native melee to `knockdown_requires_bomb_finish` and are rebound from the next fresh snapshot before bomb placement. Temporary Grub/Lava Lurk gates and custom types fail closed. |
| `mining.player_resources.slingshot_slots[]` | Expose ranged inventory and native charge inputs | live slingshot item ID, attachment, stack, buffs | n/a | covered_for_read / covered_for_compile / pending_runtime_validation | Includes 1x/2x/4x multiplier, ammo base damage, 300 ms full charge, projectile-speed bounds, and explosive-ammo radius. |
| `mining.player_resources.bomb_slots[]` | Expose typed consumable area-clear resources | live bomb object slot plus decompiled radius/fuse/damage rules | n/a | covered_for_read / covered_for_candidate / covered_for_compile / pending_runtime_validation | Cherry/Bomb/Mega use radii 3/5/7 and a 2400 ms fuse. Planner rejects protected objects, requires a dense breakable cluster, reproduces the exact `getCircleOutlineGrid` fill, and requires a BFS escape outside the full square player-damage hitbox. |
| `mining.floor_objectives` | Kill-all, ladder, treasure and descent gates | `mustKillAllMonstersToAdvance`, `shouldCreateLadderOnThisLevel`, private `netIsTreasureRoom` | n/a | covered_for_read / runtime_verified | Stone ladder formula is derived exactly; monster-drop ladder remains an after-state observation. |
| `mining.reward_chests[]` | Enumerate every live claimable MineShaft reward container without regenerating treasure RNG | `MineShaft.addLevelChests`, `overlayObjects`, `Chest.dumpContents`, `Farmer.GetItemReceiveBehavior`, `Farmer.gainExperience` | local decompile plus isolated runtime matrix | covered_for_read / covered_for_candidate / covered_for_compile / native_executor_runtime_verified | Covers ordinary fixed rewards at 10/20/40/50/60/70/80/90/100/110 and live Skull Cavern treasure/forced multi-chests. Runtime matrix `runtime-mine-reward-chest-smoke-20260730-004220` verified a remixed floor-20 weapon, floor-100 Stardrop (`+34` max stamina), and both native floor-320 random chests. Tool receipt fingerprints preserve the live `swingTicker` instead of hashing constructor-randomized clones. Floor-120 Skull Key remains a specialized chain; Quarry Mine and Volcano containers are excluded. `dumpContents` calls skill 5 with `25 + mineLevel`, but `Farmer.gainExperience` immediately ignores skill 5, so actual Luck XP delta is exactly zero. |
| `/v1/snapshot?profile=mining` | Prevent unrelated domain scans during mining planning/training | `ModEntry.AllowedDomainsForProfile`, 30-tick profile cache | n/a | covered_for_read / runtime_verified | Includes only baseline domains plus mining; generic `current_location/locations` are excluded because mining already supplies the loaded floor's player tile, action tiles, collision, objects, and occupants. |
| `mining.reach_depth` runtime | Keep the model output high-level while the compiler executes one exact floor step per fresh snapshot | `MiningFloorStepPlanner`, `MiningFloorStepCompiler`, native runtime executors | n/a | covered_for_rolling_execution / pending_full_objective_calibration | Supports mining, combat, pickup, food recovery, ladder/shaft descent, and mandatory native exit at time/energy/unrecoverable-health boundaries. Loaded vanilla MineShaft stones expose guaranteed, guaranteed-one-of, conditional, and union possible item identities from current decompiled rules; resource targets infer live source objects and prefer guaranteed nodes. Monster rows separate raw selected drops from effective death outputs, cover every vanilla runtime-type extra-drop override, and preserve special-item and pendant replacement semantics. Shared drop catalogs expose exact conditional probabilities and stack means. Complete direct-melee projections expose per-weapon discrete damage, expected attack count, animation duration, cardinal movement duration, and a selected execution slot; stable drops can therefore rank by projected milliseconds. Death-tile/current-position loot, projectile/independent damage, temporary immunity waits, and custom damage semantics fail closed. Still pending: custom-mod source introspection, calibrated whole-objective duration, and isolated multi-floor validation. |
| `player.deepest_mine_level` / `player.current_mine_level` | Bound ordinary-mine elevator offers and reject locked/current checkpoints before planning | live `MineShaft.lowestLevelReached` and `Game1.CurrentMineLevel` | n/a | covered_for_read / covered_for_candidate / runtime_verified | Distinct from Skull Cavern depth and from the transient `ridingMineElevator` flag. |
| `current_location.mine_elevator_action_tiles` | Discover the actual native elevator endpoint on the entrance map or a loaded elevator floor | map `Action=MineElevator`; `MineShaft.checkAction` on `Buildings/mine` tile index `112` | n/a | covered_for_read / covered_for_candidate / native_interaction_runtime_verified | Entrance and floor dispatch differ internally, but both compile through the existing exact map interaction primitive. No endpoint means reach-depth falls back to its current-floor planner instead of blocking the objective. |
| `menus.menu_specific_state` kind `mine_elevator` / `mining.use_elevator` | Select only an offered, unlocked checkpoint and bind the click to the current menu identity | `MineElevatorMenu` entry construction and `receiveLeftClick` | n/a | covered_for_read / covered_for_candidate / covered_for_compile / native_runtime_verified / five_gate | Publishes all visible/selectable entries and a stable SHA-256 identity. Native selections `25 -> 0` and `0 -> 25` passed in the isolated runtime. `mining.reach_depth` reuses the same candidate chain while preserving its final target as continuation state. |
| `debug.setup_quarry_mine` / `Invoke-RuntimeQuarryGoldenScytheLoop.ps1` | Create a repeatable isolated Golden Scythe claim-and-exit boundary without confusing mine families | `Game1.enterMine`, `MineShaft.quarryMineShaft/isSideBranch`, exact `GoldenScythe` action, `ExitMine_Leave` Quarry destination | n/a | native_runtime_verified_daily_plan_full_loop | Debug-only reset removes `gotGoldenScythe` and `(W)53` only from the isolated fixture. Production replanning uses only `profile=mining`, forbids ladder/shaft/Volcano steps, permits only necessary route clearance after claim, and then requires native exit to `Mine(67,10)`. Run `runtime-quarry-golden-scythe-20260809-203952` completed 68 DailyPlan decisions: 56 verified native actions/rows and 12 fresh recoverable replans; exact claim and native exit were verified. |
| `mining.monster_drop_catalogs[]` | Avoid repeating large shared possible-drop sets and their probability tables in every live monster row | `Utility.getRandomCosmeticItem`, `Utility.getRandomSingleTileFurniture`, `Trinket.GetRandomTrinket`, `MineShaft.getTreasureRoomItem`, current mail/mastery/trinket gates | n/a | covered_for_read / covered_for_candidate / pending_runtime_validation | Catalogs are stable-sorted and carry `active`, identity completeness, selection-probability completeness, and optional per-identity conditional selection entries. Random cosmetics reproduce the exact 20% furniture / 20% hat / 60% shirt tree, aggregate duplicate furniture paths, and redirect invalid/error furniture weight to `(F)1369` using loaded item metadata. Natural trinkets are uniform over loaded `DropsNaturally` entries. The planner accepts weights only when entries cover the identity set and sum to one; inactive, missing, incomplete, or malformed probabilities fail closed. |
| `mining.monsters[].drop_probability_rules[]` | Keep exact event probability separate from item/catalog selection probability | `MineShaft.monsterDrop`, `GameLocation.monsterDrop`, all vanilla `getExtraDropItems` overrides, `Utility.trySpawnRareObject`, `Utility.tryRollMysteryBox`, `Trinket.TrySpawnTrinket` | n/a | covered_for_vanilla_current_snapshot / covered_for_candidate_weighting / pending_runtime_validation | Current-state formulas cover the primary MineShaft branch, all eleven vanilla runtime-type overrides, Burglar's Ring data rolls, secret notes, mystery boxes, Book of Mysteries/Void, hard-mode ore/soul else-if ordering, rare objects, and natural trinkets. `event_chance` is per `getExtraDropItems` call; `calls_per_base_branch` is two with Burglar's Ring; `effective_per_kill_chance` is the at-least-one probability including the 0.999 base branch; `expected_quantity_per_kill` includes geometric/stack quantities and the Book of the Void `1.03` duplication factor only for pre-duplication loot. Targeted planning ranks guaranteed sources first, then stable exact `per_identity_chance/(BFS distance+1)`, then unknown probability. Current-position/current-death-tile seeded previews never enter stable probability ranking. `per_identity_chance` stays null where a shared catalog's weighted runtime fallback has not been collapsed exactly. |

## Volcano Dungeon Transparency

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `volcano.current_level` | Keep Volcano level semantics separate from MineShaft depth | `VolcanoDungeon.level/layoutIndex/generationSeed/startPosition/endPosition`, exact mail gates | pending secondary comparison | covered_for_read / covered_for_gate | Levels are `0..9`; level 5 is rest/shop, level 9 exits to Caldera. |
| `volcano.tiles` | Exact loaded collision and cooling input | `waterTiles`, `cooledLavaTiles`, `dirtTiles`, read-only `IsTileBlockedBy`, `performToolAction` level-5 exclusion | pending secondary comparison | covered_for_read / covered_for_gate | Raw water/lava tiles are not mislabeled as all lava; only `coolable_uncooled_tiles` enter cooling planning. |
| `volcano.connectors` / `volcano.gates` | Select native forward warp or unpressed DwarfSwitch | live `warps`; `DwarfGate.switches/opened/pressedSwitches/tilePosition` | pending secondary comparison | covered_for_read / covered_for_candidate / covered_for_compile | Reachable switches compile to movement onto the touch tile; reachable forward warps compile with exact target and arrival. |
| `/v1/snapshot?profile=volcano` | Purpose-limit Volcano planning while retaining generic movement/connector evidence | `ModEntry.AllowedDomainsForProfile` | n/a | covered_for_read / runtime_verified | Includes baseline, `volcano`, `current_location`, and `locations`; repeated hidden rolling runs retained fresh snapshots and changed state hashes. |
| `volcano.objects` / `volcano.player_resources` | Select and budget one exact native obstacle action | live stone `MinutesUntilReady`; private `BreakableContainer.health`; pickaxe upgrade damage; `Tool.isHeavyHitter`; club double-hit rule | pending secondary comparison | covered_for_read / covered_for_candidate / covered_for_compile | The planner emits an exact target, stand tile, tool slot, and deterministic maximum tool-use budget. |
| `volcano.monsters` | Bind combat to one live target without assuming all monster semantics are equal | `Monster` health/resilience/miss/invincibility fields; `Spiker.takeDamage` returns `-1`; runtime type assembly gate | pending secondary comparison | covered_for_read / covered_for_candidate / covered_for_compile | Supported vanilla targets carry exact identity/type/name. Spiker and custom runtime types fail closed. |
| `volcano.reach_caldera` | Keep model output high-level and compiler-owned | `VolcanoFloorStepPlanner`, `VolcanoFloorStepCompiler`, `VolcanoReachCalderaCandidateBuilder` | n/a | rolling_executor_implemented / full_loop_runtime_verified | Switch-touch, forward connector, cooling, stone, container, and supported melee steps compile independently. One full generated `0..9 -> Caldera` run passed; duration calibration remains pending. |
| `executor.cool_volcano_lava` | Create a native passable lava bridge without direct state mutation | exact live target/stand/watering-can slot; `Farmer.BeginUsingTool/EndUsingTool`; `cooledLavaTiles` after-state | pending secondary comparison | covered_for_compile / native_executor_implemented / needs_runtime_regen | Fails closed on level 5, empty/missing can, target drift, path drift, nearby monster, or absent cooled after-state. |
| `executor.break_volcano_stone` / `executor.break_volcano_container` | Remove one exact current-floor obstacle through normal player input | native tool lifecycle or `C` input; object identity/type recheck; object absence and tool-use count | pending secondary comparison | covered_for_compile / native_executor_implemented / needs_runtime_regen | Uses Volcano-only active state and never calls direct object mutation. |
| `executor.combat_volcano_monster` / `combat_intent` | Defeat an explicit target or bound nonessential transit combat through normal player input | native WASD pursuit and `C` melee input; runtime identity lock; health sequence, player damage, initial target tile, and typed intent recording | pending secondary comparison | covered_for_compile / native_executor_implemented / runtime_disengagement_verified | `target_defeat` requires observed death. `transit_route_clearance` and `transit_self_defense` have bounded movement/disengagement rules and return blocked/replan rather than false success. Uses Volcano-only active state and never calls direct monster damage or removal. |
| `tool_use_count` | Preserve exact native obstacle action count in executor-calibration output | `TrainingExecutionResult`, RuntimeTestHarness, LiveTrainingLoop feature row | n/a | covered_for_recording / runtime_pending | Prevents the final native hit from disappearing when the game removes the object before the next tick. |
| `debug.setup_volcano_floor` / `Invoke-RuntimeVolcanoReachCalderaLoop.ps1` | Create a repeatable isolated full-loop calibration boundary | `VolcanoDungeon.GetLevelName/GetLevel`, `Game1.warpFarmer`, generated start/end positions, purpose-limited snapshots | n/a | static_complete / runtime_pending | Debug-only setup may install typed pickaxe/watering-can calibration tools plus runtime-selected weapon/food in the isolated save. The production executor cannot select this option or call direct warp. |
| `--skip-training` | Separate runtime evidence collection from model fitting | LiveTrainingLoop option gate around baseline train/rank calls | n/a | covered_for_calibration_control | The Volcano loop writes verified rows step-by-step; fitting remains a separate stage after an accepted complete run. Existing callers retain the previous default behavior. |

## Runtime / Training Recording

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `state_hash` | Detect stale before/after training samples | LiveTrainingLoop state hash handling | n/a | covered_for_gate | Prevents stale snapshot training. |
| `before_game_tick` / `after_game_tick` | Confirm time/tick progression across execution | Bridge snapshot tick fields | n/a | covered_for_gate | Added to execution artifacts. |
| `state_hash_changed` | Output recording signal for applied/blocked episodes | LiveTrainingLoop after-snapshot poll | n/a | covered_for_gate | Runtime smoke passed. |
| `after_snapshot_fresh` | Freshness gate for post-action state | LiveTrainingLoop poll and artifact write order | n/a | covered_for_gate | Runtime smoke passed. |
| `primitive_kind` | Identify runtime primitive whose result is being verified | RuntimeTestHarness result contract | n/a | covered_for_gate | Hidden/silent runtime regeneration completed under `artifacts\hidden-training-primitive-verification-*`. |
| `primitive_verification_status` / `primitive_verification_reasons` | Gate executor-calibration rows on observed runtime effect | RuntimeTestHarness result contract plus LiveTrainingLoop stale-after-snapshot downgrade | n/a | covered_for_gate | Values include `verified`, `observed_mismatch`, `blocked`, `not_applicable_no_op`, and `stale_after_snapshot`. Runtime regeneration completed. |
| `requested_effect` / `observed_effect` | Audit requested vs observed primitive result | RuntimeTestHarness result contract | n/a | covered_for_audit | String audit field, not a gameplay rule. Runtime regeneration completed. |
| `block_reason` / `BlockingReasons` | Explain why candidate/compiler/executor blocked | Core compiler/verifier | n/a | covered_for_gate | Required for training filters. |

## Candidate Availability / Model Scoring

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `option_availability.v1` | Pre-model candidate availability map | `OptionRegistry.RequiredStateFactors`, `Verifier.Verify` | n/a | covered_for_gate | Hard-rule field availability is mapped before rank/scoring. |
| `OptionAvailability.available` | Decide whether small model may score/select option | Candidate availability evaluator | n/a | covered_for_gate | False for missing hard-rule fields, preview-only/executor-disabled options, or candidate-local hard gates such as closed shop, absent service owner, blocked route gate, unsupported branch, active menu, or unreachable endpoint. |
| `OptionAvailability.missing_state_factors` | Explain unavailable hard-rule fields before model scoring | Candidate availability evaluator | n/a | covered_for_gate | Preserves exact field paths from `OptionSpec`. |
| `OptionAvailability.preview_only` | Keep preview options out of runtime action scoring | Candidate availability evaluator | n/a | covered_for_gate | Buying/selling/route/interact are explicit preview/blocked unless executor safety exists. |
| `OptionAvailability.executor_enabled` | Distinguish candidate preview from executable primitive | Candidate availability evaluator | n/a | covered_for_gate | Runtime-enabled first pass: crop maintenance, move, face, wait. |
| `OptionAvailability.candidates[].parameters` | Carry bound target/action parameters into pre-model availability | Candidate availability request | n/a | covered_for_gate | Reuses `SmallModelActionParameter[]` so compiler gates and candidate gates share parameter names. |
| `OptionAvailability.current_time` | Anchor candidate time-window scheduling to the transparent snapshot time | `time.time` via `CandidateOptionAvailabilityEvaluator` | n/a | covered_for_gate | Required so rank-time scheduling does not infer a default day time. |
| compiler dry-run availability probe | Reuse route/interact hard gates before model scoring | `ActionQueueCompiler.Compile` synthetic non-executing envelope | n/a | covered_for_gate / blocked_for_execution | Only item-level block reasons are merged; `queue_global_compiler_block` is filtered defensively. |
| availability-aware `rank-options` | Filter unavailable candidates before event scorer | Backend rank endpoint | n/a | covered_for_gate | With `state_hash`, ranker scores only `available=true` by default and returns availability audit. |
| `economy.buy_supplies` rolling purchase | Prune when no safe preview or currently legal shop endpoint exists; otherwise bind one exact shop/item/quantity/max-price objective through route, interaction, optional whitelisted dialogue, native purchase and cleanup | `locations.shop_stock_preview`, `locations.route_graph.shop_endpoint`, current gate/owner facts, `menus.shop_stock.entries` | n/a | five_gate_training_admitted_evd_219 / bounded_safe_money_purchase / not_autonomous | Every stage requires a fresh snapshot and the same continuation identity. Known closed shops are excluded before ranking. Completion requires exact `executor.buy_shop_item` applied/verified; broader purchase families remain blocked. |
| `economy.sell_items` rolling shop sale | Prune when there is no explicitly authorized unprotected positive-value inventory stack accepted by a currently legal shop; otherwise bind one exact shop/item/slot/quantity/unit-price objective through route, interaction, optional whitelisted dialogue, native sale and cleanup | `player.inventory`, `locations.shops[].salable_item_tags`, `locations.shops[].sale_preview`, route/shop endpoint facts, `menus.sell_context` | n/a | five_gate_training_admitted_evd_220 / bounded_single_shop_sale / not_autonomous | Every stage requires a fresh snapshot and the same continuation identity. Known closed or owner-unavailable shops are excluded before ranking. Completion requires exact `executor.sell_shop_item` applied/verified. Strategic surplus selection remains policy-owned. |
| `economy.ship_items` rolling shipping-bin sale | Bind one explicitly authorized unprotected positive-payout inventory item to the exact completed Farm bin; emit one resolved connector, one exact stand approach, or one native one-item deposit per fresh snapshot | `player.inventory.sell_to_store_price`, `farm.shipping_bins`, `locations.route_graph`, `locations.route_connectors`, `world_progress.shipping_collection` | n/a | five_gate_training_admitted_evd_221 / bounded_single_shipping_item / not_autonomous | Continuation freezes item/slot/quantity/payout and bin/stand identity. Immediate inventory/bin receipt and delayed `basicShipped` settlement are separate checks. Day-end value uses native `sellToStorePrice(-1L)`, not `salePrice()`. Mini-Shipping Bin, batching and surplus inference remain excluded. |
| `farm.animals[]` tool-harvest fields / `farm.collect_animal_products` | Select one exact ready base animal and delegate all movement/tool mechanics to the existing native executor | `GameLocation.animals`, base `FarmAnimal`, live `FarmAnimalData`, exact Milk Pail/Shears inventory slot, current inventory stacks, `Farmer.addItemToInventory`, `FarmAnimal.HandleStatsOnProduceCollected` | n/a | five_gate_training_admitted_evd_222 / bounded_loaded_base_animal_tool_harvest / autonomous_mechanical | Exposes animal/tool identity, stand, output runtime type/ID/quality/quantity/unit-state, cracker multiplier, `harvest_native_stat_increment_amount`, per-stat before/after, Farming XP, energy and capped friendship. The stat amount is the temporary item's remaining stack after native inventory merging, not necessarily output quantity. Non-tool harvests, Auto-Grabber, remote/unloaded animals and custom runtime types are excluded upstream. |
| `farm.pets[]`, `farm.pet_bowls[]` / `farm.care_for_pets` | Select one exact daily interaction or assigned unwatered bowl; retain maximum-friendship gift opportunities and keep bowl friendship/mail as delayed settlement | base `Pet.checkAction`, `Pet.GetBoundingBox`, `PetData.GiftChance/Gifts`, base `PetBowl.performToolAction`, base `WateringCan`, `Pet.dayUpdate`, live mail sets | n/a | five_gate_training_admitted_evd_223 / loaded_exact_base_pet_and_bowl / autonomous_mechanical | Exposes exact GUID/runtime type/location/tile, daily grant/last-day/times-pet state, capped friendship, deterministic gift trigger plus runtime-observed global-RNG selection, bowl assignment/action tile/water state, exact tool water/energy projection, and next-day friendship/mail projection. Runtime rebinds moving pets by GUID and bounding-box action reach. Bowl output creates a durable pending receipt and becomes final only after native sleep advances exactly one day. Custom/legacy pet classes, custom bowls/cans and drift remain excluded upstream. |
| `economic_candidates[]` | Concrete item-level buy/shop-sell/shipping scoring candidates | `menus.shop_stock.entries`, `player.inventory`, `menus.sell_context`, `farm.shipping_bins` | n/a | covered_for_read / covered_for_gate / bounded_native_execution | EVD-219/220/221 admit one exact strategy-selected purchase, shop sale, or shipping item through separate rolling continuations. They do not authorize autonomous economic inventory policy. |
| `event_candidates[]` | Concrete non-economic scoring candidates | `current_location.crops[].needs_watering` from `HoeDirt.needsWatering()` | Crops page | covered_for_read / covered_for_gate / runtime_verified_targeted_watering | `water_crop_tile` carries candidate id, location/tile, expected effect, ticks, energy cost, and block reasons; the daily plan compiles one accepted candidate into `executor.water_crop`. |
| `current_location.terrain_features[].tree_*` / `clear_obstacle_tile` | Clear a live vanilla wild tree with an exact current-state tool budget and Foraging XP result | `Tree.performToolAction`, `performTreeFall`, `CreateMossItem`, live Tree net fields, current best Axe | pending secondary comparison | covered_for_read / covered_for_candidate / covered_for_compile_gate / covered_for_observed_delta / needs_runtime_regen | Exposes growth stage, health, stump/tapped/moss state, deterministic next moss stack, best Axe upgrade, and exact hits to clear the current tree plus resulting stump. Mature trunk+stump yields 16 Foraging XP, an existing stump yields 2, and current moss adds its exact stack; young trees yield zero. Tapped/custom trees and missing Axe fail upstream. Compiler rebinds status and hit budget from the current snapshot. |
| `current_location.terrain_features[].stop_growing_moss` / `executor.apply_tree_treatment` | Apply one purpose-bound Vinegar to one exact retained vanilla tree | `Object.canBePlacedHere` and `Object.placementAction` `(O)419`; live Tree moss net fields; exact player inventory slot | n/a | covered_for_read / covered_for_compile_gate / native_runtime_verified_evd_255 / evaluation_only / no_autonomous_candidate | Exposes current moss, permanent moss suppression, exact native permission and required item identity. The compiler independently rebinds the exact Tree row and Vinegar slot/stack, requires a treatment reason, then reuses shared BFS adjacency. Runtime calls native placement and verifies moss cleared, permanent suppression enabled and stack reduced by one. Custom types, stale state, unsafe menus and already-treated trees fail closed. |
| `current_location.objects[].is_spawned_object` / `foraging.collect_spawned_objects` / `executor.collect_spawned_object` | Distinguish stationary spawned pickups from moving Debris and preserve native output/skill semantics | `GameLocation.checkAction`, `GetHarvestSpawnedObjectQuality`, `OnHarvestedForage`, tile-seeded day/save RNG | n/a | covered_for_read / covered_for_candidate / covered_for_compile_gate / native_executor_runtime_verified / training_admitted_evd_211 | Rows expose forage/quest/special identity, exact quality, Gatherer duplicate, total quantity, and separate Foraging/Farming XP. Hidden isolated EVD-211 verified ordinary, Botanist, deterministic Gatherer duplicate, special `724519`, and AnimalHouse branches through native `checkAction`, with exact inventory quantity/quality and both skill deltas. The request transport now carries those projected outputs instead of letting runtime silently recalculate them. Custom subclasses, error identities and the Lewis basement `(O)789` Bat/audio side-effect branch fail closed. |
| `current_location.objects[].clear_kind` / `clear_output_items_json` / `executor.clear_obstacle` | Clear exact single-tile skill objects without losing tool, XP, output, terrain, or persistent-state semantics | `Object.performToolAction`, `GameLocation.digUpArtifactSpot`, native location overrides, `ItemQueryResolver`, `trySpawnRareObject`, `makeHoeDirt` | n/a | covered_for_read / covered_for_candidate / covered_for_compile_rebind / strict_runtime_verifier / native_runtime_verified_twig_seed_spot_ordinary_artifact / rare_variant_runtime_pending / conditional_global_rng_block | All three object families carry a normalized expected-output multiset containing runtime type, qualified ID, quality, quantity, and complete unit-item save-state hash. Hidden runtime runs verify Twig `(O)294 -> (O)388 x1, +1 XP`, SeedSpot seasonal seeds plus `ArtifactSpotsDug +1`, HoeDirt and mail state, and ordinary `(O)590` output multiset plus `+15 XP`; a Grass regression still requires the tile to become fully clear. Artifact/Seed spots succeed with expected HoeDirt only when the original spot object is absent and all projected effects match. Generous, defense-book-hit and Island/Desert override variants remain runtime-pending. Custom location overrides, unreviewed queries, resolver failures, and eligible secret-note global-RNG outcomes fail closed. |
| `current_location.resource_clumps[]` / `foraging.clear_green_rain_bushes` / `executor.break_current_location_resource_clump` | Clear loaded vanilla Green Rain bushes without guessing their multi-tile tool, output, XP, or random-note semantics | `GameLocation.performGreenRainUpdate/performToolAction/tryToCreateUnseenSecretNote`; `ResourceClump.performToolAction/destroy`; live clump/player/event state | Wiki refresh pending; local decompile is normative | covered_for_read / covered_for_candidate / covered_for_compile_gate / native_executor_runtime_verified / task_attached_runtime_verified / training_admitted_evd_212 | Only exact base `ResourceClump` rows with indexes 44/46 and 2x2 geometry can become ready. Rows expose the best Axe, exact damage/hit count, fixed Foraging XP `15`, and exact Moss/Fiber/Mossy Seed multiset from the day/save/anchor local RNG. Secret-note identity, unseen/total counts, outer/inner/combined probability, festival block, and global-RNG boundary remain separate; no snapshot call advances RNG. EVD-212 verified both parent indexes through native Axe input and exact completion-frame core-output/XP checks. Existing ordinary and special-order source runs used native task callbacks and both advanced `0/1 -> 1/1`. Later Debris presence is not required because native near-player auto-pickup may consume it after the executor has already verified the exact multiset. |
| `event_candidates[].route_connector_tile` | Concrete current-map connector movement candidates | `locations.route_connectors.connectors[]`, `locations.collision_grid`, `locations.route_action_branch_coverage`, optional `locations.route_gate_context`, `player.tile_x/y`, compiler dry-run route gates | Location/shop pages pending | covered_for_read / covered_for_gate / covered_for_candidate_to_daily_plan / controlled_for_movement / controlled_for_cross_map_execution | Candidate parameters preserve `execution_option_id`, connector kind/source tile, expected target location, optional arrival tile, maximum movement tiles, and current-step time estimate. `DailyPlanCompiler` emits exactly one `executor.traverse_connector` step; `ActionQueueCompiler` revalidates the live connector, gate, and start-segment reachability before native movement, touch-warp entry, or `checkAction`. Every location change requires a fresh snapshot and replan; arbitrary location/position mutation is forbidden. |
| `exploration.visit_location` bounded training admission | One resolved current-map cross-location connector, or one exact clearable route obstacle | Same route facts as `route_connector_tile`; obstacle detail is branch-local through `current_location.objects`, `current_location.terrain_features`, player inventory/tool state and the existing clear candidate | n/a | five_gate_training_admitted_evd_218 / rolling_one_step / fresh_snapshot_required | Candidate IDs include source map/tile, connector kind, target map and optional arrival. Route-repair candidates preserve the existing clear parameters plus original route identity and expected target. Hidden E-drive EVD-218 verified candidate -> daily plan -> sole traverse queue -> native transition -> fresh snapshot -> one feature row. It does not authorize a stale multi-step queue. |
| `social_candidates[].route_connector_tile` | Rolling route step toward a legal NPC's current loaded location | `npcs.social_interaction[].current_instance_loaded/location_id`, `locations.route_graph`, `locations.route_connectors`, `locations.collision_grid`, `locations.route_action_branch_coverage`, optional `locations.route_gate_context` | NPC schedule pages are not used for pursuit | covered_for_current_loaded_position / covered_for_candidate_to_daily_plan / future_schedule_not_used / full_pursuit_runtime_verified | The candidate preserves `continuation.option_id`, NPC name, final current target location, and exact gift slot/item. Only the first resolved and currently executable connector is emitted. A fresh snapshot re-evaluates NPC position, route, inventory, limits/taste, menu state, time gates, and dynamic friendship-door collision after every transition. E run `runtime-native-social-smoke-20260717-164525` verified 6 connectors + 19 waits + one Abigail talk across 26 iterations. |
| `locations.collision_grid.notable_tiles[].friendship_door_*` | Live passability for interior `TouchAction=Door <NPC...>` room gates | Current map `TouchAction`, `Farmer.getFriendshipHeartLevelForNPC`, `GameLocation.IsGreenRainingHere`, year | n/a | covered_for_read / covered_for_gate / runtime_verified | Mirrors the decompiled touch-action branch: two hearts with any listed NPC opens passage; year-one Green Rain overrides Sebastian's door. A closed gate sets `collision_blocked=true` while preserving native collision separately, so all existing BFS consumers fail closed upstream. |
| `event_candidates[].interact_endpoint` | Concrete shop/action endpoint scoring candidates | `current_location.shop_action_tiles`, `locations.route_action_branch_coverage`, route-to-adjacent dry-run gates, `owner_service_status`, `service_time_status` | Shop pages pending | covered_for_read / covered_for_gate / controlled_for_movement / blocked_for_interact_execution | Candidate generation must exclude hard failures such as absent service owner or illegal route gate. Windowed time closures carry `allowed_now`, `allowed_today`, `next_open_time`, `effective_open_time`, `closes_at`, and `wait_cost` so the timeline layer can defer instead of dropping a same-day shop action. |
| `event_candidates[].recovery_*` | Concrete stabilize-day scoring candidates | `time.time`, `player.energy`, `menus.active_menu`, `current_location.home_context`, `locations.route_graph`, `locations.route_connectors`, `locations.route_gate_context`, `locations.collision_grid` | n/a | covered_for_gate / controlled_for_wait / current_home_sleep_runtime_verified / cross_map_return_runtime_verified | `recovery_refresh_plan` uses wait semantics; a proven current-home late-night target compiles to terminal `executor.sleep`; outside home the compiler emits exactly one verified connector toward home and replans from the next fresh snapshot. EVD-195 verifies the rolling return and following native sleep. |
| `current_location.home_context` | Home/bed target preview for recovery | `Utility.getHomeOfFarmer`, `FarmHouse.getEntryLocation`, `FarmHouse.GetPlayerBedSpot`, `BedFurniture.IsBedHere` | n/a | covered_for_read / covered_for_gate / current_home_sleep_runtime_verified | Enables already-home bed-adjacent route validation and supplies the exact target for terminal `executor.sleep`. It does not prove or execute a route home from another location. |
| `ranked_event_candidates[]` | Rank and time-schedule concrete economic and non-economic events by candidate id | `economic_candidates[]`, `event_candidates[]`, baseline option scores, candidate time windows | n/a | covered_for_scoring / covered_for_timeline_gate / controlled_for_movement / blocked_for_execution | Baseline scorer now preserves availability time fields and emits `timeline_status`, `scheduled_start_time`, `scheduled_wait_cost`, and `timeline_reasons`. Same-day closed shops can appear as `deferred`; closed-for-day or finish-after-close candidates are excluded upstream. |
| `daily_plan_compile_response.v1` | Convert ranked timeline candidates into `small_model_plan.v1` before action queue compilation | `ranked_event_candidates[]`, `DailyPlanCompiler`, `/api/v1/planner/daily-plan/compile` | n/a | covered_for_plan_surface / partial_for_execution | First slice supports wait chunks, shop/interact endpoint steps, buy-shop-item steps, recovery wait refresh, and route connectors only when expected target location is known. Unsupported kinds are skipped rather than faked. |

## Crop / Planting / Watering

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `time.season` | Seasonal crop legality and growth | `Game1.currentSeason`, `GameLocation.GetSeason()` | Crops / Seasons pages | covered_for_gate | Required by crop maintenance and buy supplies. |
| `time.weather` | Watering behavior context | Game weather fields, `HoeDirt.needsWatering()` | Weather page | covered_for_gate | Required by crop maintenance. |
| `current_location.terrain_features[].is_ginger` / `current_location.debris[]` / `foraging.harvest_ginger` / `executor.harvest_ginger` | Keep hoe-only ginger outside ordinary crop harvest while preserving exact planning and feedback | `Crop.hitWithHoe`, `HoeDirt.performToolAction`, `Hoe.DoFunction`, live `HoeDirt`/Crop/Hoe/player/debris fields | Ginger page pending refresh | covered_for_read / covered_for_candidate / covered_for_compile_gate / ordinary_and_special_collect_source_bound / native_executor_runtime_verified / task_attached_runtime_verified / dry_rain_efficient_inventory_energy_matrix_verified | Exposes exact Hoe slot, Efficient state, fractional energy cost, `(O)829 x1` world debris, native output context tags, Foraging XP `7`, retained HoeDirt, and rain-derived soil state. Generic current-map debris is transparent and can be rebound as the next native receipt after the action. Candidate selection requires an adjacent transparent stand tile. Runtime uses the shared cross-frame native tool lifecycle, rechecks attached special-order identity/tags, and verifies every projected delta without direct crop, debris, energy, XP, or quest mutation. Hidden Forest task-attached runs passed for ordinary and special-order collection. The 2026-07-30 isolated no-task matrix additionally passed dry standard Hoe, rainy Efficient full-inventory world-debris retention, and insufficient-energy upstream exclusion. |
| `current_location.large_terrain_features[]` / `foraging.harvest_bushes` / `executor.harvest_bush` | Harvest ordinary berry, tea, and golden-walnut Bush branches without collapsing their output contracts | `GameLocation.largeTerrainFeatures/checkAction`, `Bush.readyForHarvest/inBloom/GetShakeOffItem/performUseAction/shake`, `FarmerTeam.collectedNutTracker` | Wiki refresh pending; local decompile is normative | covered_for_read / covered_for_candidate / covered_for_compile_gate / ordinary_and_special_collect_source_bound / native_executor_runtime_verified / task_attached_runtime_verified / full_native_branch_matrix_verified | Fails closed on custom Bush subclasses and town bushes. Exposes full footprint, age/shelter/cooldown, exact branch/output/context tags/quality/quantity/XP, tile offset, and nut key. Candidate chooses a stand outside the full footprint plus an adjacent action tile inside it. Runtime reuses continuous movement and native `checkAction`, rechecks attached special-order identity/tags, then verifies combined debris/inventory output and branch-specific XP/nut state without direct mutation. Hidden Forest task runs passed ordinary and special-order collection. The 2026-07-30 no-task matrix additionally passed standard/Botanist berries, tea, mutex-backed walnut, already-collected exclusion, and shake-cooldown exclusion. |
| `farm.crops` | Crop maintenance candidates | Farm terrain features / `HoeDirt` | Crops page | covered_for_read | Existing crop maintenance source. |
| `current_location.crops[].needs_watering` event candidates | Concrete watering event candidates for model scoring | `HoeDirt.needsWatering()` via current-location transparent crop summaries | Crops page | covered_for_read / covered_for_gate / runtime_verified_targeted_watering | Emits `event_candidates[].kind=water_crop_tile`; daily plan compiles one exact tile into `executor.water_crop` with `target_location` and `target_tile_x/y`. |
| `farm.crops[].ready_for_harvest` event candidates | Concrete mature-crop candidates, compiler gate, single-tile harvest executor, and exact player-harvest skill XP | Farm crop summaries from `HoeDirt` / `Crop` state plus `Crop.harvest` / `HoeDirt.destroyCrop(false)` for runtime | Crops page pending | covered_for_read / covered_for_candidate / covered_for_compile_gate / covered_for_skill_xp / runtime_verified_grab_single_tile / runtime_verified_regrow_single_tile / runtime_verified_scythe_single_tile / runtime_verified_scythe_debris_output / runtime_verified_inventory_full_block / tested_offline | Emits available `event_candidates[].kind=harvest_crop_tile` for transparent ready crops; ranker includes it and daily plan compiles `harvest_crop`. Vanilla object crops expose exact Farming XP from the post-special-case harvest item base price; planted spring onions expose Foraging XP 3. Sunflower uses the native `421 -> 431` replacement before XP calculation. Ginger hoe harvest and custom Crop subclasses fail closed. Giant crops stay out of this executor. |
| `farm.resource_clumps[].is_giant_crop` / `harvest_giant_crop_tile` | Transparent boundary, dedicated executor, and exact Luck XP for giant crop clumps | `Crop.TryGrowGiantCrop`; `GiantCrop.performToolAction`; permanent `Farmer.luckLevel` | Crops page pending | covered_for_read / covered_for_candidate / covered_for_compile_gate / covered_for_skill_xp / runtime_verified_multi_hit_axe_harvest | Resource clumps include exact identity/tool fields and `runtime_verified` executor status. A complete player axe harvest carries exact Luck XP `50*((permanent_luck_level+1)/2)` through candidate and daily-plan parameters; partial swings carry no completion claim. Custom GiantCrop subclasses fail closed. Runtime smoke `runtime-giant-crop-smoke-20260712-182314` passed for clump/debris behavior; refreshed XP observation remains pending. |
| `farm.resource_clumps[].clear_kind` / `clear_farm_resource_clump` / `executor.break_farm_resource_clump` | Remove vanilla farm stumps and hollow logs without losing multi-hit timing or skill feedback | `ResourceClump.performToolAction`; `Axe.DoFunction`; `GameLocation.performToolAction`; live footprint/health/inventory | n/a | covered_for_read / covered_for_candidate / covered_for_compile_gate / native_executor_runtime_verified | Parent indexes 600/602 expose required Axe upgrades 1/2, selected best compatible slot, base upgrade, native `additionalPower`, effective upgrade, damage per hit, exact current-health hit count, and exact Foraging XP 25. The candidate binds an outside perimeter stand tile to one occupied hit tile. Runtime uses one native tool lifecycle per frame-separated swing; synchronous `clear_obstacle` is intentionally not used because equal `swingTicker` values are deduplicated. Exact clump identity, geometry, tool slot, hit budget, removal, and XP delta are rebound or observed. Hidden isolated smoke `runtime-farm-resource-clump-smoke-20260729-184321` loaded only the Bridge and Harness: both stump and hollow log matched four projected Gold Axe hits, were naturally removed, and awarded exactly 25 Foraging XP. |
| `current_location.debris[]` and `farm.debris[]` harvest output and pickup | Transparent dropped-item output plus location-generic pickup candidate/compiler/runtime accounting | `GameLocation.debris`, `Debris.itemId`, `Debris.item`, `Item.GetContextTags()`, `Debris.itemQuality`, `Debris.Chunks`, `Debris.collect(Farmer, Chunk)` | n/a | covered_for_read / covered_for_candidate / covered_for_compile_gate / ordinary_and_special_collect_receipt_bound / runtime_verified_farm_and_non_farm_debris_pickup | Current-location rows expose item identity, native context tags, quality, bounce timing, sink/essential flags, and per-chunk pixel/tile positions. Candidate, plan and compiler preserve the loaded location and validate `current_location.debris`; the farm field remains a compatibility projection. `executor.pickup_debris` calls decompile-confirmed `Debris.collect` and verifies inventory/debris state. Farm smoke `runtime-pickup-debris-smoke-20260712-185126` and Forest ordinary/special smokes `runtime-nonfarm-debris-ordinary-20260729` / `runtime-nonfarm-debris-special-20260729` passed with transparent tile following, debris `1 -> 0`, native task progress and changed state hash. |
| `farm.machines[]` ready output collection and single input loading | Transparent machine output/input plus candidate/compiler/runtime/value-diagnostic accounting | `GameLocation.objects` big craftables, complete `Data/Machines` asset (39 vanilla rows in the pinned 1.6.15 dump), CLR declaring types for `performObjectDropInAction`/`OutputMachine`, `Object.GetMachineData()`, `Object.readyForHarvest`, `Object.MinutesUntilReady`, `Object.heldObject`, `Item.salePrice()`, `Object.CheckForActionOnMachine`, `Object.performObjectDropInAction(probe:true)`, `Object.PlaceInMachine`, `MachineDataUtility.TryGetMachineOutputRule`, `MachineDataUtility.GetOutputItem(probe:true)` | Machines page pending | covered_for_read / covered_for_execution_semantics / rng_safe_read_gate / covered_for_candidate / covered_for_compile_gate / covered_for_value_diagnostic / covered_for_rule_summary / conservative_exact_rule_prediction / native_output_probe_compile_verified / runtime_verified_machine_output_collect / runtime_verified_machine_input_load / special_machine_models_pending / broad_machine_strategy_blocked | Machine rows expose tile, machine qualified id/name, ready flag, minutes remaining, held output item with `sale_price`, `machine_data` rule summary, and `machine_execution_semantics.v1`. The execution projection records runtime input/output declaring types and dispatch kind, item-input rule counts, conditional/random branch counts, random item/stack/modifier counts, static callback names, recalculate-on-collect count, input-probe RNG safety, exact-training status, and explicit block reasons. `loadable_inputs[]` is generated by live `performObjectDropInAction(probe:true)` only when item-trigger conditions cannot consume RNG. Prediction mirrors native rule selection without `Game1.random.ChooseFrom`: it evaluates nonrandom conditions itself and continues only when selection is deterministic. Random conditions, multiple valid random choices, `RandomItemId`, RNG-backed stack ranges/modifiers, unbounded item queries, static callbacks, recalculate-on-collect, and external runtime overrides are transparent but fail closed for exact training. Every accepted input carries `load_executor_status` and `predicted_output.training_eligibility_status`; missing fields are not treated as legacy success. `farm.process_machines` emits auditable unavailable rows for unsupported inputs, while only `available_data_driven`/`available_native_runtime_override` plus `exact_current_snapshot_probe_supported` may enter planning. The action compiler independently rebinds all three gates, so direct small-model output cannot bypass them. Deterministic inputs still expose output item/value/preserve/rule/time/additional-consumption diagnostics. Runtime reparses live machine data and verifies all six skill XP slots plus `MasteryExp` after native collection. Broad stochastic expectation models, vetted static-callback models, and broad multi-machine strategy remain pending. |

2026-07-13 correction update: the native machine output probe and snapshot `loadable_inputs[]` must not be generated inside the HTTP read request. Runtime testing showed that path can time out. EVD-067 reintroduces those fields through a SMAPI main-thread cache: `profile=machine` serves cached `loadable_inputs[]` and deterministic native `predicted_output` rows, verified by hidden/silent runtime smoke `runtime-machine-input-smoke-20260713-122831`. EVD-068 adds `profile=training_machine`, which is light enough for backend ingestion while still carrying `farm.machines`; `runtime-machine-daily-plan-smoke-20260713-123542` verified that the field survives candidate generation, daily-plan compilation, action-queue compilation, feature-row writing, and real executor loading. EVD-128 removed the former 64-row truncation; EVD-132 replaces its Farm/FarmHouse/Cellar-only scope with the live persistent interior topology, including player-controlled building interiors and player-owned machines on other persistent maps. Expensive input/native-output probes remain bounded to 16 eligible current-map machines per refresh and rotate across the eligible set; every unprobed row carries an explicit blocked status. EVD-140 closes exact current-map placement and adds the remote-location output head; EVD-149 closes generic one-connector cross-location placement with an untrimmed full snapshot; EVD-150 composes the same transparent fields into ordinary positive-benefit strategic relocation across one connector; EVD-151 adds compressed remote static walkability and exact BFS route evidence for cluster-internal targets; EVD-152 persists and revalidates every segment of an arbitrary resolved connector chain and verifies `Farm -> FarmHouse -> Cellar` in the isolated runtime. EVD-153 closes the Workbench-to-Furnace lifecycle. Remaining boundaries are stochastic expectation models, vetted static-output callbacks, demand/throughput strategy, special-machine execution models, and multiplayer ownership/sharing, not hidden row omission.

2026-07-26 RNG-safety correction: a transparent read must not call vanilla `MachineDataUtility.GetOutputData` on a `UseFirstValidOutput=false` rule, because vanilla calls `Game1.random.ChooseFrom` even when only one output is currently valid. The bridge now performs a side-effect-free equivalent selection for nonrandom conditions and requires exactly one valid output before calling `GetOutputItem`. It also refuses input probes with random trigger conditions and refuses output probes with any known RNG-backed field. This closes read-induced RNG mutation; it does not claim that stochastic machines already have an expected-value model.

2026-08-04 EVD-213 admission correction: `farm.collect_machine_outputs` is the training-facing output-only view of the existing machine service family. It accepts only exact ready non-incubator outputs in the current loaded location with inventory acceptance, reachable stand, exact held-item identity and complete structured skill/Mastery projection. It reuses the existing candidate, DailyPlan, queue compiler and native executor. `farm.process_machines` remains calibration-only because its input loading, crafting, placement, relocation and storage families require demand, reservation and time-value policy that is not universally closed. This is a MECE semantic split, not a second machine implementation. EVD-180 task binding remains compiler-covered but is not promoted to task-attached runtime evidence by EVD-213.

2026-08-04 EVD-214 admission correction: `farm.load_supported_machine_input` is the training-facing input-only view of the existing machine loader. It requires an active placement-bound `MachineSupportIntent` for the exact machine, a fresh deterministic positive input net value, exact proof that `AdditionalConsumedItems` count is zero, and enough unreserved quantity in the exact player slot. The main-thread cached prediction now carries `additional_consumed_item_count` directly, so the value gate remains transparent without reintroducing full machine catalogs into high-frequency snapshots. Candidate, plan, compiler and dispatch all bind ledger IDs/revisions and fail closed on drift. The hidden isolated run `runtime-machine-daily-plan-smoke-20260804-104556` verified native load, processing start, dataset output and intent completion. This does not admit arbitrary loading, stochastic or extra-resource recipes, task/collection demand, or the complete craft-placement-load lifecycle.

2026-08-04 EVD-215 lifecycle admission: `farm.establish_supported_machine_capacity` does not add a new transparent field family or executor. It consumes the existing machine-crafting demand/value projection, `player.machine_placement`, `farm.machines[].loadable_inputs[]`, strategy/material ledgers, route/collision evidence, and `MachineSupportIntent` stage. The controller exposes exactly one next stage: select a bounded positive-net craft for `goal.economy.earn_money`, continue the same intent through native placement, then continue the exact placement-bound machine through the EVD-214 loader. A bound placement absent from the world retries its exact target. Hidden isolated run `runtime-supported-machine-capacity-20260804-120211` verified native craft, planner-selected `Farm:61,15` placement, deterministic first load, processing, exact intent completion, and three feature rows. Invalid active intents, non-money goals, incomplete value evidence, additional consumption, reservation conflict, remote placement, task/collection demand, and projection drift remain fail closed. The semantic is now training-admitted only within this bounded scope.

2026-08-05 EVD-216 task-machine admission: deterministic machine probes now transport the exact predicted output context tags alongside output identity and additional-consumption count. `farm.fulfill_machine_task_demand` exposes only an existing current-location machine load whose exact output satisfies one live `ResourceCollectionQuest` or `CollectObjective`, has zero additional consumption, and has no unresolved active material reservation. The load is a task source step and cannot claim progress. When the live output becomes ready, the same high-level semantic reuses the existing EVD-180/EVD-213 receipt binding and native collector; only native inventory receipt may advance the task. The isolated ordinary/special matrix `runtime-machine-task-demand-20260805-001630` observed `0/1` after load and `1/1` after natural output collection, including runtime context-tag rebinding. Task-driven machine construction, remote processing, stochastic output, additional consumption, Raccoon requests and broad machine/quest options remain fail closed.

2026-08-05 EVD-217 task-capacity admission: `farm.establish_supported_machine_capacity` composes the existing machine-crafting demand projection, inventory machine count, placed machine count, placement rows, material graph, exact task source evidence, and `MachineSupportIntent` into one task-driven capacity continuation. It selects current-location direct placement when the machine is already in inventory and otherwise selects the existing craft chain; after placement, task input and receipt remain owned by `farm.fulfill_machine_task_demand`. Exact output identity/tags come only from a current native deterministic probe with zero additional consumption. Canonical task sources, task-demand freshness, ledger revisions, exact slot availability, and placement identity are rebound at candidate, plan, compiler, binder, and dispatch boundaries. Hidden/silent runs `runtime-supported-machine-capacity-evd217-final2-ordinary-20260805`, `runtime-supported-machine-capacity-evd217-final-special-20260805`, and `runtime-supported-machine-capacity-evd217-final2-inventory-20260805` verified ordinary-task craft, special-order craft, and direct-inventory placement respectively, followed by native placement/load/natural processing/collection and progress `0/1 -> 1/1` only at receipt. The initial direct-placement run exposed a remote-route precedence bug; task capacity now accepts only current-location `place_machine_item`. Final-source reruns also require array-valued queue IDs and a stable direct-placement intent identity ending in `fleet=0:required=1`. No field family or executor was added. Remote, stochastic, additional-consumption, Raccoon, mixed-source, task-drift, and conflicting-reservation cases remain fail closed; those negative boundaries are static compiler/ledger evidence, not runtime claims.

2026-07-26 runtime-dispatch correction: `machine_execution_semantics.v1` also records declaring type and dispatch kind for collection, minute update, day update, Fairy Dust, and placement, plus explicit native/external override method lists. This is required to expose post-load behavior such as Cask quality aging and Wood Chipper collection state, rather than classifying only the initial drop-in call. Live isolated smoke `runtime-machine-semantics-final-20260726-2002` verified a real `StardewValley.Objects.WoodChipper`: random-trigger reads remained blocked and produced no training input, while the low-level native executor still loaded `(O)169`, started 180 minutes of processing, and produced `(O)388`.

2026-07-26 Cask special-model update: the vetted `StardewValley.Objects.Cask: OutputCask` callback is modeled as `cask_quality_aging.v1`; other static callbacks remain blocked. A valid operational Cask now exposes exact input/output identity, initial/next/final quality, `AgingMultiplier`, days to next quality, days to iridium, final-quality sale value, and the fact that `MinutesUntilReady=999999` is a processing sentinel. Loaded Casks expose the same live feedback through `machine_special_state.v1`, including raw `agingRate` and `daysToMature`. Isolated runtime `runtime-cask-special-model-final-20260726-2028` verified normal-quality wine at rate 1, 14 days to silver, and 56 days to iridium; `runtime-cask-cheese-multiplier-20260726-2032` verified Cheese `AgingMultiplier=4`, 14 days to iridium, final sale price 920, and native loading in `Cellar`. Cask candidates may now enter exact training; random and unvetted callback machines remain fail-closed.

2026-07-26 Deconstructor special-model update: vanilla `(BC)265` with the exact `StardewValley.Object: OutputDeconstructor` callback is modeled as `deconstructor_recipe_recovery.v1`. The bridge invokes only the audited native `probe:true` callback, whose implementation reads the live `CraftingRecipe.craftingRecipes` table and performs no RNG or game-state mutation. It exposes the recovered item/count/value, recipe rule and effective duration; unsupported recipe shapes remain blocked. Hidden isolated runtime `runtime-deconstructor-special-model-final-20260726-02` verified Hardwood Fence `(O)710` -> Iron Bar `(O)334` x2, predicted and observed 60 minutes, native load verification, and a changed bridge state hash. The then-blocked special callbacks were subsequently split into exact, distribution-only, and fail-closed branches by EVD-158 through EVD-162.

2026-07-27 Incubator natural lifecycle and catalog closure: `machine_is_incubator`, `machine_completion_interaction_kind`, `ordinary_output_collection_supported`, `machine_data.is_incubator`, and `machine_special_state.v1` distinguish an incubated egg from an ordinary collectible machine output. The exact `incubator_animal_hatch.v1` model is limited to the base `Object.OutputIncubator` callback, resolves native animal type/duration, uses `FarmAnimalData.PurchasePrice` without scaling, and reserves future capacity as `limit - occupants - active_incubators`. The bridge now enumerates every distinct live `Game1.farmAnimalData[*].EggItemIds` entry, evaluates the exact machine rule, building occupant compatibility and vetted native output probe, and exposes both the complete accept/reject matrix and compatible-only catalog without example IDs or row truncation. Ready eggs expose the first-ready `AnimalHouse.objects.Values` selection contract, deterministic name and native birth-message/NamingMenu controls. The typed plan/compiler rebinds all fields; runtime advances the birth message only under a ready-incubator AnimalHouse event, clicks `NamingMenu.doneNamingButton`, and verifies exact animal creation, occupancy `+1`, egg removal and menu closure without direct state mutation. EVD-156 verified direct ready-hatch naming. EVD-157 copied the source save per run and verified two complete natural paths: ordinary Incubator `(BC)101` + Egg `(O)176` in Coop exposed 8/9 compatible egg IDs and hatched `White Chicken:NaturalPip`; Ostrich Incubator `(BC)254` + Ostrich Egg `(O)289` in Barn exposed 1/9 and hatched `Ostrich:NaturalOllie`. Each path used native load plus six verified `executor.sleep` transitions from 9000 minutes to ready.

2026-07-27 Seed Maker special-model update: vanilla `(BC)25` with the exact base `StardewValley.Object: OutputSeedMaker` callback is modeled as `seed_maker_day_save_rng.v1`. The callback does not consume shared `Game1.random`; it creates a fresh deterministic RNG from live days played/save ID, machine tile and placement time, so the bridge may call the audited native callback with `probe:true` without advancing game RNG. The prediction exposes exact output identity/stack/value, source crop mapping, selected branch, duration, seed inputs and the native branch distribution. Non-vanilla runtime types, callback drift, missing crop mapping, unrecognized output branches and added ready-time modifiers fail closed. EVD-162 later corrected Geode Crusher to exact fresh-counter RNG and Anvil to complete pre-load distribution plus exact post-load Trinket state.

2026-07-27 Solar Panel and non-input callback coverage: `time.weather_for_tomorrow` now exposes the default-context engine field, while `time.location_context_weather[]` losslessly reports current flags and tomorrow weather for every already-initialized location context. The adapter enumerates the existing network dictionary and never calls `GetWeatherForLocation`, because creating a missing context would run weather generation and advance game RNG. Vanilla Solar Panel `(BC)231` exposes `solar_panel_special_state.v1`: current lifecycle state, Battery Pack `(O)787`, exact timer/readiness, location context, current/tomorrow weather, current clock-progress gate, the seven-day `CalculateMinutesUntilMorning` initialization, initial sunny-outdoor `-2400` adjustment, and native `Inside`/`Rain` blockers. Multi-day completion remains explicitly weather-dependent instead of guessed. `machine_execution_semantics.v1` separately inventories all custom output methods and the `ItemPlacedInMachine`, `DayUpdate`, `OutputCollected`, and `MachinePutDown` families; non-input callbacks without a vetted state model are marked `blocked_unvetted_custom_callbacks`.

2026-07-27 Statue of Endless Fortune conditional-output update: vanilla `(BC)127` with the exact base `Object.OutputStatueOfEndlessFortune` callback exposes `endless_fortune_special_state.v1`. `Utility.getTodaysBirthdayNPC` and `NPC.getFavoriteItem` are pure current-state reads; the birthday branch therefore reports the exact NPC and first resolvable loved item. If that branch has no item, the callback consumes shared `Game1.random.Next(4)` and returns one each of `(O)72`, `(O)337`, `(O)749`, or `(O)336`; the bridge reports the complete uniform `0.25` distribution without invoking the callback or RNG. Ordinary-day actual output identity is explicitly ineligible for exact training. Live held output/readiness remain exact, and `ClearContentsOvernightCondition=TRUE` is represented so planners know an uncollected result is discarded and regenerated at the next `DayUpdate`.

2026-07-25 removal update: every `farm.machines[]` row now exposes `runtime_type`, `object_type`, `fragility`, `removal_status`, `removal_safe_now`, all block reasons, exact Pickaxe slot/qualified ID, the decompile-bound native contract, and `removal_projection_fingerprint`. Safety is false for noncurrent/nonowned machines, non-Crafting or nonrecoverable objects, processing or ready output, any held item/attachment, temporary invisibility, unknown tool-action overrides, or no Pickaxe. `executor.remove_machine` requires an explicit relocation intent and has no model-facing daily candidate. EVD-147 proves native removal, debris/adjacent auto-collection, and re-placement; strategic layout selection remains separate.
| `farm.crop_catalog` | Seed/crop seasons, phases, paddy, regrow, harvest structure, conservative and estimated season harvest value | `Game1.cropData`, `CropData`/`Crop`, `Crop.harvest`, `ItemRegistry.Create(...).salePrice()` | Crops / Rice Shoot / Fertilizer | covered_for_read / covered_for_gate / runtime_verified_catalog_fields / value_signal_unverified / estimated_value_unverified / regrow_estimate_unverified | Required by `economy.buy_supplies`; planting candidates still use exact `current_location.planting_context` seed/tile results rather than re-deriving crop legality. Runtime snapshot `runtime-harvest-crop-smoke-20260712-162723` verified populated vanilla catalog fields, including 45 Grab and 5 Scythe entries. `expected_first_harvest_value` is the conservative min-stack lower bound; `estimated_first_harvest_value` uses mean min/max stack plus expected extra harvest chance. Regrow crops also carry transparent `regrow_days`, estimated regrow/total harvest counts, and season harvest value/net diagnostics based on `adjusted_grow_days`, `days_remaining_in_season`, and one-time seed cost. |
| `plant_seed_tile.seed_unit_cost/net_value` | Transparent seed cost and first-harvest net value diagnostics | `menus.shop_stock`, `locations.shops.stock_preview`, `farm.crop_catalog` | Crop/shop pages pending | covered_for_candidate / tested_offline | Candidate generator emits seed cost only when transparent shop stock/preview has a price for that seed. Net values subtract that cost from conservative and estimated first-harvest values; missing price means no net field, not guessed. |
| `player.seed_inventory` | Candidate seeds currently owned | `Game1.player.Items`, `Object.SeedsCategory`, `Crop.ResolveSeedId` | Crops page | covered_for_read / covered_for_gate | Required by `economy.buy_supplies`. |
| `player.seed_inventory` event linkage | Owned seed item id / slot / stack for planting candidates | `Game1.player.Items`, seed category, transparent planting context seed ids | Crops page | covered_for_candidate / compile_verified_offline / reservation_rule_unverified | `plant_seed_tile` carries `item_id`, `(O)seed_id`, optional `slot_index`, and transparent stack quantity into daily-plan parameters; daily plan reserves one seed per accepted planting candidate. Broad seed acquisition remains separate. |
| `current_location.planting_context` | Per-tile seed legality preview | `GameLocation.CanPlantSeedsHere`, `CheckItemPlantRules`, `SeedsIgnoreSeasonsHere`, `HoeDirt` | Crops / Greenhouse / Indoor Pot pending | covered_for_read / covered_for_gate / runtime_verified_single_tile_planting / candidate_compile_verified_offline | `executor.plant_seed` is enabled only for one exact tile/seed when `hard_rule_allows_planting=true`; `plant_seed_tile` candidates now require transparent tile/seed approval before daily-plan compile. |
| `current_location.planting_context.*grow_days*` | Mature-before-season-end preview and maturity-risk ranking | `Crop` growth phase logic, fertilizer speed, Agriculturist | Crops / Fertilizer / Farming Skill | covered_for_read / covered_for_candidate_gate / rank_signal_unverified | Candidate generator blocks `plant_seed_tile` when `can_mature_before_season_end_with_paddy_if_eligible=false`; expected effects carry adjusted grow days and days remaining when available; ranker derives maturity-risk plus conservative first-harvest value signals and daily plan emits `maturity_slack_days`. Runtime/unit re-smoke pending. |
| `current_location.planting_context.*paddy*` | Paddy/rice growth acceleration | `HoeDirt.hasPaddyCrop`, water scan, `CropData.IsPaddyCrop` | Rice Shoot | covered_for_read / covered_for_candidate_gate | Candidate timing consumes the paddy-aware maturity flag already emitted by the bridge; no broad rice-layout policy is enabled yet. |

## Buying / Shops

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `locations.shops` | Shop data, owners, festival closure context | `DataLoader.Shops`, `ShopBuilder.GetCurrentOwners`, `AreStoresClosedForFestival` | Shop pages pending | covered_for_read / covered_for_gate | Required by buy supplies. |
| `current_location.shop_action_tiles` | Map shop endpoints | `GameLocation.performAction` `OpenShop`/`Buy` branches | Shop pages pending | covered_for_read / covered_for_gate | Shop-opening candidates consume endpoint tile, action token, owner service status, and service time status before model scoring. |
| `current_location.shop_action_tiles[].owner_service_status` | Owner/NPC counter availability for shop endpoints | Shop owner/NPC data and current-location NPC positions/service areas | Shop pages pending | covered_for_read / covered_for_gate | Includes `owner_required`, `owner_npc`, `owner_found`, owner tile, `in_service_area`, and `block_reason`. Candidate layer blocks with `interact_shop_owner_npc_not_at_service_counter` when required owner service is unavailable. |
| `current_location.shop_action_tiles[].service_time_status` | Door/service time and state gate for shop endpoints | Entrance `LockedDoorWarp`, shop endpoint action fields, festival/Town Key/friendship/green-rain/Wed closure checks | Shop pages pending | covered_for_read / covered_for_gate | Includes current time/day/season, `open_time`, `effective_open_time`, `close_time`, `allowed_now`, `time_allowed`, entrance tile/action, and `block_reasons`. Candidate layer blocks with `interact_shop_service_time_blocked` when `allowed_now=false`. |
| `menus.active_menu` | UI/menu state for shop preview | SMAPI/UI active menu | n/a | covered_for_gate | Required by buy/sell previews. |
| `menus.shop_stock` | Active shop item/price/stock preview | `ShopMenu.itemPriceAndStock`, `ItemStockInformation`, `CanBuyItem` | Shop pages pending | covered_for_read / covered_for_gate | Required by buy supplies. |
| `menus.shop_stock.*affordability*` | Money/currency/trade/inventory audit | `ShopMenu` purchase logic | Shop pages pending | covered_for_read / controlled_for_single_purchase | Purchase executor only accepts quantity `1`, matching `expected_shop_id`, in-stock/affordable/inventory-acceptable entries, and a safe active `ShopMenu`. |
| `menus.shop_stock.entries[].executor_purchase_enabled` | Per-entry purchase side-effect allow flag | Core/Bridge explicit safety field | n/a | covered_for_gate / controlled_for_single_purchase | `false` entries remain preview-only; `true` entries may be bought by `executor.buy_shop_item` after all price/shop/menu/quantity checks pass. |

## Selling / Shipping

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.inventory` sell audit fields | Sell/shipping candidate audit | `Item`, `Object`, `ShopMenu` sell path | Shipping/Selling pages pending | covered_for_read / covered_for_gate / runtime_verified_bounded_shop_sale | Required by shop-sale preview and exact runtime rebind. |
| `player.inventory.protected_from_auto_sell` | Conservative protection gate | `Item.specialItem`, `canBeTrashed`, quest/object flags | n/a | covered_for_read / hard_block_for_shop_sale | Protected stacks never become shop-sale candidates. Strategic reserve policy is an additional upstream responsibility. |
| `player.inventory.auto_sell_protection_reasons` | Explain protected sell blocks | Same as above | n/a | covered_for_read / hard_block_for_shop_sale | Prevents model judgement replacing the default hard gate. |
| `player.inventory[].donate_color_context` | Reproduce `DonateObjective` preserved-parent color matching without changing ordinary item tags | `ColoredObject.preservedParentSheetIndex`; `ItemContextTagManager.GetBaseContextTags`; `DonateObjective.IsValidItem` | EVD-234 hidden/silent native Qi red donation | covered_for_read / covered_for_candidate / covered_for_compile_rebind / native_runtime_verified | Publishes `is_colored_object`, exact parent item ID, sorted parent base tags and projection status. Parent tags are never merged into `context_tags`. Colored items with a parent fail closed if this projection is absent or non-exact; non-Donate objectives continue using only current-item tags. |
| `player.inventory_capacity` | Inventory free-slot gate for mechanical actions with inventory outputs | `Game1.player.Items`, `Game1.player.maxItems`, `Farmer.couldInventoryAcceptThisItem` runtime precheck | n/a | covered_for_read / covered_for_compile_gate / runtime_verified_inventory_full_block | Compiler blocks Grab harvest when no empty slot or compatible stack is transparent; RuntimeTestHarness rechecks with `couldInventoryAcceptThisItem` before calling `Crop.harvest`. |
| `menus.sell_context` | Active shop sell filters and buyback context | `ShopMenu` sell/buyback fields | Shop pages pending | covered_for_read / covered_for_gate / runtime_verified_exact_sale | Active-menu continuation rebinds exact shop, slot, item, quantity and unit price before native right-click sale. |
| `locations.shops[].salable_item_tags` / `sale_preview` | Global unloaded-shop sale acceptance and payout preview | `DataLoader.Shops`, `ShopData.SalableItemTags`, current gate/owner state, inventory context tags and `sellToStorePrice` | Shop pages pending | covered_for_read / covered_for_candidate / runtime_verified_bounded_shop_sale | Enables upstream shop selection without opening every menu. Runtime EVD-220 verified Blacksmith `category_metal_resources` acceptance for `(O)378`; active `ShopMenu` remains authoritative immediately before input. |
| `farm.shipping_bins` | Shipping bin positions/range/status | `ShippingBin` building/range logic | Shipping page pending | covered_for_read / covered_for_gate | Shipping executor disabled. |

## Route / Movement / Shop Opening

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `current_location.route_context` | Local collision probes around player | `GameLocation.isCollidingPosition`, `PathFindController` rect | n/a | covered_for_read | Local radius only. |
| `locations.collision_grid` | Current-map compressed collision grid | `GameLocation.isCollidingPosition`, map layers | n/a | covered_for_read / covered_for_gate | Current-map path gate consumes it. |
| `locations.route_blockers` | Collision participant classes/bounding boxes | `characters`, `objects`, `terrainFeatures`, `resourceClumps`, `largeTerrainFeatures`, `furniture` | n/a | covered_for_read | Informational; collision API remains source of truth. |
| `current_location.warps` | Raw current map warps | `GameLocation.warps` | Location pages pending | covered_for_read | Cross-map graph not solved. |
| `current_location.doors` | Door connector hints | `GameLocation.doors` and door resolution | Location pages pending | covered_for_read | Locked/target resolution partial. |
| `current_location.interior_doors` | Interior door open/action/touch state | `InteriorDoorDictionary`, `InteriorDoor` | n/a | covered_for_read | Cross-room graph not solved. |
| `locations.route_connectors` | Normalized current-map connector index | Warps, doors, interior doors, action warps, shops | Location/shop pages pending | covered_for_read | Index only, not full graph. |
| `route_connector_tile` event candidates | Candidate connector tiles for current-map movement scoring | `locations.route_connectors`, `locations.collision_grid`, `locations.route_action_branch_coverage`, route compiler gates | Location/shop pages pending | covered_for_gate / covered_for_candidate_to_daily_plan / controlled_for_movement / controlled_for_cross_map_execution | Connector activation is fail-closed and uses native movement or `checkAction`; target location and arrival coordinates are carried structurally through ranking and daily-plan compilation, then checked against the latest live connector before activation. |
| `locations.route_graph` | Loaded-location cross-map graph preview | `Game1.locations`, warps, doors, action warps, locked-door warps | Location/shop pages pending | covered_for_read / covered_for_gate | Uses `resolved` edges only for compiler graph gate; execution disabled. |
| `locations.route_map_summaries` | Loaded-location route map dimensions and segment status | `Game1.locations`, map layers, route actions/connectors | n/a | covered_for_read / covered_for_audit | Summaries only; full collision grids are current-location only. |
| `locations.route_gate_context` | Locked/conditional route gates | `LockedDoorWarp`, `ConditionalDoor`, `GameStateQuery`, `BuildConditions` | Shop/location pages pending | covered_for_read / covered_for_gate | Route planner and route-to-shop smoke must honor `allowed_now=false` before traversal instead of using direct traversal to bypass the gate. |
| `locations.route_gate_context.action_gates[].allowed_now` | Route gate legality before connector traversal | Same as route gate context, consumed by route-to-shop smoke before crossing a gate edge | Shop/location pages pending | covered_for_read / covered_for_gate | Direct traversal harness must skip blocked gate edges. Example: SeedShop Wednesday closure is rejected before entering the store. |
| `locations.route_action_branch_coverage` | Hardcoded action branch audit | `GameLocation.performAction` branches | n/a | covered_for_read / covered_for_gate | Unsupported branches block only target/path relevant cases. |
| `exploration.visit_location` target branch gate | Block unsupported target action tile | Core compiler | n/a | covered_for_gate | Does not block unrelated map branches. |
| `exploration.visit_location` current-map path gate | BFS path avoids unsupported branch tiles | Core compiler over transparent collision grid | n/a | covered_for_gate / blocked_for_execution | Current-map only; route executor disabled. |
| `exploration.visit_location` cross-map graph gate | Resolved loaded-location path existence | Core compiler over `locations.route_graph` | n/a | covered_for_gate / blocked_for_execution | Blocks `route_graph_no_resolved_path` only when graph is present and no resolved path exists. |
| `exploration.visit_location` start-segment graph gate | Current-map path to first cross-map connector | Core compiler over `locations.route_graph`, `locations.collision_grid`, and branch coverage | n/a | covered_for_gate / blocked_for_execution | Validates only the first segment; later per-location segments pending. |
| `exploration.visit_location` route summary context | Target-location segment validation status | Core compiler over `locations.route_map_summaries` | n/a | covered_for_audit / blocked_for_execution | Exposes pending later segment status without blocking unrelated legal actions. |
| `executor.interact` compiler contract | Adjacent transparent map-action interaction primitive | `Game1.pressActionButton`, `GameLocation.checkAction`, `performAction` evidence | n/a | covered_for_gate / blocked_for_execution | Narrow contract only; generic action button and runtime execution remain blocked. |
| `interact_endpoint` event candidates | Endpoint preview and route-to-adjacent scoring | `current_location.shop_action_tiles`, `locations.route_action_branch_coverage`, `locations.collision_grid`, `owner_service_status`, `service_time_status` | Shop pages pending | covered_for_gate / controlled_for_movement / blocked_for_interact_execution | Candidate may move to adjacent tile via safe movement, but does not press action. Closed service windows, missing owners, and illegal route gates are upstream candidate exclusions. |

## Strategic / Value Context

| Field | Purpose | Local evidence | Wiki evidence | Gate/status | Notes |
|---|---|---|---|---|---|
| `player.skills_detail` / `event_candidates[].skill_experience_*` | Select a current positive skill-growth action toward Grandpa total-level thresholds without confusing buffs with permanent progress | `Farmer.Level`, `GetUnmodifiedSkillLevel`, `GetSkillLevel`, `experiencePoints`, `getBaseExperienceForLevel`, action-specific native XP branches | pending secondary comparison | covered_for_read / world_model_projected / strict_current_candidate_binding_implemented / needs_runtime_regen / family_coverage_partial | Contains all six vanilla rows, exact XP and next-level gaps. Current crop, giant-crop, wild-tree, farm stump/log, spawned-forage, twig, seed/artifact-spot, fishing, MineShaft stone, and native monster candidates carry structured XP evidence. `raise_skill_levels` accepts only permitted current candidates with one complete `exact*` status, valid bounds, and positive possible XP; duplicate, malformed, zero-only, incomplete, or unknown-skill evidence is rejected. The full missing-source inventory and exit conditions are in `docs/skill-experience-source-audit.md`. |
| `player.luck_context` | Keep daily luck, permanent Luck skill, and temporary Luck buffs separate for rule-specific weighting | `Farmer.DailyLuck`, `Farmer.LuckLevel`, `FarmerTeam.sharedDailyLuck`, `Farmer.hasSpecialCharm`, `BuffManager.AppliedBuffs` | pending secondary comparison | covered_for_read / world_model_projected / needs_runtime_regen | TV fortune telling only observes the already-set daily value. Special Charm modifies daily luck; food/drink/equipment buffs modify effective Luck level. Gameplay rules combine these parameters with different coefficients, so the bridge does not invent one universal summed luck score. |
| Fishing skill XP projection | Rank a legal fishing attempt by transparent skill progress without pretending its stochastic quality/treasure outcome is fixed | `BobberBar` quality/difficulty state; `FishingRod.pullFishFromWater`; `FishingRod.openTreasureMenuEndFunction` | pending secondary comparison | covered_for_read / covered_for_candidate_bounds / covered_for_observed_delta / needs_runtime_regen | Each resolved fish outcome carries current effective difficulty after Blessing of Waters/first-catch rules, boss status, and the current rod's maximum raw quality. Aggregated candidates expose strict successful-catch Fishing XP bounds and conditional treasure Luck XP. Runtime records before/after Fishing and Luck XP; failures remain executor calibration. |
| `player.money` | Buy/sell/value planning | `Game1.player.Money` | n/a | covered_for_gate | Required by buy supplies. |
| `player.energy` | Work/time planning | `Game1.player.Stamina` | n/a | covered_for_gate | Required by movement/farm plans. |
| `time.time` | Daily time budget and shop/route gates | `Game1.timeOfDay` | Time/shop pages pending | covered_for_gate | Required by economic/exploration/recovery. |
| `npcs.friendships` | Gift/social and some route gates | Player friendship data | Friendship page pending | partially covered | Also used by `LockedDoorWarp` preview when NPC provided. |
| `quests.mail_received` | Progress unlocks, warp mail gates | Player mailReceived | Quest/location pages pending | partially covered | Needs broader field dictionary alignment. |
| `player.quest_crafting` | Bind an accepted `CraftingQuest` to an exact learned non-cooking recipe, personal or current-location Workbench material source, output-capacity result and native task terminal | `CraftingQuest.ItemId/OnRecipeCrafted`; `CraftingRecipe.craftingRecipes/recipeList/ItemMatchesForCrafting`; `CraftingPage.receiveLeftClick`; existing personal and Workbench native crafting projections | n/a | covered_for_read / covered_for_candidate / covered_for_compile_rebind / native_personal_runtime_verified_evd_235 / workbench_shared_executor_source_guarded | The scan runs only when an accepted incomplete `CraftingQuest` exists and only on the full snapshot profile. Rows preserve exact task/recipe/output identity, reverse-slot or native container consumption, unavailable materials, projected craft count, output acceptance, Workbench topology and native contract. Unknown, unlearned, cooking, nondeterministic or multiple-output recipes fail closed. Runtime does not mutate task progress directly; it requires the native `OnRecipeCrafted` terminal and records task identity plus before/after terminal state in the training result. |
| `farm.grandpa_score` / progress fields | Long-term strategy value | Existing core goal evaluator | Grandpa page pending | covered_for_gate | Strategic, not executor hard rule. |
| `world_progress.marriage_house` / `housing.advance_farmhouse` | Bind the exact next vanilla host upgrade without conflating direct Grandpa points, construction delay, or level-3 infrastructure value | `Farmer.HouseUpgradeLevel`, `daysUntilHouseUpgrade`, `GameLocation.carpenters/houseUpgradeOffer/houseUpgradeAccept`, ScienceHouse Carpenter action, Robin location, construction state, exact money/material tuple, Cellar projection | pending secondary comparison | covered_for_read / covered_for_candidate / covered_for_compile_rebind / native_runtime_verified_all_three_purchase_tuples / explicit_player_confirmation_required | Levels 0->1, 1->2, and 2->3 share one typed native executor. Immediate settlement is exact money/material/countdown; eventual level/remap remains a distinct construction receipt. EVD-229 closes five gates but does not admit autonomous training. |
| `world_progress.marriage_house.home_renovations` / `housing.renovate` | Bind one exact explicit home-layout command to the complete live vanilla renovation catalog without asking the model to generate menu mechanics or map writes | `Data/HomeRenovations`; `HouseRenovation.GetAvailableRenovations`; public FarmHouse `NetInt` fields; player mail/FirstPurchase markers; `CanModifyCrib`/crib bounds; region tile/furniture occupancy; Robin Carpenter service and native menu order | player-facing semantics can be secondarily compared, but runtime authority is locked decompile plus live data | covered_for_read / player_command_only / covered_for_candidate / covered_for_daily_plan / covered_for_fresh_compile_rebind / native_runtime_verified_all_18 / policy_training_excluded | Every option publishes ID/order, requirements/actions, regions, obstruction, price, first-purchase/refund and projection fingerprint. Changed payload, unsatisfied family/house requirement, missing destructive confirmation, blocked region, money/menu/service/route drift or stale projection fails closed. EVD-301 covers 18/18 entries and both negative-price refund states. |
| `npcs.friendships[]`, `npcs.social_interaction[]` / `social.advance_partnership` | Advance exact vanilla dating, marriage proposal, or Krobus roommate state without treating relationship items as ordinary gifts | `NPC.tryToReceiveActiveObject`, `NPC.engagementResponse`, `Game1.queueWeddingsForToday`, player inventory/context tags, current commitment and house level, friendship status/points/wedding date/roommate flag, current NPC instance and route | n/a | covered_for_read / covered_for_candidate / covered_for_compile_rebind / native_runtime_verified_bouquet_marriage_roommate_and_wedding / five_gate_evaluation_only_evd_230 / explicit_player_confirmation_required | Bouquet is `(O)458` at 2000 points; Mermaid's Pendant is `(O)460` at 2500 points plus house level 1; roommate proposal requires the exact `propose_roommate_Krobus` tag, 10 hearts and house level 1. All three reuse the sole social route and executor. EVD-230 verifies exact item decrement, relationship output, spouse/roommate fields, and a three-sleep native `Engaged -> Married` settlement on the scheduled date. Wilted Bouquet stays outside this progression option. |
| `machine_infrastructure.v1` candidate parameters | Evaluate level-3 farmhouse infrastructure without a fixed Cellar bonus or hidden machine-row omissions | Complete `farm.machines[]`, live machine status/durations, bounded native input probes, `locations.route_graph`, Cellar placeability projection, and exact purchase balance | pending secondary comparison | covered_for_candidate / covered_for_compile_rebind / native_purchase_runtime_verified / numeric_utility_blocked | Separates complete fleet state from partial current-map input evidence. Route cost is only a resolved graph-hop lower bound, not walking ticks or round-trip time. Current inventory alternatives are not plan-horizon demand. The exact 100000g native level-3 purchase is runtime verified by EVD-229, but numeric ranking still waits for plan-horizon demand, Cask aging, durable supply, and comparable capital uses. |
| `player.machine_crafting` | Expose every learned machine recipe from game start and exact personal or Workbench-connected supply | `Game1.player.craftingRecipes`, `CraftingRecipe.craftingRecipes/recipeList/ItemMatchesForCrafting`, `ItemRegistry`, `Object.GetMachineData`, `Workbench.checkForAction`, `MultipleMutexRequest`, `CraftingPage.receiveLeftClick/exitThisMenuNoSound`, `Cask.IsValidCaskLocation` | pending secondary comparison | covered_for_read / covered_for_candidate / covered_for_compile_rebind / native_personal_runtime_verified / native_workbench_runtime_verified | Recipe identity and materials are scanned independently of farmhouse level. Every Workbench source records its exact access point, ordered native adjacent container nodes, player-then-container consumption rows, ownership/topology blockers and post-consumption output acceptance. Demand order is task requirement, then production capacity, then collection path. Runtime rebinds all source fields, walks adjacent, opens through native `checkAction`, requires native Workbench/chest mutex ownership, clicks the native recipe and inventory, exits through the menu callback, and verifies exact per-source material/output and recipe-count deltas. EVD-153 continues the resulting Furnace through placement, Iron Ore/Coal input, natural processing and collection. |
| `player.machine_crafting.sources[].consumption_plan[].unit_sale_price` / `total_sale_value`; `farm.machines[].loadable_inputs[].unit_sale_price` / `stack_total_sale_value`; `goal_support_*` | Price a machine-crafting support action for an explicit current `goal.economy.earn_money` subgoal without inventing future inventory or a realized random output | Native recipe consumption rows, `Item.salePrice()`, complete current input stacks, deterministic `predicted_output.sale_price/stack/required_count`, current machine-capacity deficit and current build-window state | n/a | covered_for_exact_current_backlog / covered_for_material_opportunity_cost / covered_for_rank_plan_compile_rebind / incomplete_evidence_fail_closed / future_inventory_neutral | The projection values only the current transparent input backlog that the additional machine capacity can process. Gross processing gain is output sale value minus consumed input sale value; machine recipe materials are priced separately from the exact native consumption plan. Support activates only when the effective goal is exactly `goal.economy.earn_money`, demand is a current production-capacity requirement, the build window is open, every accepting output context is deterministic and fully priced, and gross gain exceeds material opportunity cost. Missing or ambiguous rows, overflow, nonpositive net value, broad Grandpa goals, and speculative future crops add zero support. Candidate, daily plan and compiler preserve and freshly recompute every economic and `goal_support_*` field; drift blocks compilation. This does not yet persist a support intent through machine placement/loading across replans. |
| `player.machine_placement` | Let the small model choose machine layout, route and time from native legal placement evidence instead of a map allowlist | `Utility.ForEachLocation(includeInteriors:true, includeGenerated:false)`, `Utility.isPlacementForbiddenHere`, `Object.canBePlacedHere`, `GameLocation.CanItemBePlacedHere`, `GameLocation.IsTileBlockedBy`, `Cask.IsValidCaskLocation`, `Utility.playerCanPlaceItemHere`, `Utility.tryToPlaceItem`, `Object.placementAction` | pending secondary comparison | covered_for_read / covered_for_current_map_exact_tile_head / covered_for_remote_location_head / covered_for_remote_static_walkability / covered_for_rolling_continuation / covered_for_plan / covered_for_compile_gate / runtime_verified_current_map / runtime_verified_one_connector_cross_location / runtime_verified_one_connector_strategic_remote_bfs_relocation | On machine/training/full profiles, every inventory-machine type and recoverable placed-machine type is probed across the current source plus player-controlled persistent locations with an existing machine cluster. Each location reports player-control classification, root/parent-building evidence, a static legal-tile count, and lossless row-compressed legal ranges. The root-level `relocation_route_reachability` field publishes row-compressed native static walkability once per relevant map; it excludes transient characters, farmers and animals, retains persistent blockers, and avoids per-machine route duplication. Static projections are fingerprint-cached over inventory, maps and persistent blockers; ownership is planning evidence, not a native placement gate. The current-map exact-tile head selects a reachable legal tile and adjacent stand. A remote placement or strategic relocation head records the first resolved transparent connector and preserves exact inventory/machine identity, target location, and typed relocation intent. Strategic relocation additionally reconstructs deterministic BFS from the connector arrival, requires an adjacent stand with a proven route that avoids the target, and records connector count/kind/estimate, arrival, target stand, exact target-route distance, explicit cost, eight-cycle benefit policy, target-selection policy and time-estimate policy. Candidates are deduplicated per target location rather than globally. Backend independently replays connector, native legality, adjacency, reachability and distance checks before removal. Every transition requires a fresh snapshot, and only the newly loaded map may rebind exact legality and stand. Runtime rechecks loaded location, BFS reachability, adjacency, machine identity, `Utility.playerCanPlaceItemHere`, native placement, exact object identity, inventory consumption, and intent completion. Hidden isolated current-map placement passed at `runtime-machine-placement-smoke-20260724-164517`; EVD-149 passed generic one-connector placement; EVD-150 passed arrival-adjacent strategic relocation; EVD-151 passed non-arrival-adjacent `Farm 56,15 -> FarmHouse 28,29`. Multi-connector runtime remains pending. Cask operational context remains strict even where raw placement is legal. |
| `farm.chests` / `current_location.chests` / `player.storage_placement` / `executor.place_storage` | Plan route-safe storage layout and execute native chest placement/use without duplicating or guessing canonical contents | `Utility.ForEachLocation(includeInteriors:true,includeGenerated:false)`; `Chest.GetItemsForPlayer`, `GetActualCapacity`, `GetMutex`, owner/global identity, flags/color; decompiled `Object.placementAction`, `Utility.playerCanPlaceItemHere/tryToPlaceItem`, `checkForAction`, `performToolAction`, `HandleChestHit`; canonical `farm.material_inventory_graph` node references | pending secondary comparison | covered_for_cross_location_read / canonical_reference_only / purpose_scoped / current_map_candidate_compile_native_executor_implemented / cross_map_and_relocation_pending / runtime_pending | `storage_infrastructure.v1` covers placed player chests and owned built-in fridges with stable access/node identity, topology, capacity, occupied/free slots, ownership, locks, flags/color, and native empty-remove/nonempty-shove status. Contents exist only in canonical graph nodes. `storage_placement.v1` classifies every vanilla inventory player-storage branch and supplies native legal ranges. Core protects graph connectivity and all existing access stands, emits movement plus `executor.place_storage`, recomputes the exact layout and ledger binding at compile time, then calls native placement and expects a matching player `Chest` plus exact inventory consumption. Remaining order: isolated runtime validation; rolling cross-map continuation; chest craft/buy and purpose/demand policy; evacuation-aware relocation; explicit shared-resource grants. |
| `farm.machines[].machine_special_state` / Mushroom Log | Price and schedule Mushroom Log capacity from the complete current nearby-tree state without manufacturing a realized random label | `Data/Machines[(BC)MushroomLog]`; `Object.OutputMushroomLog`; `Object.DayUpdate`; existing `NetWorldState.LocationWeather` | pending secondary comparison | covered_for_current_environment_marginals / held_item_exact / shared_rng_identity_blocked / runtime_pending | Scans the native 7x7 square in native order. All tree terrain features affect stack and quality; only mature trees contribute type-specific pool entries and mature moss contributes the extra quality chance. Item, stack and quality marginals each expose a probability sum; shared `Game1.random` is neither read nor advanced, so actual next identity and the joint tuple remain unavailable. Existing held output is exact, but its generation-time tree environment is not reconstructed from the current layout. |
| `farm.machines[].loadable_inputs[].predicted_output` / Geode Crusher | Produce an exact current-counter geode label without advancing either the game RNG or geode counter | `Data/Machines[(BC)182]`; `Object.OutputGeodeCrusher`; `Utility.getTreasureFromGeode`; `Stats.GeodesCracked` | pending secondary comparison | exact_for_four_vanilla_ordinary_geodes / callback_not_invoked / runtime_pending | Only base-object `(O)535`, `(O)536`, `(O)537`, and `(O)749` pass the vetted callback precondition. The predictor uses the native fresh RNG seeded from current Geodes Cracked, save ID and player ID; it does not increment stats. Mystery Boxes and custom geodes remain excluded because their branches or query handlers have different side-effect boundaries. |
| `farm.machines[].loadable_inputs[].predicted_output` / Anvil; `player.trinket_loadout`; item `special_state`; `anvil_reforge_*` plan/result fields | Plan a reforge from complete property rules, compare it with the exact current Trinket and equipped loadout, and attribute native realized utility without pretending the shared-RNG seed or unresolved long-term preference is known early | `Data/Machines[(BC)Anvil]`; `Object.OutputAnvil`; all six vanilla `TrinketEffect.GenerateRandomStats` overrides; `Farmer.stats["trinketSlots"]`; `Farmer.trinketItems`; live Trinket net fields | Stardew Wiki Frog Egg / Speed / Iridium Bar secondary comparison complete | complete_vanilla_generative_rules / exact_mechanical_utility_distribution / exact_live_loadout / broad_goal_neutral / explicit_subgoal_rule_prior / held_output_exact / runtime_feedback_complete | Requires one vetted reforgeable Trinket and three Iridium Bars. Input candidates exclude callback-level rejects that generic `probe:true` cannot detect. Output identity is the same Trinket after ten minutes. Pre-load output properties remain a complete distribution because `Game1.random.Next(9999999)` is unread. The shared Core projection computes current, expected and realized normalized mechanical utility for Iridium Spur buff duration, Parrot Egg kill-gold probability level, Fairy Box heal/interval tier, Ice Rod control uptime and Magic Quiver expected projectile DPS; Frog Egg variants are mechanically equal within type but expose their no-kill-credit/no-loot capability semantics across types. Exact unlocked/occupied/empty slots and same/other-type counts are rebound from a fresh snapshot. Candidate, plan and compiler bind utility and loadout fields; explicit subgoals add bounded rule-based affinity, while a broad Grandpa/daily goal stays at zero. Runtime records exact native outcome and realized delta as a strategy-value sample; material opportunity cost remains independent. |
| `recovery.stabilize_day` event candidates | Recovery scoring and safety fallback | `time.time`, `menus.active_menu`, `menus.sleep_prompt_context`, `current_location.home_context`, `locations.route_graph`, `locations.route_connectors`, `locations.route_gate_context`, `locations.collision_grid`, `ActionQueueCompiler.CompileRecoverySteps` | n/a | covered_for_gate / controlled_for_wait / current_home_sleep_runtime_verified / cross_map_return_runtime_verified | Late-night compile selects terminal `executor.sleep` when already home, otherwise selects one verified `executor.traverse_connector` step toward `home_location_id`. Every map transition requires a fresh snapshot and queue replan; EVD-195 closes the resulting runtime/output chain. |
| `recovery_return_home` bed preview | Bed-adjacent recovery candidate and rolling cross-map home handoff | `current_location.home_context`, `locations.collision_grid`, `locations.route_action_branch_coverage`, `locations.route_graph`, `locations.route_connectors`, `locations.route_gate_context` | n/a | covered_for_gate / controlled_for_movement / current_home_sleep_runtime_verified / cross_map_return_runtime_verified | The queue compiler, not the model, chooses the exact next connector and exports `execution_option_id`. Bed tile collision is allowed when an adjacent stand tile is reachable. Full-route duration is accumulated from fresh map snapshots rather than predicted across unloaded maps. |
| `menus.active_menu.last_question_key` | Last question key stored by the current location | `Game1.currentLocation.lastQuestionKey` | n/a | covered_for_read / runtime_verified | The compiler-owned sleep macro requires the native `Sleep` prompt before issuing `Sleep_Yes`; generic/direct prompt confirmation remains unavailable to the model. |
| `menus.active_menu.is_sleep_prompt` | Whether active menu is the sleep confirmation prompt | active `DialogueBox` plus `Game1.currentLocation.lastQuestionKey == "Sleep"` | n/a | covered_for_read / runtime_verified | Used by terminal `executor.sleep` to verify the native prompt. Standalone confirmation remains blocked outside that compiler-owned macro. |
| `menus.sleep_prompt_context` | Dedicated sleep prompt status and direct-confirmation guard | active `DialogueBox`, `Game1.currentLocation.lastQuestionKey` | n/a | covered_for_read / covered_for_gate / runtime_verified | Splits prompt-open verification from generic confirmation. `confirm_executor_enabled=false` deliberately blocks a standalone/model-selected `Sleep_Yes`; terminal `executor.sleep` owns the verified confirmation lifecycle internally. |
| `sleep_confirm_executor_disabled` | Direct sleep-confirmation hard block | `menus.sleep_prompt_context.prompt_open` | n/a | covered_for_gate / compiler_macro_only | Prevents `recovery_return_home` or a direct action from confirming an already-open prompt outside the terminal sleep macro. It does not mean `executor.sleep` is unimplemented. |
| `executor.sleep` | Terminal compiler-owned sleep macro | `current_location.home_context`, `menus.active_menu`, `menus.sleep_prompt_context`, `locations.collision_grid`, `locations.route_action_branch_coverage` | n/a | covered_for_compile / covered_for_gate / runtime_verified | One semantic `sleep` step compiles into bed-adjacent movement, walking onto `TouchAction=Sleep`, and verified `Sleep_Yes` confirmation. It must be terminal and reports success only after the date changes and the post-sleep save menu closes. |
| `sleep_action_must_be_terminal` | Sleep macro ordering guard | `compiler_context.is_terminal_step` injected by plan compiler | n/a | covered_for_gate | Blocks sleep unless it is the final plan step. |
| `player.safe_item_context` | Safe toolbar fallback after active-object actions | `Game1.player.CurrentToolIndex`, `Game1.player.Items`, `Game1.player.ActiveObject` | n/a | covered_for_read / pending_runtime_validation | Supports future `select_safe_item_slot` post-step after click/place/consume actions; policy prefers empty toolbar slot then tool slot. |
| `player.book_candidates[]` | Native inventory-book planning and execution | `Game1.player.Items`, `Object.performUseAction/readBook`, `Farmer.gainExperience`, `Stats.checkForBooksReadAchievement`, `Game1.getAchievement`, `DataLoader.Tv_CookingChannel` | Books pages pending secondary comparison | covered_for_read / covered_for_candidate / covered_for_compile / static_executor_complete / runtime_pending | Enumerates every current category `-102/-103` object. Records slot/runtime identity, one-item stack transition, original context-tag iteration order, exact six-way native branch, structured skill XP, permanent level transitions, ordered `newLevels` queue before/after, sequential Mastery XP, first/repeated book stats and `read_a_book`, Well Read ID 35 plus `hatter`/dialogue consequences, exact newly learned Queen of Sauce recipes, all native use gates, and a fail-closed custom-override status. The plan appends a 75-tick settling wait for the native one-second reading animation. |
| `options.auto_run` | Movement speed determinism | `Game1.options.autoRun`; run key inverts behavior | Options Wiki | covered_for_read / opt_in_apply | AI settings recommend `true` so executor movement uses run by default. |
| `options.stowing_mode` / `options.allow_stowing` | Prevent Use Tool from becoming stow behavior | `Game1.options.stowingMode`; `Options.allowStowing`; `Game1.pressUseToolButton` stow path | Options Wiki | covered_for_read / opt_in_apply | AI settings recommend `Off`. Active-item safety should switch safe slots explicitly, not use stow ambiguity. |
| `options.gamepad_mode` / `options.gamepad_controls` | Avoid controller/snappy input path drift | `Game1.options.gamepadMode`; `InputState` gamepad checks | Options/Controls Wiki | covered_for_read / opt_in_apply | AI settings recommend `ForceOff`. |
| `options.snappy_menus` / `options.snappy_menus_effective` | Menu cursor determinism | `Game1.options.snappyMenus`; `Game1.options.SnappyMenus` | Options Wiki | covered_for_read / opt_in_apply | AI settings recommend `false` for keyboard/mouse executor determinism. |
| `options.invert_toolbar_scroll_direction` | Toolbar cycling direction | `Game1.options.invertScrollDirection`; tool cycling over slots 0-11 | Options/Controls Wiki | covered_for_read / opt_in_apply | AI settings recommend `false`. Direct slot selection is still preferred over scroll. |
| `options.pause_when_out_of_focus` | Hidden runtime should not pause | `Game1.options.pauseWhenOutOfFocus`; pause check in `Game1` | Options Wiki | covered_for_read / opt_in_apply | AI settings recommend `false` for hidden/silent sandbox runs. |
| `player.tent_placement` | Bind one exact Tent Kit to an outdoor direction probe, exact stand, 3x2 native-clear rectangle, center anchor and live tomorrow-festival state | `Object.placementAction` `(O)TentKit`; `Utility.isFestivalDay`; passive-festival data; `GameLocation.isAreaClear`; `Tent` | n/a; locked decompile and runtime are authoritative | covered_for_read / covered_for_gate / native_runtime_verified / evaluation_only | Exact base `(O)TentKit` only. Direction rows compress legal stands; compiler independently derives probe/rectangle/anchor, checks passable rectangular layout/protected endpoints, and native runtime consumes one kit and verifies exact base Tent health/bounds/passability. |
| `current_location.large_terrain_features[].is_tent` / `sleptInTemporaryBed` | Hand off a placed exact Tent to the future terminal temporary-bed sleep chain | `Tent.performUseAction`; `GameLocation.SleepTent_Yes`; wake/reset branches | n/a; locked decompile is authoritative | covered_for_read_handoff / blocked_for_compile / blocked_for_runtime | Rows expose exact anchor/bounds/health/passability, grab geometry, live prompt gates, `SleepTent` question key, `lastTentTouchedByPlayer` and temporary-bed flag. Same-location next-day wake, save settlement and destruction receipt remain the next separate slice. |

## Machine Support Intent Addendum

EVD-169 supersedes the earlier `goal_support_*` row's final pending sentence. `strategy_commitment_ledger.v1.machine_support_intents[]` now preserves an explicitly valued machine-capacity decision across craft selection, exact same-map placement, fresh positive deterministic input loading, and later processing observation. Inventory copies of the same machine reduce unmet capacity. Additional consumed materials, nonpositive or incomplete value, remote continuation, and stochastic outputs fail closed. Compiler and dispatch reject ledger drift. A hidden/silent isolated full-lifecycle runtime smoke remains pending.

## Remaining Dictionary Gaps

| Gap | Why it matters | Current blocker |
|---|---|---|
| Cross-map route graph | Needed for arbitrary player navigation between locations | Resolve connectors, target maps, locked conditions, path stitching. |
| Full hardcoded action branch implementations | Needed for arbitrary action endpoints | Unsupported branches are audited and blocked until transparent. |
| Runtime regenerated snapshot with new route fields | Needed before model training resumes | Backend/SMAPI currently paused. |
| Purchase executor side-effect whitelist | Needed before real buying | Click row, held item, stock post-state, side effects. |
| Sell/shipping executor side-effect whitelist | Needed before real selling/shipping | Buyback, protected policy, post-state/payout. |
| Planting executor post-state | Needed before broad planting policy | Seed consumption and tile crop state are runtime-verified for one single-tile executor slice; day/growth output recording remains pending. |
| Wiki secondary coverage for shop/location/selling pages | Required by handoff rule for player-visible rules | Local decompile evidence exists; Wiki pass pending. |

## 2026-08-09 crop-maintenance projection

| Field family | Consumers | Authority | Evidence | Output and boundary |
|---|---|---|---|---|
| `current_location.crops`, `current_location.planting_context`, `current_location.resource_clumps` | candidate generator, daily planner, action compiler, executor calibration | Exact currently loaded terrain `HoeDirt`, `IndoorPot.hoeDirt`, fertilizer inventory/rule matrix, crop readiness, giant clump identity/geometry/tool slot and movement reachability from live 1.6.15 objects and local decompile | EVD-226; six hidden/silent E-drive branch smokes; Core 1575/1575; reconciliation 585/585 blocking 0 | One exact fresh candidate compiles to `executor.water_crop`, `executor.plant_seed`, `executor.apply_fertilizer`, `executor.harvest_crop`, or `executor.harvest_giant_crop`. No high-level direct runtime and no farm fallback. Five-gate calibration is closed; strategy training and custom overrides remain excluded. |
