# Transparency Coverage

| field or output id | consumer | required for training | transparent source path or reason unavailable | evidence claim id | runtime snapshot/artifact path | output recording path | status |
|---|---|---|---|---|---|---|---|
| `quests.active_quests[].runtime_type` | candidate_generator, planner | yes for per-class planning | Bridge `ProgressQuestReadAdapter.ResolveRuntimeClass` via CLR pattern match (11 subclasses + base Quest); no heuristic field patterns | EVD-080 | `quests.active_quests` snapshot field | `quest_candidate.candidate_id`, `quest_candidate.runtime_type` | bridge_wired |
| `quests.active_quests[].per_type_fields` | candidate_generator, planner | yes for exact progress/target | Bridge `ProgressQuestReadAdapter.ReadPerTypeQuestFields` reads all 11 subclass Net fields directly; base `Quest` is an explicit supported case; only unknown derived classes fail closed | EVD-080, EVD-081 | `quests.active_quests` snapshot field | `quest_candidate.required_target_*`, `quest_candidate.current_progress_count`, `quest_candidate.required_item_id` | bridge_wired |
| `quests.active_quests[].title` | candidate_generator, planner | yes for identification | Bridge reads `Quest._questTitle` public backing field directly; never calls `questTitle` getter; `title_available` indicates cache population | EVD-090 | `quests.active_quests` snapshot field | `quest_candidate.title` | bridge_wired |
| `quests.active_quests[].description` | candidate_generator, planner | yes for understanding | Bridge reads `Quest._questDescription` public backing field; never calls `questDescription` getter; `description_available` indicates cache population | EVD-090 | `quests.active_quests` snapshot field | `quest_candidate.description` | bridge_wired |
| `quests.active_quests[].current_objective` | candidate_generator, planner | yes for current step | Bridge reads `Quest._currentObjective` public backing field; never calls `currentObjective` getter | EVD-090 | `quests.active_quests` snapshot field | `quest_candidate.current_progress_count` | bridge_wired |
| `quests.active_quests[].reward_description` | candidate_generator | yes for reward transparency | Bridge reads `Quest.rewardDescription.NetString.Value` directly | EVD-081 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].show_new` | candidate_generator | yes for lifecycle | Bridge reads `Quest.showNew.NetBool.Value` directly | EVD-081 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].destroy` | candidate_generator | yes for lifecycle | Bridge reads `Quest.destroy.NetBool.Value` directly | EVD-081 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].next_quests` | candidate_generator | yes for chain awareness | Bridge reads `Quest.nextQuests.NetStringList` directly | EVD-081 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].per_type_fields.reward` | candidate_generator | yes for subclass reward | Bridge reads subclass `reward` (NetInt) for Slay/Fishing/ResourceCollection quests | EVD-081 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].per_type_fields.target_message` | candidate_generator | yes for completion dialogue | Bridge reads `targetMessage` (plain string or NetString per subclass) | EVD-081 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].quest_type == 9 next_action` | candidate_generator | yes for correct type-9 routing | `CategorizeType9NextAction` produces decompile-backed categories: `harvest_items`, `find_lost_item`, `return_lost_item_to_npc`, `find_secret_lost_item`, `return_secret_lost_item_to_npc` | EVD-080, EVD-081 | `quests.active_quests` snapshot field | `quest_candidate.next_action_category` | bridge_wired |
| `quests.special_orders[].objectives[].runtime_type` | candidate_generator, planner | yes for per-objective planning | Bridge `ProgressQuestReadAdapter.ResolveObjectiveRuntimeClass` via CLR pattern match (9 subtypes) | EVD-082 | `quests.special_orders` snapshot field | `special_order_candidate.blocked_diagnostics` | bridge_wired |
| `quests.special_orders[].objectives[].fail_on_completion` | candidate_generator, planner | yes for failure prediction | Bridge reads `OrderObjective.failOnCompletion.NetBool` directly | EVD-082 | `quests.special_orders` snapshot field | `special_order_candidate.blocked_diagnostics` when fail_on_completion objective met | bridge_wired |
| `quests.special_orders[].special_rule` | candidate_generator, planner | yes for difficulty detection | Bridge reads `SpecialOrder.specialRule.Value`; empty string `""` is valid direct value; only `null` is unavailable | EVD-083 | `quests.special_orders` snapshot field | `special_order_candidate.blocked_diagnostics` if unavailable | bridge_wired |
| `quests.special_orders[].rewards[].runtime_type` | candidate_generator, planner | yes for reward prediction | Bridge `ProgressQuestReadAdapter.ResolveRewardRuntimeClass` via CLR pattern match (6 subtypes) | EVD-083 | `quests.special_orders` snapshot field | `special_order_candidate.blocked_diagnostics` if unavailable | bridge_wired |
| `quests.special_orders[].participants` | candidate_generator | yes for multi-player awareness | Bridge reads `SpecialOrder.participants.NetLongDictionary` ordered by key | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].seen_participants` | candidate_generator | yes for participant tracking | Bridge reads `SpecialOrder.seenParticipants.NetLongDictionary` ordered by key | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].unclaimed_rewards` | candidate_generator | yes for reward claim status | Bridge reads `SpecialOrder.unclaimedRewards.NetLongDictionary` ordered by key | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].donated_items` | candidate_generator | yes for contribution evidence | Bridge reads `SpecialOrder.donatedItems` collection; `SpecialOrderDonatedItemRef` with ItemId, Stack, Quality, DonorId | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].pre_selected_items` | candidate_generator | yes for randomization | Bridge reads `SpecialOrder.preSelectedItems.NetStringDictionary` ordered by key | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].selected_random_elements` | candidate_generator | yes for randomization | Bridge reads `SpecialOrder.selectedRandomElements.NetStringDictionary<int,NetInt>` ordered by key | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].generation_seed` | candidate_generator | yes for deterministic RNG replay | Bridge reads `SpecialOrder.generationSeed.NetInt.Value` | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].ready_for_removal` | candidate_generator | yes for lifecycle | Bridge reads `SpecialOrder.readyForRemoval.NetBool.Value` | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].item_to_remove_on_end` | candidate_generator | yes for cleanup | Bridge reads `SpecialOrder.itemToRemoveOnEnd.NetString.Value` | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].mail_to_remove_on_end` | candidate_generator | yes for cleanup | Bridge reads `SpecialOrder.mailToRemoveOnEnd.NetString.Value` | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.quest_candidates[]` | option_availability, planner | yes for quest selection | `QuestCandidateBuilder` from `QuestProgressRef[]` with per-type fields, blocked diagnostics, unknown time/energy; type-9 has decompile-backed action categories | EVD-084 | `quests.active_quests` snapshot field | `availability.options[].event_candidates[].candidate_id`, `block_reasons` | bridge_wired |
| `quests.special_order_candidates[]` | option_availability, planner | yes for special-order selection | `QuestCandidateBuilder` from `SpecialOrderProgressRef[]` with single-objective index binding, objective/reward fields, blocked diagnostics, unknown time/energy | EVD-084 | `quests.special_orders` snapshot field | `availability.options[].event_candidates[].candidate_id`, `block_reasons` | bridge_wired |
| `quest.advance` option safety constraint | option_availability, planner | yes for preview-only gating | `quest_native_executor_not_implemented` hard block on all quest candidates and compiler envelope | EVD-085 | `option_registry.quest.advance.safety_constraints` | `availability.options[].blocking_reasons`, `hard_block_reasons` | bridge_wired |
| `quest.advance` compiler envelope | planner, executor | yes for handoff evidence | `QuestPlanEnvelope` with selected candidate, runtime type, next action category, required targets, `quest_native_executor_not_implemented`, unknown time/energy, live evidence from snapshot | EVD-085 | `action_queue.v1.items[].normalized_command.quest_plan` | `queue_item.normalized_command.quest_plan` | bridge_wired |
| `quest.advance` time budget | planner, executor | yes for rankable execution | `TimeBudgetValidator` returns unknown duration `quest_duration_unknown_until_route_and_native_executor_timing`; no fixed 120-tick rule | EVD-085 | `time_budget_report.items[].estimated_minutes=-1` | `time_budget_report.block_reasons` | bridge_wired |
| `type9_runtime_disambiguation` | candidate_generator | yes for correct quest routing | Bridge `ResolveRuntimeClass` uses CLR pattern match; `CategorizeType9NextAction` provides decompile-backed action categories for all three type-9 classes | EVD-080, EVD-081 | `quests.active_quests[].runtime_type` | `quest_candidate.runtime_type`, `quest_candidate.next_action_category` | bridge_wired |
| `quests.active_quests[].mod_data` | candidate_generator | yes for modded state | Bridge reads `Quest.modData.Pairs` ordered by key; deterministic `Dictionary<string,string>` | EVD-080 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].obsolete_completion_string` | candidate_generator | yes for legacy save migration | Bridge reads `Quest.obsolete_completionString` directly | EVD-080 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.active_quests[].current_objective_available` | candidate_generator | yes for cached-value visibility | Bridge reads `Quest._currentObjective` backing field; available only when non-null and non-empty | EVD-090 | `quests.active_quests` snapshot field | — | bridge_wired |
| `quests.special_orders[].donated_items[].qualified_item_id` | candidate_generator | yes for exact item identity | Bridge reads `Item.QualifiedItemId` for qualified identity alongside raw `ItemId` | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].donated_items[].mod_data` | candidate_generator | yes for modded item state | Bridge reads `Item.modData.Pairs` ordered by key; deterministic `Dictionary<string,string>` | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_orders[].donated_items[].qualified_item_id` (null-safe) | candidate_generator | yes for null entry filtering | Bridge filters `null` entries from `OnFail`-written nulls; emits zeroed ref for null entries | EVD-083 | `quests.special_orders` snapshot field | — | bridge_wired |
| `quests.special_order_candidates[].selected_objective_index` | planner, compiler | yes for stale-selection rejection | `BuildSpecialOrderCandidate` emits matched objective index; `BuildCompilerEnvelope` validates it | EVD-084 | `quests.special_orders` snapshot field | `quest_compiler.selected_objective_index` | bridge_wired |
| `quest.advance compiler envelope selected_objective_index` | planner, executor | yes for handoff evidence | `CompileQuestPlan` populates from matched candidate; cross-checks against model parameter | EVD-085 | `action_queue.v1.items[].normalized_command.quest_plan` | `queue_item.normalized_command.quest_plan.selected_objective_index` | bridge_wired |
| `quest.advance compiler envelope malformed-numeric rejection` | planner, executor | yes for safe parameter parsing | `BuildCompilerEnvelope` and `CompileQuestPlan` reject malformed target/current counts and objective index | EVD-085 | `action_queue.v1.items[].normalized_command.quest_plan` | `executor_block_reason` | bridge_wired |

No new required model input field is missing or unavailable after this slice. Broad policy training remains blocked outside the narrow runtime-calibration coverage described here.

## Bridge Wiring Status (Resolved)

All 11 ordinary quest subclasses, 9 special-order objective subtypes, and 6 reward subtypes are populated by direct Net-field reads. Base `Quest` is handled as a supported case. Three type-9 subclasses have decompile-backed next-action categories. Title/description/objective are read from public backing fields without calling loading getters. All special-order lifecycle/randomization/participant fields are emitted with deterministic ordering.

## Items 39-45 Resolution

| Item | Resolution Status | Key Change |
|------|------------------|------------|
| 39 | Resolved | `QuestProgressRef` emits `mod_data` (Dictionary) and `obsolete_completion_string` with deterministic ordering; bridge adapter reads from `Quest.modData.Pairs` and `obsolete_completionString` |
| 40 | Resolved | `TitleAvailable`/`DescriptionAvailable` fail closed for empty cached values; new `CurrentObjectiveAvailable` field emitted |
| 41 | Resolved | Pure `ReadQuestProgressMapper` extracted implementing `IQuestProgressMapper`; `ProgressQuestReadAdapter` uses it via constructor injection; direct mapper contract tests added |
| 42 | Resolved | `evidence.md` and `transparency-coverage.md` updated with mod-data, cached-value, and mapper evidence |
| 43 | Resolved | Null-safe `donatedItems` iteration; `QualifiedItemId` + `ModData` alongside raw `ItemId`; null entries produce zeroed ref; donor-ID claim removed (no direct field exists) |
| 44 | Resolved | `SelectedObjectiveIndex` added to `QuestCandidateRef`, `QuestCompilerEnvelope`, `QuestPlanEnvelope`; cross-checked in `CompileQuestPlan` and `BuildCompilerEnvelope`; tests cover binding and stale rejection |
| 45 | Resolved | `BuildCompilerEnvelope` and `CompileQuestPlan` reject malformed numeric params with explicit `_malformed:` block reasons; consistent null/empty/whitespace handling |

## Items 30-38 Resolution

| Item | Resolution Status | Key Change |
|------|------------------|------------|
| 30 | Resolved | Base `Quest` (`typeof(Quest)`) is an explicit supported case; only unknown derived classes fail closed |
| 31 | Resolved | Selected incomplete objective index is captured and used consistently for category/target/count |
| 32 | Resolved | Type-9 next-action categories are decompile-backed: `harvest_items`, `find_lost_item`, `return_lost_item_to_npc`, `find_secret_lost_item`, `return_secret_lost_item_to_npc` |
| 33 | Resolved | Empty `specialRule` (`""`) is valid; only `null` triggers `special_rule_unavailable` |
| 34 | Resolved | Participant/reward-claim mappings emitted as `Dictionary<long,bool>` with deterministic ordering; `DonatedItems` as structured array |
| 35 | Resolved | Six special-order lifecycle/randomization fields emitted: `PreSelectedItems`, `SelectedRandomElements`, `GenerationSeed`, `ReadyForRemoval`, `ItemToRemoveOnEnd`, `MailToRemoveOnEnd` |
| 36 | Resolved | Title/description/objective read from public backing fields (`_questTitle`, `_questDescription`, `_currentObjective`), not from loading getters. `TitleAvailable`/`DescriptionAvailable` indicate population |
| 37 | Resolved | `RewardDescription`, `ShowNew`, `Destroy`, `NextQuests` added to `QuestProgressRef`; subclass `Reward`/`TargetMessage` added to `PerTypeQuestFields` |
| 38 | Resolved | `CompileQuestPlan` reconstructs candidates from live snapshot, requires identity, cross-checks all parameters, rejects missing/ambiguous/mismatch, populates envelope from live evidence |
