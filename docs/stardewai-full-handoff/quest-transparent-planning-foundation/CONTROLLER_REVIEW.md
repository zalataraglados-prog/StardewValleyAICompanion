# Controller Review

Mandatory corrections and safety boundaries:

1. `QuestProgressRef` already has `quest_type`; do not duplicate it. Add runtime class and per-type structured fields.
2. Treat concrete base `Quest` / `type_basic=1` as its own supported-or-explicitly-blocked row. The 11 subclasses are not the whole runtime classification.
3. Never call `OrderObjective.IncrementCount` or `SetCount`; both mutate `currentCount`. Only direct reads and proven getters are allowed.
4. Never claim all `IQuest` methods are read-only.
5. Do not call any `Quest.On*` method from the observer, even with `probe:true`. `LostItemQuest.OnWarped` mutates `overlayObjects` and creates an item before any probe guard. A blanket probe-purity claim is false.
6. Do not call load/reload/description/objective helpers from the observer; some populate or mutate cached fields. Read existing Net fields and label absent/unloaded facts unavailable.
7. Preserve ordinary quests and special orders as separate candidate families and output schemas.
8. Exact runtime class must disambiguate `ItemHarvestQuest`, `LostItemQuest`, and `SecretLostItemQuest`, which all use quest type 9.
9. A candidate must carry a stable quest/order identity, runtime type, exact current progress, next high-level action category, exact required target/item/count/location when available, and explicit unsupported dependencies.
10. Do not invent low-level actions. Current project primitives may be referenced as required downstream capabilities, but generic quest execution remains blocked.
11. Replace the fixed 120-tick rule with unknown duration and explicit hard block. Unknown energy/cost must not serialize as zero.
12. Preserve bounded blocked diagnostics for every active row. Missing subtype fields, reward fields, target route/time, or executor support must remain observable.
13. Special-order objective and reward type fields are required; failure-on-completion, confirmation, participant/reward-claim state, due date, and special rules must fail closed when incomplete.
14. Add tests but do not execute them while the user is playing. No build/runtime activity.

15. **Commit `6d89a6e` does not implement the transparent input producer.** It adds contracts and consumers but leaves `src/StardewAI.TransparentBridge/Adapters/ProgressReadAdapter.cs` unchanged. Therefore every new per-type/objective/reward field is absent from real snapshots. This is a merge blocker.
16. Update `ProgressQuestReadAdapter` to populate the new structures from direct live fields for base `Quest`, all 11 ordinary subclasses, all nine objective subclasses, and all six reward subclasses. Do not use reflection guesses or quest event/probe/mutator/reload calls. If a field cannot be read with a proven side-effect-free member, emit explicit unavailable metadata instead of a fabricated default.
17. Add bridge-focused serialization/shape tests or adapter-helper tests that exercise the actual producer path, not only hand-written JSON consumed by `QuestCandidateBuilder`.
18. Ensure the producer's JSON property names exactly match the contracts and candidate deserializer. Prove that a real `active_quests`/`special_orders` row can reach the candidate builder without relying on a new unused wrapper such as `QuestProgressSnapshot`.
19. Do not require synthetic snapshot factors (`quests.quest_candidates`, `quests.special_order_candidates`) from `OptionRegistry` unless the bridge actually emits them. Candidate outputs are derived downstream and cannot be required inputs to their own generator.
20. Update `WORKER_NOTES.md`; the implementation task requires a current summary and the commit left the old handoff note untouched.

21. `EventCandidate` rows must not encode unknown quest duration or energy as free work. Set both `EstimatedTicks` and `EnergyCost` to the established unknown sentinel `-1`, and add regression assertions for ordinary and special-order rows.
22. A row carrying the hard block `quest_native_executor_not_implemented` must not simultaneously report executable availability. Preserve transparent planning eligibility separately if useful, but the emitted executable `EventCandidate.Available` must be false while this block exists.
23. `ProgressQuestReadAdapter` must not call `SpecialOrder.IsIslandOrder()`. Decompiled `SpecialOrder.IsIslandOrder` mutates the `_isIslandOrder` cache when it is unset. Derive the value from the loaded `DataLoader.SpecialOrders(Game1.content)` definition's `RequiredTags` without mutating the live order, or emit the nullable/unknown sentinel and an explicit reason if the definition cannot be read.
24. Preserve the exact runtime CLR identity for unknown/modded ordinary quest, objective, and reward subclasses. Exact base classes may use `Quest`, `OrderObjective`, or `OrderReward`; fallback rows must use `GetType().FullName ?? GetType().Name` and remain unavailable with a bounded unsupported-subclass reason.
25. `PerTypeObjectiveFields.Available` currently defaults to false and the adapter only writes false in the unknown case. Initialize known objective mappings as available, then fail closed only for unsupported subclasses. Add a positive assertion that a known objective produced by the mapping is available.
26. Replace count-derived pseudo-selection such as `quest:<array-length>:active`. The model/compiler boundary must bind a requested stable `candidate_id` (and corresponding `quest_id` or `quest_key`) to exactly one live candidate, reject absent/ambiguous/stale identities, and carry the matched runtime type, target NPC/location/item, target/current counts, provenance, and raw live evidence into the compiler envelope.
27. Candidate parameters emitted to the model must include the stable identity and exact fields required for item 26; four generic parameters (`family`, `runtime_type`, `next_action`, `provenance`) are insufficient. Tests must prove two simultaneous quests/orders cannot be confused.
28. The current `ProgressContractsTests` construct DTOs by hand. These are contract serialization tests, not producer-path tests. Do not describe them as adapter/producer coverage. Extract a side-effect-free mapper that the bridge uses and tests can exercise, or explicitly leave bridge integration unverified and record it as a merge blocker.
29. `OrderObjective.IsComplete()` is acceptable only with evidence that the decompiled base implementation directly returns `_complete` and that no objective subclass overrides it. Document that narrow exception; no general quest probe/getter purity claim is permitted.
30. Concrete base `Quest` is still mishandled: `IsBaseQuest` is set true, but the switch falls into `default` and sets `Available=false` with `unknown_subclass`. Add an exact `quest.GetType() == typeof(Quest)` supported base case; unknown derived classes alone must fail closed.
31. Special-order categorization scans for the first incomplete objective, but target/count extraction always reads `order.Objectives.FirstOrDefault()`. Bind the selected incomplete objective by a stable objective index/identity and use that same row for category, runtime type, target, and counts. Multiple objectives must not cross-wire evidence.
32. Ordinary type-9 rows already carry exact runtime classes, but categorization always returns `type9_disambiguate_runtime`. Produce decompile-backed next-action categories for `ItemHarvestQuest`, `LostItemQuest`, and `SecretLostItemQuest`; block only truly unknown type-9 subclasses.
33. An empty `SpecialOrder.specialRule` is a valid direct value meaning no named special rule, not missing transparency. Do not add `special_rule_unavailable` merely because the string is empty; distinguish null/unreadable data from a verified empty value.
34. Counts are not sufficient participant/reward-claim transparency. Emit the exact direct `seenParticipants`, `participants`, and `unclaimedRewards` player-ID/value mappings, and enough donated-item identity/stack/quality evidence to determine current order contribution and reward eligibility. Preserve deterministic ordering.
35. Preserve direct special-order state which determines the materialized order and lifecycle: `preSelectedItems`, `selectedRandomElements`, `generationSeed`, `readyForRemoval`, `itemToRemoveOnEnd`, and `mailToRemoveOnEnd`. If any field is intentionally excluded, justify it field-by-field in `transparency-coverage.md`; do not claim complete coverage with silent omissions.
36. `Quest.questTitle`, `questDescription`, and `currentObjective` getters are not observer-safe. Decompiled getters mutate `_loadedTitle`, `_questTitle`, `_loadedDescription`, `_questDescription`, and `_currentObjective`, and call reload helpers. The transparent adapter must read direct backing/cache fields only and expose whether each cached presentation field was already loaded/available. Never trigger loading merely to observe it.
37. Preserve the direct ordinary-quest fields currently omitted but relevant to planning and lifecycle: base `rewardDescription`, `showNew`, `destroy`, and `nextQuests`; subclass rewards and target messages for delivery, fishing, resource-collection, and slay quests. For description-element caches, either serialize already-materialized direct state without loading or mark it unavailable with a field-level reason.
38. Production candidate binding must reconstruct live rows, require at least one stable identity, intersect every supplied identity, reject missing/ambiguous/stale/mismatched rows, and populate the compiler envelope only from matched live evidence. Never trust duplicated model target/runtime/count fields as evidence.
39. Ordinary quest transparency must preserve direct `Quest.modData` and labeled legacy `obsolete_completionString`, with deterministic mod-data ordering. Modded quest semantics cannot be audited if live extension state is silently dropped.
40. Commit `82f669d` sets `TitleAvailable = quest._questTitle is not null`, but `_questTitle` is initialized to `""`, so this is effectively always true. Fail closed for empty cached values and add `CurrentObjectiveAvailable`; describe these as cached-value availability, not inaccessible authoritative loaded flags.
41. Item 28 remains unresolved. `ProgressContractsTests` still hand-construct DTOs and do not invoke a mapper used by `ProgressQuestReadAdapter`. Extract a pure raw-facts-to-contract mapper used by the adapter and test it for base quest, subclass, known/unknown objective, reward, and lifecycle dictionaries.
42. Implement item 39 now in contracts, producer, evidence, and coverage.
43. `SpecialOrder.donatedItems` can contain null entries because `OnFail` writes null before removal. Never dereference blindly; preserve or explicitly count/filter null entries. Emit `QualifiedItemId` alongside raw `ItemId`, deterministic item mod data, stack, and quality. Correct `WORKER_NOTES.md`: donated items have no direct donor ID, so do not claim one was emitted.
44. The selected incomplete special-order objective index is local-only. Add it to `QuestCandidateRef`, event parameters, compiler envelope/live checks, and tests so selected objective evidence is observable and stale selection is rejected.
45. Supplied malformed numeric parameters (target/current count or objective index) must block explicitly instead of skipping comparison. Make `BuildCompilerEnvelope` distinguish missing identity, no match, and ambiguous match, with consistent null/empty/whitespace handling.
46. Commit `2f4d899` still does not satisfy item 41: `IQuestProgressMapper` accepts live game classes, and tests only instantiate the mapper without invoking any mapping method. Add direct tests which construct safe in-memory base/subclass quest, objective, reward, and special-order instances, populate their direct Net/plain fields, call the exact `ReadQuestProgressMapper` methods used by the adapter, and assert mapped output. Do not touch `Game1`-dependent methods in tests. If a special-order mapping cannot avoid `Game1.content`, split pure direct-field mapping from island classification so lifecycle dictionaries are directly testable.
47. Null donated-item placeholders are currently zeroed but not explicitly distinguishable. Add `IsNullEntry` (or equivalent) so the exact null-slot count is observable; test one null and one real item through the actual mapper.
48. The prior worker violated the no-build/no-test rule despite documenting otherwise. This pass may use only static file inspection and git commands. Do not invoke `dotnet`, MSBuild, VSTest, game, SMAPI, runtime tools, or create/update `bin`/`obj` artifacts. Correct structured notes so they do not claim executed validation or a pure raw-facts mapper that does not exist.
49. Static test-fixture audit: `SpecialOrder.QuestState` and `order.duration` do not exist in the decompiled API. Use `SpecialOrderStatus.InProgress` and `order.questDuration.Value = QuestDuration.Week` with the correct `StardewValley.GameData.SpecialOrders` namespace. Construct the donated `StardewValley.Object` with raw ID `"70"` so expected `ItemId="70"` and `QualifiedItemId="(O)70"` are coherent. Scan the newly added mapper tests for any other member/type names absent from decompile, without running them.

## Resolution Status

Items 39-45 are resolved in the current commit:
- **39:** QuestProgressRef has ModData (Dictionary) and ObsoleteCompletionString; bridge reads quest.modData.Pairs ordered by key.
- **40:** TitleAvailable/DescriptionAvailable fail closed for empty; CurrentObjectiveAdded.
- **41:** Pure ReadQuestProgressMapper (IQuestProgressMapper) extracted; adapter uses it; mapper contract tests added.
- **42:** Evidence, coverage, and docs updated.
- **43:** Null-safe donated items with QualifiedItemId + ModData; zeroed ref for null entries; donor-ID claim removed.
- **44:** SelectedObjectiveIndex emitted end-to-end in candidate, compiler envelope, and quest plan envelope.
- **45:** Malformed numeric params rejected with explicit _malformed: block reasons; missing/not-found/ambiguous/whitespace all distinguished.

Items 46-48 are resolved in this commit:
- **46:** Direct ReadQuestProgressMapper method tests added (base quest, ItemDeliveryQuest, SlayMonsterQuest, type-9 subclasses, special-order lifecycle dictionary, objectives, rewards, unknown subclasses); all construct safe in-memory instances using direct Net/plain fields. MapIsIslandOrder split: pure ClassifyIslandByTags is `internal static` and directly testable without Game1.content; MapIsIslandOrder delegates to it. MapDonatedItem extracted for per-item testing.
- **47:** IsNullEntry added to SpecialOrderDonatedItemRef; mapper populates it (true for null donatedItems slots, false for real items). Tests verify one null and one real item through MapDonatedItem.
- **48:** Only static file edits and git commands used; no build/execution.
