# Hard Rule Transparency Audit

Training status: paused. Do not resume broad model training until every required hard-rule field has local decompile evidence and Wiki secondary confirmation.

This is an append-only historical audit. Early rows in the blocked-domain table describe the state when they were written and are not a current capability matrix. Use `FULL_SYSTEM_AUDIT_20260719_CN.md`, `transparency-coverage.md`, current source, and runtime artifacts for present status.

## Current Safe Scope

| scope | allowed before full hard-rule completion | reason |
|---|---|---|
| `executor.move_to_tile` calibration | yes | Live executor checks Stardew `GameLocation.isCollidingPosition`; rows are `executor_calibration` and excluded from policy training. |
| `executor.face_direction` calibration | yes | Compiler validates direction `0..3`; no broad game-rule dependency. |
| `executor.wait_ticks` calibration | yes | Compiler validates bounded wait `1..600`; no mutation. |
| `farm.maintain_crops` watering-candidate calibration | limited | Candidate list comes from transparent `HoeDirt.needsWatering()`; compiler now requires and records `time.season` and `time.weather`. |

## Blocked Before Training

| domain | current fields | missing hard-rule fields | training status | evidence needed |
|---|---|---|---|---|
| Crop planting and replanting | `time.season`, `time.weather`, existing `farm.crops`, inventory item ids, `farm.crop_catalog`, `player.seed_inventory`, `current_location.planting_context` including paddy maturity estimate; single-tile `executor.plant_seed` runtime smoke | planting candidate generation, layout/crop-choice planner, buying missing seeds, post-day growth/output recording tests, exception-specific executor tests | limited | Local decompile for `HoeDirt.plant`, `GameLocation.CanPlantSeedsHere`, `CropData`, crop content data; Wiki Crops/Seasons/Greenhouse/Ginger Island/Indoor Pot/fertilizer/Agriculturist/Rice Shoot/Taro Tuber. |
| Crop harvesting | crop `ready_for_harvest`, crop phase, tile, harvest method, exact single-tile compiler gate, Grab runtime primitive for one-shot and regrow after-states, Scythe single-tile runtime primitive with transparent debris output, Grab inventory-full blocked case, `farm.resource_clumps` giant-crop classification, `harvest_giant_crop_tile` candidate, dedicated axe/multi-hit giant-crop executor, `farm.debris` chunk/item transparency, `pickup_debris_item` candidate, and dedicated debris pickup executor | other special crop cases | limited | Local decompile for `HoeDirt.readyForHarvest`, `Crop.harvest`, `Crop.GetHarvestMethod`, `Farmer.couldInventoryAcceptThisItem`, `Crop.TryGrowGiantCrop`, `GiantCrop.performToolAction`, and `Debris.collect`; hidden/silent harvest smokes passed on isolated runtime for Grab seed 472, regrow seed 473, Scythe seed 273 with debris output, full-inventory Grab block, giant pumpkin clump harvest, and debris pickup `(O)388`. Giant crops stay separate from normal single-tile crop harvests; dropped item pickup uses transparent actual chunk tile, not requested source tile. |
| Shopping/buying | `time.time`, `player.money`, `locations.shops`, `current_location.shop_action_tiles`, `menus.active_menu`, `menus.shop_stock` with affordability/trade/inventory audit | route/shop-opening executor, purchase executor/menu safety, held-item/click-row post-state recording, exact legacy branch coverage | blocked | Local decompile for `ShopMenu`, shop builders/stock loaders, shop locations; Wiki shop pages. |
| Selling | inventory slots with sell/shipping/protected-item audit fields and auto-sell protection reasons, `menus.sell_context`, `farm.shipping_bins` | user-configurable protected item policy, exact shop click/active-item execution, buyback/post-state effects, end-of-day payout recording, menu executor safety | blocked | Local decompile for item sale/shipping logic, `ShopMenu`, `Object.sellToStorePrice`; Wiki shipping/shops/items. |
| Social gifting | friendships and current-location NPC positions | complete schedule resolution, NPC availability/location, birthday, gift tastes, weekly/daily gift limits, Green Rain/festival rules, item protection | blocked | Local decompile for `NPC.TryLoadSchedule`, `NPC.checkSchedule`, friendship/gift methods, gift taste data; Wiki Friendship/Gifts/NPC schedule pages. |
| Cross-location navigation | player tile/location, current map summary, warps, doors/interior doors, local `current_location.route_context` probes, compressed current-map `locations.collision_grid`, current-map `locations.route_connectors` index, current-map `locations.route_blockers` classes, current-map `locations.route_gate_context`, current-map `locations.route_action_branch_coverage` audit, loaded-location `locations.route_graph`, `locations.route_map_summaries`, scoped target/path/compiler graph blocks, current-map start-segment connector validation | later-segment per-location collision grids, fully resolved door target graph, route executor, unsupported hardcoded action branch implementations | blocked for policy/navigation | Local decompile for `GameLocation.isCollidingPosition`, `PathFindController`, warps/doors/location entry; Wiki location pages secondary. |
| Fishing | global time/season/weather, fish collection progress | fish tables by location/season/weather/time, water tile validity, rod/bait/tackle, bite timings, minigame result, energy/inventory | blocked | Local decompile for `FishingRod`, `FishingGame`, fish data selection; Wiki Fish/Fishing. |
| Mining/combat | player health/energy/inventory, progress collection | mine level/layout, ladders/elevator, monsters, ore nodes, tool/weapon, food, exit safety, health/time risk | blocked | Local decompile for `MineShaft`, monsters, tools/weapons; Wiki Mines/Skull Cavern/Mining. |
| Festivals/events | date/time/season only | festival detection, entry/exit windows, location closures/replacements, festival shops/minigames, NPC overrides | blocked | Local decompile for `Event`, festival setup/menu classes, `Utility.isFestivalDay`; Wiki Festivals. |
| Machine processing | farm machines summary with ready state, held output sale price/stack, `machine_data` rule summary from `Object.GetMachineData()`, and `loadable_inputs[]` from live `performObjectDropInAction(probe:true)` including input sale price/stack; deterministic native output probe via `MachineDataUtility.TryGetMachineOutputRule` + `MachineDataUtility.GetOutputItem(probe:true)` for output sale price/quality/preserve metadata when safe; conservative input-specific summary prediction fallback; transparent additional-consumed-item cost/reservation when all additional items are readable/priced; `collect_machine_output_tile` and `load_machine_input_tile` candidates; runtime collectors/loaders for one ready output or one verified input | broad prediction for random/custom-output rules, condition strategy, broad multi-machine scheduling, machine-specific edge cases, runtime smoke for native-probe preserve/copy-quality cases | limited | Local decompile for `Object.CheckForActionOnMachine`, `Object.performObjectDropInAction`, `Object.PlaceInMachine`, `MachineDataUtility.TryGetMachineOutputRule`, `MachineDataUtility.GetOutputItem`, `Object.salePrice`, and `ObjectDataDefinition.CreateFlavored*`; Wiki secondary confirmation from Preserves Jar, Keg, and Artisan Goods pages. Hidden/silent runtime smokes passed before native-probe field for collecting ready `(O)388` output and loading Keg input `(O)262 -> (O)346`. Native-probe runtime re-smoke is pending. |
| Animal care/products | all outdoor and animal-house animals; location, adulthood, live produce, quality, Animal Cracker multiplier, tool/slot, inventory acceptance, exact output/unit state, stat increments, energy/friendship/Farming XP; native Milk Pail and Shears executor | feeding, mood policy, petting, pregnancy/incubator/birth policy, auto-grabber interaction | limited | Local decompile verified for `FarmAnimal`, `MilkPail`, `Shears`, and `Utility.GetBestHarvestableFarmAnimal`; isolated native Milk Pail/Shears smoke passed. Remaining policy families still need local evidence and Wiki secondary checks. |

## Required Rule

Any option whose feasibility depends on a row above must remain unavailable for policy/value training until:

- The transparent field exists in snapshot state.
- The field has local decompile evidence in `evidence.md`.
- The player-facing rule has Wiki secondary confirmation where applicable.
- The action compiler or candidate generator consumes the field as a hard rule.
- Tests cover both valid and blocked cases.

## Progress Log

| date | domain | progress | remaining blocker |
|---|---|---|---|
| 2026-07-24 | Material reservation dispatch freshness | Added a backend authority check immediately before each machine-crafting runtime request. It compares the queue item's general/material ledger bindings and active reservation IDs with the current save/player ledger; stale queues send no input and trigger bounded daily-plan recompilation. | Apply this gate to every future material-consuming executor; explicit shared grants, generic placement/service, and recurring non-crop supply remain. |
| 2026-07-24 | Persistent plan-horizon material reservations | Extended the save/player strategy ledger with optimistic, auditable exact-slot reservations. Live graph identity and actor authorization gate upsert; cancellation releases supply; machine-crafting candidate and compiler bind the ledger revision and exact consumption slots. | EVD-139 closes machine-crafting dispatch freshness. Explicit shared grants, generic machine placement/service, and recurring non-crop supply remain before broad concurrent policy training. |
| 2026-07-19 | Dynamic machine scale and latest useful build window | Added factory/workshop/collection labels, complete machine capability output catalogs, exact native Raccoon request matching, current player-inventory backlog, live-crop next-harvest clock, current fleet busy capacity, deficit/required machine count, too-early defer, and full candidate/plan/compiler rebinding. | Cross-season strategy commitments, chest/Workbench stock, recurring animal/mining supply, incoming-wave steady-state throughput, generic machine placement/service runtime, and live craft/place/load smoke remain before broad policy training. |
| 2026-07-19 | Native machine crafting and storage sequencing | Added output acceptance after projected material consumption, `craft_machine_item` candidate/daily-plan/compiler rebinding, typed runtime transport, and native `CraftingPage` recipe plus inventory clicks with exact material/output and recipe-count verification. Explicitly scheduled ordinary player-chest transparency, safe layout, crafting/acquisition, placement, native transfer, purpose assignment, relocation, and Workbench connectivity after generic machine placement. | Isolated live crafting smoke remains pending. Machine placement, chest chain, Cask aging/load/value, and plan-horizon supply are not yet implemented. |
| 2026-07-18 | Animal tool-harvest transparency and executor | Added `farm.collect_animal_products` and `executor.collect_animal_product`. The bridge covers outdoor and animal-house animals and projects exact produce, quality, Animal Cracker quantity, required Milk Pail/Shears slot, inventory acceptance, stat increments, `-4` energy, `+5` friendship, and `+5` Farming XP. The compiler rebinds every value from a fresh snapshot; runtime follows moving animals and invokes the native tool lifecycle without direct state mutation. Local decompile is authoritative; Stardew Valley Wiki Animals/Milk Pail/Skills pages secondarily confirm adult production, tool assignment, `+5` friendship, `-4` Milk Pail energy, and `+5` Farming XP. | Hidden isolated smoke `artifacts\runtime-animal-product-smoke\runtime-animal-product-smoke-20260718-125259\summary.json` passed for Milk Pail and Shears. Feeding, petting, pregnancy/incubator, auto-grabber, and broader animal scheduling remain separate slices. |
| 2026-07-18 | Native panning transparency and executor | Added `foraging.pan_ore_spot` and `executor.pan_ore_spot`. The bridge invokes native `Pan.getPanItems` against a detached farmer clone while substituting and restoring `Game1.random`, so read-side previews don't consume ambient RNG. It projects the exact semantic reward multiset, aggregate inventory acceptance, Pan upgrade/enchantments, luck/day/TimesPanned seed inputs, direct item-receipt stats, Mining XP, Foraging XP, and post-use point status. The compiler rebinds these values from a fresh snapshot. Runtime walks to shore, uses `Farmer.BeginUsingTool`, and verifies all projected deltas. `Object.flipped` is normalized out of inventory identity because the decompiled constructor proves it is ambient-RNG ground-debris animation state; all other serialized unit state remains hashed. Wiki Pans/Foraging pages secondarily confirm no energy cost, upgrade behavior, Mining XP, and 7 Foraging XP per returned list item. | Hidden isolated smoke `artifacts\runtime-panning-smoke\runtime-panning-smoke-20260718-134321\summary.json` passed copper and steel Pan native lifecycles. Upgraded-Pan point respawn and generic `Farmer.OnItemReceived` quest/special-order/collection callbacks are explicitly runtime-observed, not guessed. |
| 2026-07-07 | Crop planting seed/crop catalog | Added transparent `farm.crop_catalog` sourced from `Game1.cropData (Data\Crops)`. Added `economy.buy_supplies` hard requirement for `farm.crop_catalog` so seed-buying candidates cannot bypass crop season catalog context. | Planting executor remains blocked until seed inventory candidates, plantable tile rule result, location season override, fertilizer/speed modifiers, and executor interaction are transparent and verified. |
| 2026-07-07 | Crop planting seed inventory | Added transparent `player.seed_inventory` sourced from `Game1.player.Items`, `Object.SeedsCategory`, `Crop.ResolveSeedId`, and `Game1.cropData`. Added `economy.buy_supplies` hard requirement for `player.seed_inventory`. | Planting executor remains blocked until plantable tile rule result, location season override, grow-time/fertilizer/speed modifiers, and executor interaction are transparent and verified. |
| 2026-07-07 | Crop planting current-location feasibility | Added read-only `current_location.planting_context` sourced from `GameLocation` planting APIs and `HoeDirt` tiles. It previews per-seed/per-tilled-tile `CanPlantSeedsHere`, season legality, and season-ignore/indoor-pot bypass flags. | Planting executor remains blocked until grow-time/fertilizer speed, shop stock linkage, item consumption, UI/executor interaction, and exception-specific Wiki evidence are covered. |
| 2026-07-07 | Crop planting growth timing | Extended `current_location.planting_context` with current day, days remaining in season, base grow days, Speed-Gro/Agriculturist-adjusted grow days without paddy, and conservative mature-before-season-end preview. | Planting executor remains blocked until paddy-water acceleration, shop stock linkage, item consumption, UI/executor interaction, and exception-specific Wiki evidence are covered. |
| 2026-07-07 | Buying shop stock | Added transparent `menus.shop_stock` sourced from active `ShopMenu.itemPriceAndStock` and `ItemStockInformation`. Added `economy.buy_supplies` hard requirement for `menus.shop_stock`. | Buying remains blocked until shop open hours/days, owner availability, festival closures, route/shop-opening executor, and safe purchase UI execution are transparent and verified. |
| 2026-07-07 | Buying shop access context | Added transparent `locations.shops` sourced from `DataLoader.Shops`, `ShopBuilder.GetCurrentOwners`, current-location NPC positions, and festival closure APIs. | Buying remains blocked until exact door/action rectangles, route/shop-opening executor, purchase executor/menu safety, and Wiki shop-hour confirmation are covered. |
| 2026-07-07 | Buying shop action tiles | Added transparent `current_location.shop_action_tiles` by scanning current map `Action` properties for `OpenShop` and legacy `Buy` actions. | Buying remains blocked until route/shop-opening executor, purchase executor/menu safety, exact legacy branch coverage, and Wiki shop-hour confirmation are covered. |
| 2026-07-07 | Purchase audit and paddy timing | Extended `menus.shop_stock` with affordability/trade/inventory/side-effect audit fields. Extended crop catalog/planting context with `is_paddy_crop`, paddy water eligibility, paddy speed boost, and mature-before-season-end with paddy when eligible. | Buying remains blocked until purchase executor and side-effect whitelist exist. Planting remains blocked until executor/post-day output recording tests exist. |
| 2026-07-11 | Single-tile planting executor | Added `executor.plant_seed`, compiler hard gate over `current_location.planting_context.hard_rule_allows_planting`, and isolated runtime smoke using `HoeDirt.plant`. | Broad planting policy remains blocked until candidate generation, crop/layout choice, seed acquisition, exception maps, fertilizer, and next-day growth/output recording are covered. |
| 2026-07-11 | Planting candidate and daily-plan wiring | Added `plant_seed_tile` event candidates from transparent `current_location.planting_context.hoe_dirt_tiles[].seed_results[]`, including exact tile/seed, owned-seed item id, slot index, `hard_rule_allows_planting`, and mature-before-season-end gate. `DailyPlanCompiler` emits one `plant_seed` step with `seed_id` for each accepted candidate. | This is offline-verified only in this pass; user requested no game/runtime test while playing. Broad crop/layout choice, seed purchasing, fertilizer, exception-map strategy, harvest, and next-day output labels remain blocked. |
| 2026-07-11 | Planting daily-plan reservation rules | Added plan-level reservations so only one seed candidate can claim a given planting tile, and seed usage cannot exceed transparent `player.seed_inventory` stack carried on the candidate. Planting candidates now preserve grow-day diagnostics in expected effects. | Source/tests/docs were updated only; no build, unit test, or runtime smoke was run because the user explicitly requested no testing. Broad crop/layout choice and runtime re-smoke remain pending. |
| 2026-07-11 | Planting maturity-risk ranking | Replaced the fixed planting urgency signal with a transparent timing signal derived from `adjusted_grow_days` and `days_remaining_in_season`. Daily-plan seed steps now expose `maturity_slack_days` and carry a maturity-timing safety constraint. | Source/tests/docs were updated only; no build, unit test, or runtime smoke was run because the user explicitly requested no testing. This is not an economic crop-value model; it only prioritizes legal candidates that are closer to missing the season window. |
| 2026-07-11 | Planting conservative first-harvest value signal | Extended `farm.crop_catalog` with `harvest_item_qualified_id` and `harvest_unit_sale_price`, then propagated `expected_first_harvest_value` into `plant_seed_tile`, ranker scoring, and daily-plan parameters. | Source/tests/docs were updated only; no build, unit test, or runtime smoke was run because the user explicitly requested no testing. This is a weak conservative signal for one base harvest unit only; quality, multi-yield, extra harvests, regrow cycles, seed cost, fertilizer, and end-of-day realized value remain pending. |
| 2026-07-11 | Crop harvest structure fields for planting value | Extended `farm.crop_catalog` with `harvest_min_stack`, `harvest_max_stack`, `harvest_max_increase_per_farming_level`, `extra_harvest_chance`, `harvest_min_quality`, `harvest_max_quality`, and `harvest_method`, matching fields used by decompiled `Crop.harvest`. Planting candidates and plan steps now carry these diagnostics. | Source/tests/docs were updated only; no build, unit test, or runtime smoke was run because the user explicitly requested no testing. Candidate value still uses only `harvest_min_stack * harvest_unit_sale_price`; stochastic extra harvest, quality, farming-level scaling, regrow cycles, and realized output labels remain pending. |
| 2026-07-12 | Crop catalog runtime field repair | Updated `FarmReadAdapter` crop-data reflection to read public properties or public fields. Runtime snapshot `runtime-harvest-crop-smoke-20260712-162723` verified `farm.crop_catalog` now reports populated seasons, harvest item, sale price, stack bounds, paddy/watering flags, and `HarvestMethod`; the same snapshot found 45 Grab and 5 Scythe crop entries. | Runtime verified for vanilla crop catalog field presence. Economic estimates still exclude quality multipliers, farming-level scaling, fertilizer, crop loss, and realized sale labels. |
| 2026-07-11 | Crop first-harvest estimated value diagnostics | Added a separate `estimated_first_harvest_quantity/value` beside the conservative lower bound. The estimate uses mean min/max stack plus geometric expected extra harvest chance, while preserving `expected_first_harvest_value` as the conservative min-stack value. Ranker prefers the estimate when present. | Source/tests/docs were updated only; no build, unit test, or runtime smoke was run because the user explicitly requested no testing. Estimate excludes quality price multipliers, farming-level max-stack scaling, regrow cycles, seed cost, fertilizer, and realized output labels, and must not be used as ground truth. |
| 2026-07-11 | Crop regrow season-value diagnostics | Added transparent regrow-season diagnostics for `plant_seed_tile`: `regrow_days`, estimated regrow/total harvest counts, conservative/estimated season harvest values, and season net values with seed cost subtracted once. Ranker now prefers season net value before first-harvest value, and daily-plan parameters carry the same diagnostics. | Source/tests/docs were updated only; no build, unit test, or runtime smoke was run because the user explicitly disabled testing. The season value is still an estimate from transparent timing/catalog fields, not a realized output label; quality multipliers, farming-level scaling, fertilizer, crop loss, and sale timing remain unverified. |
| 2026-07-12 | Crop harvest candidates and compiler gate | Added `harvest_crop_tile` candidates under `farm.maintain_crops` when transparent crop state says `ready_for_harvest=true`. Registered `executor.harvest_crop` and added action-queue compiler validation for exact target tile, clear menu, target location, and transparent ready crop. Daily plan now emits `harvest_crop` steps. | Offline tests pass. Candidate/ranker/compiler path is enabled for exact ready crop tiles. |
| 2026-07-12 | Crop harvest runtime smokes | RuntimeTestHarness supports `executor.harvest_crop` and `debug.setup_harvest_crop_target`. The executor calls decompile-confirmed `Crop.harvest(...)`; for non-regrow crops it follows HoeDirt's branch by clearing the crop with `destroyCrop(false)`. Hidden/silent runtime smokes verified mature `(O)472` Grab, `(O)473` regrow Grab, and `(O)273` Scythe crops at `64,15`; all returned `applied/verified` and changed state hash. Scythe verification requires transparent target debris creation. A full-inventory Grab smoke returned `blocked` with `harvest_crop_inventory_cannot_accept_grab_yield` and left the crop ready. | Verified for single-tile Grab one-shot, Grab regrow, Scythe one-shot with debris output, and Grab full-inventory blocked behavior. Giant crop handling is covered separately by `executor.harvest_giant_crop`. |
| 2026-07-12 | Giant crop transparency and executor | Decompile confirmed giant crops are created by `Crop.TryGrowGiantCrop` as `GiantCrop : ResourceClump`, not normal `HoeDirt` crop entries. `GiantCrop.performToolAction` accepts Axe hits, reduces clump health, and emits debris only after health reaches zero. `farm.resource_clumps` classifies these with `is_giant_crop`, `giant_crop_id`, `required_tool=axe`, and `executor_status`; `farm.maintain_crops` emits `harvest_giant_crop_tile`, daily plan emits `harvest_giant_crop`, and RuntimeTestHarness resolves numeric crop id `276` to giant crop key `Pumpkin` for fixture setup. | Hidden/silent runtime smoke `artifacts\runtime-giant-crop-smoke\runtime-giant-crop-smoke-20260712-182314\summary.json` passed: setup verified, `executor.harvest_giant_crop` applied/verified, `swings=2`, clump removed, debris `0 -> 28`, state hash changed. |
| 2026-07-12 | Debris pickup transparency and executor | Decompile confirmed `Debris.collect(Farmer, Chunk)` transfers debris items/resources into inventory and fails when inventory cannot accept them. `farm.debris` now exposes item ids, quality, chunk count, bounce timing, sink/essential flags, and per-chunk pixel/tile positions. `farm.maintain_crops` emits `pickup_debris_item`; daily plan moves to the transparent chunk tile and emits `pickup_debris`; compiler gates target tile, transparent debris, menu state, target location, and inventory acceptance. | Hidden/silent runtime smoke `artifacts\runtime-pickup-debris-smoke\runtime-pickup-debris-smoke-20260712-185126\summary.json` passed: fixture placed `(O)388`, transparent debris tile resolved to `64,16`, `executor.pickup_debris` applied/verified, debris `1 -> 0`, inventory updated, state hash changed. |
| 2026-07-12 | Ready machine output collection | Decompile confirmed `Object.CheckForActionOnMachine` collects `heldObject` when `readyForHarvest=true`, adds it to inventory, clears `heldObject`, and resets `readyForHarvest`. `farm.process_machines` now emits `collect_machine_output_tile`; daily plan emits move-to-adjacent plus `collect_machine_output`; compiler gates exact transparent machine tile, ready state, held output, menu state, target location, requested output id, and inventory acceptance. | Hidden/silent runtime smoke `artifacts\runtime-machine-output-smoke\runtime-machine-output-smoke-20260712-194224\summary.json` passed: fixture produced ready `(O)388`, executor applied/verified, machine held item cleared, inventory updated, state hash changed. Machine input/loading recipes remain blocked. |
| 2026-07-12 | Single machine input loading | Decompile confirmed `Object.performObjectDropInAction` delegates machine input to `PlaceInMachine`, which uses `MachineDataUtility.TryGetMachineOutputRule` and supports `probe:true`. `farm.machines[]` now includes idle machines with `loadable_inputs[]` generated by live `performObjectDropInAction(probe:true)` over current inventory. `farm.process_machines` emits `load_machine_input_tile`; daily plan emits move-to-adjacent plus `load_machine_input`; compiler gates exact machine tile, non-busy state, transparent probe input slot, menu state, target location, and requested item id. | Hidden/silent runtime smoke `artifacts\runtime-machine-input-smoke\runtime-machine-input-smoke-20260712-200049\summary.json` passed: Keg accepted `(O)262` from transparent slot 10, `executor.load_machine_input` applied/verified, machine started with `held_item=(O)346`, `minutes_until_ready=1750`, inventory updated, state hash changed. Broad machine recipe/value scheduling remains blocked. |
| 2026-07-12 | Machine value diagnostics | Machine held outputs and loadable inputs now expose transparent `sale_price`/stack. Machine output candidates carry `output_stack`, `output_sale_price`, `output_total_value`, and `machine_value_basis=held_item_sale_price_times_stack`; input candidates carry `input_stack_available`, `input_sale_price`, `machine_input_opportunity_cost`, and `machine_input_value_basis=transparent_input_sale_price_output_unknown`. Daily-plan parameters preserve these fields, the baseline ranker gives ready output a transparent value signal while applying a small opportunity-cost penalty to input loading, and daily-plan reservation prevents two machine-input candidates from consuming the same transparent inventory slot beyond its stack. | Unit/solution tests pass: Core 207, Backend 36. This is an auditable heuristic only; it does not predict machine output value before loading, solve recipes, schedule multiple machines, or authorize broad machine strategy training. |
| 2026-07-12 | MachineData rule summary and conservative exact-rule prediction | `farm.machines[].machine_data` now exposes read-only `Object.GetMachineData()` summary fields: `has_input`, `has_output`, `use_first_valid_output`, `output_rule_count`, and capped output-rule summaries with ids, conditions, required item ids, output item ids/stack/quality/price hints, required item counts, `additional_consumed_items[]`, and minutes until ready where readable. `load_machine_input_tile` candidates preserve `machine_output_rule_count` and `machine_has_output_rule`; when a summarized rule has exact `required_item_id` match, no condition, no preserve/copy-quality/copy-color pricing, a readable `output_item`, and any additional consumed items are fully readable/priced, candidates emit `machine_output_prediction_status=machine_data_exact_required_item_match`, predicted output id/stack/sale price/price source/total value/minutes, `machine_additional_consumed_total_value`, and `predicted_output_net_value`. `copy_price=true` is priced from the transparent input `sale_price`. | Unit/solution tests pass: Core 211, Backend 36; bridge build passes. Conditional rules are explicitly left unpredicted with `machine_data_exact_required_item_match_condition_not_evaluated`; unpriced additional consumption remains blocked. This is conservative exact-rule prediction only. It still does not execute `TryGetMachineOutputRule` as a full predictor, evaluate rule conditions, compute preserve/copy-quality/copy-color pricing, random outputs, or solve multi-machine strategy. |
| 2026-07-12 | Machine additional-consumed resource reservation | Exact-rule machine predictions now keep transparent `machine_additional_consumed_items` and `machine_additional_consumed_available` summaries alongside `machine_additional_consumed_total_value`; daily planning preserves those fields as action parameters and reserves additional-consumed stacks in addition to the main machine input slot. If two accepted machine candidates would exceed the transparent available stack for an additional consumed item, the later candidate is skipped with `daily_plan_machine_additional_consumed_stack_already_reserved`. | Unit/solution tests pass: Core 212, Backend 36. This prevents false training plans where several machines consume the same hidden extra ingredient. It is still conservative: rules with unpriced or unreadable additional consumption remain unpredicted rather than guessed. |
| 2026-07-12 | Machine derived-pricing block status split | Exact-rule machine prediction no longer collapses `copy_quality`, `copy_color`, and preserve-derived output prices into one generic unavailable status. It now emits `machine_data_exact_required_item_match_copy_quality_not_priced`, `machine_data_exact_required_item_match_copy_color_not_priced`, or `machine_data_exact_required_item_match_preserve_not_priced` respectively. | Unit/solution tests pass: Core 215, Backend 36. This is a transparency improvement only; those derived pricing formulas remain blocked until implemented from decompile/wiki evidence. |
| 2026-07-12 | Machine native output probe | `farm.machines[].loadable_inputs[]` now carries `predicted_output` when the bridge can safely call the game-native `MachineDataUtility.GetOutputItem(probe:true)` path for a deterministic output. Core prefers that native output over hand-written summary formulas and emits `machine_output_prediction_status=machine_native_probe_available`, `predicted_output_price_source=machine_native_probe_sale_price`, native sale price/stack, preserve metadata, matched rule id, additional-consumed cost, and net value. Random multi-output and custom output methods are blocked rather than probed. | Unit/solution tests pass: Core 216, Backend 36; bridge build passes. This removes the need to manually reproduce preserve/copy-quality/copy-color pricing for deterministic native-probe cases, but runtime re-smoke is still required before broad training treats the new field as fully runtime-covered. |
| 2026-07-13 | Machine native probe rule/time alignment | Native probe output now records rule/effective minutes until ready. Candidate prediction reads `effective_minutes_until_ready` first, then override/rule fallback, and emits `predicted_minutes_until_ready`. Additional-consumed cost lookup now uses `predicted_output.matched_rule_id` before falling back to required-item matching, preventing same-input multi-rule cost drift. | Not tested in this pass because the user was playing. This is a wiring refinement over EVD-065 and must be covered by the next offline test run plus native-probe runtime smoke. |
| 2026-07-13 | Machine snapshot safety correction | Runtime testing showed HTTP snapshot reads time out when machine input/native output probes are performed inside the read adapter. `profile=machine` now narrows farm reads to minimal machine rows; `farm.machines[].loadable_inputs` is intentionally empty with a blocked status; `Invoke-RuntimeMachineInputSmoke.ps1` uses the main-thread harness setup result's `input_slot_index` instead of snapshot loadable inputs. | Runtime smoke `artifacts\runtime-machine-input-smoke\runtime-machine-input-smoke-20260713-030848\summary.json` passed: setup/load verified, machine held `(O)346`, `minutes_until_ready=1750`, state hash changed. Solution tests pass: Core 216, Backend 36. Machine loadable/native output probe must be reintroduced as a main-thread command/cache before training uses it as transparent input. |
| 2026-07-18 | Cross-location machine identity and rolling route | `farm.machines[]` now enumerates every machine in the player's Farm, assigned FarmHouse, and assigned Cellar without row truncation. Every row carries live location identity and probe status. Expensive current-map probes rotate over at most 16 eligible idle manual-input machines per refresh. Same-map candidates use only same-location occupancy and the current collision grid; remote candidates execute one resolved connector, preserve exact machine continuation, and require a fresh snapshot. Compiler and runtime both reject location drift. | Static source/decompile/diff inspection only by user instruction. Build, tests, deployment, game launch, and runtime smoke remain pending. Demand/throughput and route-cost strategy remain blocked rather than guessed. |
| 2026-07-13 | Machine probe main-thread cache | The EVD-066 safety boundary is preserved by keeping HTTP requests out of live machine probing. `FarmReadAdapter.RefreshMachineProbeCache()` now runs on SMAPI `UpdateTicked`, probes capped machine rows on the game thread, records `loadable_inputs[]` from `performObjectDropInAction(probe:true)`, and records deterministic native output prediction from `MachineDataUtility.GetOutputItem(probe:true)`. `profile=machine` serves the cached rows to training. | Hidden/silent E runtime smoke `artifacts\runtime-machine-input-smoke\runtime-machine-input-smoke-20260713-105444\summary.json` passed with `loadable_input_before=true`, native predicted output `(O)262 -> (O)346`, source `MachineDataUtility.GetOutputItem(probe:true)`, sale price 400, matched rule `Default_Wheat`, effective minutes 1750, load applied/verified, and state hash changed. Solution tests pass: Core 216, Backend 36. Random/custom output and broad multi-machine scheduling remain blocked. |
| 2026-07-13 | Machine daily-plan input boundary | Added `profile=training_machine` for backend-ingestable machine snapshots without the heavy full profile. The runtime harness debug fixture refreshes the transparent bridge machine cache after setup/load. `LiveTrainingLoop` can ingest the machine snapshot, produce a daily plan, and compile a queue containing `executor.load_machine_input`; `--continue-after-blocked-queue-items` records blocked earlier candidates and continues to later independent queue items. | Hidden/silent E smoke `artifacts\runtime-machine-daily-plan-smoke\runtime-machine-daily-plan-smoke-20260713-124008\summary.json` passed: one feature row written, queue contained `executor.load_machine_input`, all 12 queue items were attempted with blocked-item continuation, and the compiled load item executed through the real runtime harness with `status=applied`, `primitive_verification_status=verified`, and `qualified_item_id=(O)262`. This is skip/continue, not full replanning. |
| 2026-07-11 | Seed cost and first-harvest net value diagnostics | Planting candidates now read transparent seed unit cost from active `menus.shop_stock` and `locations.shops.stock_preview` when available, then emit conservative and estimated first-harvest net values. Ranker prefers net value when present. | Unit/build verification added in this pass. Net value still depends on available transparent shop price; if no seed price is visible, no cost/net fields are guessed. Regrow cycles, quality, fertilizer, and realized sale labels remain pending. |
| 2026-07-07 | Purchase executor explicit block | Added `executor_purchase_enabled=false` and per-item executor block reasons to `menus.shop_stock`. | Buying remains blocked until click-row coordinates, held-item handling, side-effect whitelist, synchronized stock post-state, and executor tests exist. |
| 2026-07-07 | Selling inventory audit | Extended `player.inventory` items with category, max stack, `canBeShipped`, `canBeTrashed`, `sellToStorePrice(-1L)`, and `salePrice()`. | Selling/shipping remains blocked until shop sell filters, shipping-bin location/range, protected item policy, buyback effects, and end-of-day payout recording are covered. |
| 2026-07-07 | Selling shop/shipping context | Added `menus.sell_context`, `farm.shipping_bins`, and required both for `economy.sell_items`. | Selling/shipping remains blocked until exact click/active item execution, protected item policy, buyback/post-state effects, and end-of-day payout recording are covered. |
| 2026-07-07 | Protected item audit | Extended `player.inventory` rows with runtime type, `specialItem`, object type, quest item, big craftable, furniture, and wallpaper flags. | Selling/shipping remains blocked until user-protected item policy, exact execution, buyback/post-state effects, and end-of-day payout recording are covered. |
| 2026-07-07 | Auto-sell protection reasons | Added `protected_from_auto_sell` and `auto_sell_protection_reasons` to inventory rows, using conservative reasons like special item, quest item, cannot trash, furniture, wallpaper, big craftable, and non-positive price. | Selling/shipping remains blocked until user-configurable protected policy, exact execution, buyback/post-state effects, and end-of-day payout recording are covered. |
| 2026-07-07 | Local route collision probes | Added `current_location.route_context` with radius-6 farmer collision probes around the player, using Stardew `GameLocation.isCollidingPosition`, plus action/warp hints. | Cross-map routing remains blocked until full collision grid, dynamic blocker classes, door/warp graph, route executor, and tests are covered. |
| 2026-07-07 | Door connector transparency | Added `current_location.doors` and `current_location.interior_doors` with door tile, target/action hints, and interior door open state. | Cross-map/indoor routing remains blocked until destination resolution, locked-door conditions, building indoor targets, route executor, and tests are covered. |
| 2026-07-07 | Compressed collision grid | Added `locations.collision_grid` with current-map dimensions and blocked/action/warp/door/interior-door tiles, using Stardew `GameLocation.isCollidingPosition` and farmer-size tile rectangles. | Cross-map routing remains blocked until dynamic blocker classes, resolved connector graph, route executor, and tests are covered. |
| 2026-07-07 | Route connector index | Added `locations.route_connectors` with normalized current-map warps, doors, interior doors, action warps, and shop endpoints plus resolved/unresolved counts. | Cross-map routing remains blocked until conditional locks, building targets, full graph resolution, route executor, and tests are covered. |
| 2026-07-07 | Route blocker classes | Added `locations.route_blockers` with current-map collision participants grouped by characters, objects, terrain features, large terrain features, resource clumps, and furniture plus bounding boxes. | Movement candidates must still be checked through Stardew `isCollidingPosition`; route executor and cross-map planner remain blocked. |
| 2026-07-07 | Route gate context | Added `locations.route_gate_context` with map `BuildConditions`, `LockedDoorWarp`, `ConditionalDoor`, `Warp`, `Door`, festival closure, Town Key, time/friendship gates, Green Rain override, and mail-gated touch warp preview. | Route/shop-opening training remains blocked until full graph resolution, remaining hardcoded action branches, executor, and tests are covered. |
| 2026-07-07 | Route action branch coverage | Added `locations.route_action_branch_coverage` to scan current-map `Action`/`TouchAction` branches and mark route-transparent branches vs unsupported branches that must block training. | Unsupported hardcoded action branches remain blocked until separately transparent and tested. |
| 2026-07-07 | Route unsupported-branch compiler gate | Added `locations.route_action_branch_coverage` to `exploration.visit_location` required factors and scoped compiler hard block `unsupported_route_action_branch_at_target`. | Unsupported branches elsewhere on the map do not block unrelated target tiles. Route executor and path-level unsupported branch blocking remain blocked until a resolved route path exists. |
| 2026-07-07 | Current-map route path gate | Added compiler-side current-map BFS over transparent collision grid. It first searches for a path avoiding unsupported action branches, only blocking with `unsupported_route_action_branch_on_path` if the target is reachable only through unsupported branch tiles. | Current-map only; cross-map connector graph, route executor, and runtime path tests remain blocked. |
| 2026-07-07 | Loaded-location route graph preview | Added `locations.route_graph` over `Game1.locations` with warp/action-warp/locked-door/shop endpoint edges and resolved/unresolved status. Added compiler cross-map gate `route_graph_no_resolved_path` when a different `target_location` has no resolved graph path. | Graph is read-side preview only; route executor, exact door target resolution, and runtime graph validation remain blocked. |
| 2026-07-07 | Cross-map start-segment gate | Added compiler validation for the first route graph edge: player must be able to reach the current-map connector tile through `locations.collision_grid` while avoiding unsupported branch tiles. | Only current-map start segment is validated; later map segments require per-location collision grids before full route stitching can be trusted. |
| 2026-07-07 | Per-location route summaries | Added `locations.route_map_summaries` for loaded maps with dimensions, connector/action counts, unsupported branch counts, and segment validation status. Added normalized route preview parameters for target-location segment status. | This is audit/visibility only; full later-segment path validation remains pending. |
| 2026-07-07 | Interact primitive compiler contract | Added narrow `executor.interact` option and compiler gate for adjacent transparent `map_action` targets with expected action branch match and unsupported-branch/menu blocks. | Runtime interaction remains disabled until side-effect whitelist, hook-wrapped checkAction execution, and post-state verification are implemented. |
| 2026-07-07 | Runtime primitive verification contract | Added explicit primitive verification fields to `TrainingExecutionResult`, copied them into plan-execution episodes, and added calibration-only LiveTrainingLoop features for after-snapshot freshness, state-hash change, tick delta, primitive kind, and verification status. Runtime harness now records requested vs observed effects for move/face/wait/crop/blocked results. Hidden/silent runtime artifacts were regenerated under `artifacts\hidden-training-primitive-verification-*`. | Runtime `executor.interact` remains unsupported. Broad policy/value training remains blocked; regenerated rows are executor-calibration only. |
| 2026-07-07 | Candidate availability before model scoring | Added `option_availability.v1`, `CandidateOptionAvailabilityEvaluator`, `/api/v1/planner/options/availability`, and state-hash-aware rank filtering so hard-rule required-field gaps and preview/executor-disabled options are mapped before the small model ranks choices. | First pass covers required-field availability and explicit preview/executor-disabled gates. Parameter-bound route/interact gates remain compiler second-pass until extracted into a shared hard-rule evaluator. |
| 2026-07-07 | Parameter-bound candidate availability | Extended `option_availability.v1` with `candidates[]` carrying `SmallModelActionParameter[]`. Candidate availability now uses a non-executing synthetic `ActionQueueCompiler` dry-run probe to surface parameter-bound route/interact block reasons before model scoring. | Runtime route/interact executors remain disabled. Economic item-level value gates still need a dedicated candidate pass. |
| 2026-07-07 | Economic value candidate gates | Added option-level value gates for `economy.buy_supplies` and `economy.sell_items`: buy requires at least one buyable, in-stock, affordable, inventory-acceptable shop entry; sell requires at least one unprotected positive-value shop-sell or shipping candidate with sell context/bin availability. | Still option-level, not item-level candidate enumeration. Purchase, sell, and shipping executors remain disabled. |
| 2026-07-07 | Item-level economic candidates | Added `economic_candidates[]` to option availability for concrete buy and sell/shipping scoring candidates with item id, slot, price/value, availability, and candidate-local block reasons. | Candidate scoring only. Purchase, sell, and shipping executors remain disabled. |
| 2026-07-07 | Event-level economic ranking | Added `ranked_event_candidates[]` to availability-aware prediction and baseline `EventCandidateRanker` so concrete item/slot candidates can be ranked by candidate id. | Baseline heuristic only; not a trained event model. Purchase, sell, and shipping executors remain disabled. |
| 2026-07-18 | Fish-pond output and population-request native slice | `farm.buildings[].fish_pond` now exposes exact vanilla runtime identity, output/request branch priority, all legal pond-edge target/stand pairs, post-inventory output unit state, inventory and safe-slot gates, request item toolbar bindings, interception risks, population-gate before/after state, and decompile-derived Fishing XP. The read path uses `FishPond.GetRawData` plus a side-effect-free `SpawnTime=-1` derivation instead of calling cache-mutating `GetFishPondData`; temporary item cloning cannot advance the live RNG. `fishing.service_fish_ponds` produces separate output/request candidates; the compiler rebinds every typed field. The executor walks with the shared tile controller and invokes `GameLocation.checkAction -> FishPond.doAction`; it never assigns output, request, population, inventory, or XP state. | Hidden/silent isolated E runtime PASS after the read-side audit: `artifacts/runtime-fish-pond-smoke/runtime-fish-pond-smoke-20260718-142221/summary.json`. Output case collected `(O)812`, inventory `0 -> 1`, Fishing XP `378 -> 389`; request case consumed `(O)72`, needed count `1 -> -1`, maximum occupants `1 -> 3`, Fishing XP `389 -> 429`. Both primitive verifications passed and the process exited. Local decompile anchors: `FishPond.cs:288-455`; wiki cross-check: Fish Pond produce/request lifecycle. |
| 2026-07-19 | Machine location topology and placement evidence correction | Replaced the Farm/FarmHouse/Cellar scope with `Utility.ForEachLocation(includeInteriors:true, includeGenerated:false)` plus live root/parent ownership classification. Existing machine capacity now includes Sheds, Cabins, Greenhouse/farm roots, animal-building interiors and player-owned persistent-world machines. Inventory machines expose native static legal tile ranges per persistent location; transient characters/farmers stay out of layout legality and are rebound by exact `Utility.playerCanPlaceItemHere` after route. No map name is an allowlist. Cask operation remains restricted by `Cask.IsValidCaskLocation`. | Bridge build PASS; focused machine tests PASS 8/8; full solution PASS Core 1022 and Backend 62. Runtime placement remains pending. |
