# Evidence

| claim id | claim | affected files | local decompile evidence | runtime artifact | confidence | unresolved gaps |
|---|---|---|---|---|---|---|
| EVD-078 | The first mining floor execution slice is native and training-auditable. `MiningFloorStepPlanner` consumes only the transparent loaded-floor snapshot, decodes compact collision rows, and uses deterministic BFS to select an already reachable ladder, a required kill-all monster, or a reachable stone, preferring an exact ladder-producing stone. Internal `executor.mine_stone` walks collision-safely, uses `Farmer.BeginUsingTool`/`EndUsingTool`, never calls `Pickaxe.DoFunction` or removes an object directly, and records native swing count plus the health sequence. This primitive is not exposed to the small model and does not remove the honest `mining.reach_depth` block. | `src/StardewAI.Core/Execution/MiningFloorStepPlanner.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts/Invoke-RuntimeMiningSnapshotSmoke.ps1`; `tests/StardewAI.Core.Tests/MiningFloorStepPlannerTests.cs`; `docs/stardewai-full-handoff/mining-transparent-planning-foundation/*` | Uses EVD-077's `Pickaxe.cs:131-169`, `Object.cs:920-943,1121-1138,6082-6089`, `GameLocation.cs:2898-2931,7326-7415`, and `MineShaft.cs:1956-2024,3614-3637` boundaries. Runtime mutation is only the normal player tool lifecycle. | Hidden/silent isolated E: PASS `artifacts/runtime-mining-snapshot-smoke/runtime-mining-snapshot-smoke-20260715-203940/summary.json`: level 99, target `(4,6)`, health `8 -> 4 -> 0`, two native GoldPickaxe swings, stone absent in the after snapshot, 46 ticks, and a purpose-limited snapshot at 155 ms / 248,761 bytes. Full solution PASS Core 858 / Backend 49; focused planner/source tests PASS 7/7. | high for transparent per-floor selection and one native stone lifecycle | Native combat, ladder/shaft interaction, descent confirmation, resource/retreat policy, multi-floor loop, and end-to-end `mining.reach_depth` training remain blocked. |
| EVD-077 | Loaded MineShaft transparency no longer marks required planning facts unavailable: compact collision rows come from read-only loaded-map/occupant checks; breakable stones expose live durability, best-pickaxe damage/hits, and exact local seeded ladder previews; `BreakableContainer` and private treasure-room net state are read by exact runtime type/field; current monster facts require dynamic after-snapshot replanning instead of guessed future AI. A purpose-limited `profile=mining` prevents unrelated domain scans and uses the existing 30-tick profile cache. | `src/StardewAI.TransparentBridge/Adapters/MiningReadAdapter.cs`; `src/StardewAI.TransparentBridge/ModEntry.cs`; `scripts/Invoke-RuntimeMiningSnapshotSmoke.ps1`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tests/StardewAI.Core.Tests/MiningReachDepthPlanningTests.cs`; `docs/stardewai-full-handoff/mining-transparent-planning-foundation/*` | `GameLocation.cs:2898-2931,7326-7415`; `Object.cs:920-943,1121-1138,6082-6089`; `Pickaxe.cs:131-169`; `BreakableContainer.cs:14-25,33-77`; `MineShaft.cs:124,395,1974-1981,3614-3637`. | Hidden/silent isolated E: PASS `artifacts/runtime-mining-snapshot-smoke/runtime-mining-snapshot-smoke-20260715-201518/summary.json`: native entry to level 99, 50x50 collision grid, 118 objects including 79 breakable stones and 17 containers, 7 monsters, all 79 stone rows carrying durability and ladder preview; five snapshots were 127-164 ms and 232,759 bytes. The first profile version failed at 2.63 MB/3036 ms because generic `locations` duplicated floor data; upstream profile exclusion reduced the payload and passed without relaxing the 3000 ms gate. | high for decompiled field/formula mapping, non-empty runtime serialization, and purpose-limited snapshot performance | Perfect mining navigation/combat/descent execution remains blocked. Monster-drop ladder consumes live global RNG and is observed after execution rather than predicted. |
| EVD-076 | The ordinary native social chain is runtime-closed: global NPC transparency and current-location candidate priority feed production ranking/daily-plan/queue compilation; talk opens a native DialogueBox, safe ordinary dialogue advances through SMAPI MouseLeft input, and gift executes only through `Game1.currentLocation.checkAction`. Known no-spouse state is exported as available empty instead of unavailable, and the 64-row social cap preserves available/current-location candidates ahead of remote diagnostics. | `src/StardewAI.TransparentBridge/Adapters/NpcReadAdapter.cs`; `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs`; `src/StardewAI.TransparentBridge/Adapters/MenuReadAdapter.cs`; `src/StardewAI.Core/OptionRegistry/SocialCandidateBuilder.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `scripts/Invoke-RuntimeNativeSocialSmoke.ps1` | `Utility.cs:348-391` global location enumeration; `Farmer.cs:1532-1550` spouse getter returns null for known empty `netSpouse`; `DialogueBox.receiveLeftClick` advances typing/pages and closes ordinary dialogue after transition/safety gates; `GameLocation.cs:7639-7678` dispatches `checkAction` to NPC handling; `NPC.cs:2357-2415` applies gift legality, counters, item decrement, friendship and dialogue. | Hidden/silent isolated E: PASS `artifacts/runtime-native-social-smoke/runtime-native-social-smoke-20260715-183304/summary.json`. Talk Pierre applied/verified; dialogue closed `DialogueBox -> none` with one native press; gift slot 10 `(O)388` applied/verified, stack `4 -> 3`, gifts today/week `0 -> 1`, friendship `66 -> 46`. All three loops wrote one verified training row and episode. | high for ordinary talk, safe ordinary dialogue close, and stacked ordinary gift | One-item-to-null gift, blocked/replan runtime cases, duration calibration, question/event dialogue, and modded/overridden NPC paths remain unproven or fail closed. |
| EVD-075 | Vanilla special-fishing priority now has isolated E: runtime acceptance for active fish frenzy, occupied FishPond, and MineShaft lava-area non-fish output. Fixtures establish only native prerequisites: frenzy fish/point/start time, a safely placed completed pond with one legal fish, or asynchronous native entry to mine level 100. The catch still uses the normal rod state machine; no fixture calls `getFish`, `CatchFish`, injects inventory, mutates BobberBar progress, or preselects the random mine result. BobberBar control now uses a bounded relative-speed input controller and real executor calls have a separate 180-second HTTP budget. | `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `scripts/Invoke-RuntimeFishingDailyPlanSmoke.ps1`; `tests/StardewAI.Backend.Tests/RuntimeCatchFishExecutorTests.cs` | `GameLocation.cs:13663-13704` uses private `fishSplashPointTime` to expire splashes/frenzies; `GameLocation.cs:13859-13874` orders FishPond, frenzy, location data, and fallback; `FishPond.cs:881-888` decrements occupants in native `CatchFish`; `FishPond.cs:959-969` defines pond fishable tiles; `BobberBar.cs:417-457` reads input and applies bar acceleration; `MineShaft.cs:1163-1228` selects lava eel, cave jelly, or trash 167-172; `Game1.cs:10566-10576` enters generated mines asynchronously through native warp. | Hidden/silent isolated E: PASS fish frenzy `artifacts/runtime-fishing-frenzy-smoke/runtime-fishing-frenzy-smoke-20260713103210/summary.json` observed `(O)128` with BobberBar and singleton distribution; PASS FishPond `artifacts/runtime-fishing-pond-smoke/runtime-fishing-pond-smoke-20260713104128/summary.json` used native-safe placement `(70,18)`, no BobberBar, occupant count `1 -> 0`, observed `(O)128`; PASS mine area 80 `artifacts/runtime-fishing-mine-area80-smoke/runtime-fishing-mine-area80-smoke-20260713104810/summary.json` entered `UndergroundMine100`, compiled exactly lava eel, cave jelly, and trash 167-172, then observed native `(O)168` without BobberBar. All three JSONL rows preserve the canonical distribution and observed output. Solution tests PASS Core 261 / Backend 42; TransparentBridge, RuntimeTestHarness, and LiveTrainingLoop Release builds PASS with 0 warnings/errors. | high for frenzy, pond, mine distribution completeness, and mine non-fish branch | The random mine lava-eel branch has not yet been observed in its own area-80 runtime run. Farm redirect, volcano/other vanilla overrides, and unknown modded resolver/override runtime paths remain pending or fail closed. |
| EVD-074 | One native cast is one mechanical model action, not one candidate per possible fish. The candidate preserves the complete transparent result set and leaves the expected item unconstrained; compiler and runtime fail closed on incomplete/tampered distributions, while the actual caught item becomes the label. | `src/StardewAI.Core/OptionRegistry/FishingEventCandidateBuilder.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts/Invoke-RuntimeFishingDailyPlanSmoke.ps1`; `tests/StardewAI.Core.Tests/FishingMainlineTests.cs`; `tests/StardewAI.Backend.Tests/RuntimeCatchFishExecutorTests.cs` | `FishingRod.cs:491` passes bait, water depth, player and bobber tile to `location.getFish` but no rule/output selector. `GameLocation.cs:13849` exposes the same inputs; `GameLocation.cs:13859-13874` applies fish pond/frenzy/location-data/fallback priority. `GameLocation.cs:13940-14006` combines and precedence-orders rules, rolls eligible rules, resolves outputs, and returns the first valid result. | Hidden/silent isolated E: PASS `artifacts/runtime-fishing-daily-plan-smoke/runtime-fishing-daily-plan-smoke-20260713-174813/summary.json`: expected item empty, distribution complete, 17 possible ids, observed `(O)152` belongs to the compiled distribution, runtime verification includes `observed_outcome_in_compiled_distribution`, execution `applied/verified`; the JSONL row contains the normalized distribution parameter and observed catch fact. Core 261 and Backend 42 tests pass; three Release builds have 0 warnings/errors. | high for ordinary Beach aggregate action and output labeling | Exact composed probabilities remain previews because sequential rule failures, equal-precedence randomization and item-query randomness are not represented as one normalized probability mass. Special runtime coverage is tracked separately in EVD-075. |
| EVD-073 | `executor.catch_fish` is a bounded legal-input runtime state machine. It validates exact location, stand/bobber tiles, rod slot, rule key, energy, menu and rod state; uses native cast timing/release, drives BobberBar only through `Game1.oldMouseState`, and records the actual caught qualified item rather than treating a stochastic candidate as a promise. The move request has an independent `max_movement_tiles` budget derived from transparent BFS distance. | `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts/Deploy-RuntimeTestHarnessToRuntime.ps1`; `scripts/Invoke-RuntimeFishingDailyPlanSmoke.ps1`; `tests/StardewAI.Backend.Tests/RuntimeCatchFishExecutorTests.cs` | `FishingRod.cs:1645-1715` enters timing cast; `FishingRod.cs:1874-2044` polls release and advances the cast; `FishingRod.cs:595-650` shows the legal junk/special transition `isFishing=false`, `pullingOutOfWater=true`; `FishingRod.cs:1086-1095` reaches the fish-hold state. `BobberBar.cs:417-423` reads use-tool input and `BobberBar.cs:525-603` advances catch progress. `GameLocation.isCollidingPosition(... pathfinding:true)` is used consistently by transparent collision data and runtime BFS. No direct `getFish`, item injection, catch-progress mutation, teleport, or OS input injection is used. | Hidden/silent isolated E: runtime PASS: `artifacts/runtime-fishing-daily-plan-smoke/runtime-fishing-daily-plan-smoke-20260713-170624/summary.json`. Four legal route segments reached Beach, runtime moved `(38,0) -> (44,25)`, cast to `(44,29)`, completed BobberBar, observed and inventoried `(O)152`, consumed stamina `338 -> 330.1`, and wrote one training row. Execution was `applied/verified`; the compiled `(O)167` candidate mismatch was recorded as false rather than relabeled. Core 260 and Backend 41 tests pass; three Release builds pass. | high for ordinary Beach fishing path | Fish-pond, deterministic no-BobberBar, mine/volcano, Farm redirect and modded resolver/override paths still need separate runtime coverage. Unknown third-party resolvers and active unprojected Farm redirection remain fail-closed blockers. |
| EVD-072 | Rod-specific transparent fishing state now reaches pre-model candidate filtering, ranking, daily-plan expansion, queue validation, and the atomic catch executor contract. Each candidate binds a reachable stand tile, fishable bobber tile, actual inventory rod slot, rule or special-source key, cast direction/range, water depth, and stochastic possible output. Unknown item resolvers, unknown/modded location overrides, and the unresolved active Farm target-location distribution block upstream instead of emitting guessed candidates. | `src/StardewAI.TransparentBridge/Adapters/FishingReadAdapter.cs`; `src/StardewAI.Core/OptionRegistry/FishingEventCandidateBuilder.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/FishingMainlineTests.cs`; `docs/fishing-transparency-readiness-slice.md` | `GameLocation.cs:13849-13874` gives catch-source priority: fish ponds, active fish frenzy, location data, then trash fallback. `GameLocation.cs:13901-14186` applies location-rule, item-query, catch-limit, fish-data, time, weather, level, water-depth, bait, tackle, luck, and quantity stages. `MineShaft.cs:1163-1232` applies Curiosity Lure to the special-fish factor, then branches to special fish, cave jelly/trash in area 80, or `UndergroundMine` location rules; a training rod goes directly to mine trash. `IslandSouthEast.cs:273-288` returns null from the already-collected Stardrop Pool instead of falling through. `FishingRod.cs:357-369` defines added cast distance; `FishingRod.cs:1950-2039` applies cardinal cast geometry. `Farm.cs` `FarmFishLocationOverride` can redirect to another location distribution based on bobber coordinates and chance, so an active unprojected target distribution is a hard blocker. | Offline validation: Core tests PASS 259 including eight fishing mainline cases; TransparentBridge build PASS with 0 warnings/errors. No game process was launched. | high for read, gate, compile, and queue validation; medium for special-source completeness until runtime smoke | Normal rules, deterministic vanilla overrides, mine special fish/jelly/trash branches, fish ponds, fish frenzy, tutorial fallback, and trash fallback are projected. Queue validation rechecks higher-priority source exclusion. Arbitrary registered item-query resolvers remain intentionally unexecuted on the read path. The active Farm redirected target distribution is explicitly incomplete and blocks rather than silently omitting outputs. |
| EVD-071 | Controller Review 2 correction: stale after snapshots now stop blocked daily-plan continuation instead of falling through to the old queue; fresh blocked daily-plan replans filter regenerated items by stable semantic identity; persisted execution results carry the effective queue id, effective queue item, effective before state hash/path, and effective before snapshot so training rows and episodes bind to the actual executed regenerated item/state. Machine stand-tile selection uses existing route preview/path validation (`exploration.visit_location`) when collision-grid route data is available, with the previous movement probe retained only as a lightweight fallback. | `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tools/StardewAI.LiveTrainingLoop/QueueReplanFilter.cs`; `tests/StardewAI.Core.Tests/LiveTrainingLoopQueueReplanFilterTests.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `docs/stardewai-full-handoff/*`; `WORKER_NOTES.md`; `transparency-coverage.md` | No new gameplay rule. Reuses ActionQueueCompiler route path preview over transparent `locations.collision_grid` and `locations.route_action_branch_coverage`; replan corrections are output-recording/training-data integrity logic over transparent before/after snapshots and compiled queue items. | Deterministic validation: `dotnet test tests\StardewAI.Core.Tests\StardewAI.Core.Tests.csproj --no-restore --filter "CandidateOptionAvailabilityEvaluatorTests|LiveTrainingLoopQueueReplanFilterTests"` PASS 62; `dotnet build tools\StardewAI.LiveTrainingLoop\StardewAI.LiveTrainingLoop.csproj --no-restore` PASS 0 warnings/errors. Added decision tests for blocked+fresh, blocked+stale, continuable, continue-disabled, and non-daily-plan cases; added route-preview test where nearest unoccupied stand tile is unreachable and another adjacent stand tile is selected. | high for deterministic C1/C2 and route-preview B behavior | Hidden/silent runtime smoke for the corrected blocked replan branch is still pending. |
| EVD-070 | Controller rework correction: machine output/input stand-tile availability now excludes every `farm.machines[]` occupied tile and uses a movement compiler probe before a stand tile can enter machine event candidates. Output and input candidates share the same selector and become unavailable upstream with explicit stand-tile block reasons when no adjacent tile survives machine occupancy, collision-grid, and compiler-probe checks. Queue replanning now triggers only after a blocked/non-continuable item when `--continue-after-blocked-queue-items` and daily-plan mode are active, requires a fresh after snapshot, preserves the original planned item count, records replan provenance, and filters regenerated queues by stable semantic item identity instead of random `queue_item_id`. | `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tools/StardewAI.LiveTrainingLoop/QueueReplanFilter.cs`; `tests/StardewAI.Core.Tests/LiveTrainingLoopQueueReplanFilterTests.cs`; `docs/stardewai-full-handoff/*`; `WORKER_NOTES.md`; `transparency-coverage.md` | Same gameplay evidence as EVD-065/EVD-069. This pass corrects planning/availability integration, not a new Stardew machine rule: transparent `farm.machines[]` supplies occupied machine tiles; movement reachability is checked through the existing compiler probe path; regenerated queue filtering uses normalized command parameters/effects as stable semantics. | Offline validation: `dotnet test tests\StardewAI.Core.Tests\StardewAI.Core.Tests.csproj --no-restore --filter CandidateOptionAvailabilityEvaluatorTests` PASS 54; full Core tests PASS 220 after adding `LiveTrainingLoopQueueReplanFilterTests`; `dotnet build tools\StardewAI.LiveTrainingLoop\StardewAI.LiveTrainingLoop.csproj --no-restore` PASS 0 warnings/errors. | high for B upstream availability and C deterministic no-duplicate filtering; medium until a hidden/silent runtime smoke covers the new blocked-only replan branch | Runtime smoke for corrected C replan branch still pending. Movement compiler probe currently ignores only missing-required-state so lightweight unit snapshots can still test collision/machine occupancy; real training snapshots should provide complete movement fields. Broad machine scheduling and broad training remain blocked. |
| EVD-069 | The refreshed full-handoff slice restores the accepted sleep edge fixes, improves machine candidate ordering, and upgrades daily-plan queue execution from blocked-item skip/continue to after-snapshot replanning. `executor.sleep` now prefers a reachable bed-adjacent stand tile that is not the player's current tile when possible and stops movement if Stardew opens the native Sleep `DialogueBox` while stepping onto the sleep touch tile. Machine input candidates with transparent predicted net value can outrank lower-value ready-output collection, while unknown input remains below known output. `LiveTrainingLoop` ingests each runtime after snapshot and, for daily-plan execution, recompiles the remaining queue against the fresh state hash with explicit replan artifacts and a max-attempt cap. | `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `tests/StardewAI.Core.Tests/EventCandidateRankerTests.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `docs/stardewai-full-handoff/*`; `WORKER_NOTES.md` | Sleep uses the same decompile boundary as EVD-056: `GameLocation.performTouchAction("Sleep", ...)` requires actual movement/`Game1.player.hasMoved`, and the native sleep prompt is a `DialogueBox` whose `Sleep_Yes` response starts sleep. Machine loading/collection uses the same EVD-065/EVD-068 boundaries: `Object.performObjectDropInAction(probe:true)` and `MachineDataUtility.GetOutputItem(... probe:true)` provide transparent native input/output evidence; this pass changes ranking and queue control, not new Stardew machine rules. Queue replanning is integration logic over already-ingested transparent after snapshots, not a new gameplay rule. | Hidden/silent E sleep smoke PASS: `artifacts\runtime-sleep-edge-a-summary.json`, run `train.20260713051929.9d61f935`, `execution_status=applied`, `primitive_kind=sleep`, `primitive_verification_status=verified`, reasons `sleep_yes_confirmed`, `new_day_observed`, `post_sleep_menu_closed`, day `11 -> 12`, `state_hash_changed=true`, after menu `none`. Offline validation: `dotnet test tests\StardewAI.Core.Tests\StardewAI.Core.Tests.csproj --no-restore --filter EventCandidateRankerTests` PASS 18; full Core tests PASS 217; `dotnet build tools\StardewAI.LiveTrainingLoop\StardewAI.LiveTrainingLoop.csproj --no-restore` PASS 0 warnings/errors; `dotnet test StardewValleyAICompanion.sln --no-restore` PASS Core 217, Backend 36. | high for sleep edge and offline queue/ranking integration; medium until the new after-snapshot replan path receives a runtime smoke | C replan logic has build/solution-test coverage and emits artifacts, but this pass did not rerun the machine daily-plan smoke after adding replanning. Broad machine scheduling, random/custom machine outputs, generic interaction, cross-map route strategy, sell/shipping, mining, fishing, social, and broad training remain blocked. |
| EVD-068 | Machine input candidates now survive the formal training-planning boundary and queue-level blocked-item continuation: a `training_machine` snapshot profile provides backend-ingestable machine transparency, `LiveTrainingLoop` can build a daily plan and compiled action queue containing `executor.load_machine_input`, and `--continue-after-blocked-queue-items` lets later independent queue items execute even when earlier candidates block. The runtime harness debug fixture refreshes the bridge machine cache after setup/load so hidden-window tests do not depend on focus/tick timing. | `src/StardewAI.TransparentBridge/ModEntry.cs`; `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `scripts/Invoke-RuntimeMachineDailyPlanSmoke.ps1`; `docs/stardewai-full-handoff/*` | Same gameplay evidence as EVD-065/EVD-067. This pass adds integration evidence rather than new Stardew rules: transparent `loadable_inputs[].predicted_output` is consumed by candidate generation, preserved through `DailyPlanCompiler`, compiled by `ActionQueueCompiler`, and executed through `RuntimeTestHarness` using `Object.performObjectDropInAction`. | Hidden/silent E runtime smoke PASS: `artifacts\runtime-machine-daily-plan-smoke\runtime-machine-daily-plan-smoke-20260713-124008\summary.json`. It used `profile=training_machine`, verified target `(O)262` loadable input/native prediction in snapshot, ran `LiveTrainingLoop` with `farm.process_machines`, wrote one training feature row, compiled a pending queue with `executor.load_machine_input`, attempted all 12 queue items with blocked-item continuation, and reached the compiled load item. Result for the load item: `status=applied`, `primitive_verification_status=verified`, reasons `machine_input_loaded`, `machine_processing_started_or_output_ready`, `inventory_updated`, `qualified_item_id=(O)262`. `dotnet test StardewValleyAICompanion.sln --no-restore` PASS with Core 216, Backend 36. | high for single-machine input planning boundary and blocked-item queue continuation | This is not full replanning: blocked earlier queue items are recorded and skipped/continued, not repaired. Broad multi-machine scheduling, stale-output cleanup ordering, and smart queue reordering remain pending before broad autonomous machine strategy training. |
| EVD-067 | Machine input/native output transparency is reintroduced through a main-thread cache instead of HTTP read-path probing. `FarmReadAdapter.RefreshMachineProbeCache()` runs on SMAPI `UpdateTicked`, caches capped machine rows, `loadable_inputs[]` from `Object.performObjectDropInAction(probe:true)`, and deterministic `predicted_output` from `MachineDataUtility.GetOutputItem(... probe:true)`. `profile=machine` serves the cache, so HTTP snapshots avoid live machine probes while training still receives transparent input/output fields. | `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`; `src/StardewAI.TransparentBridge/ModEntry.cs`; `scripts/Invoke-RuntimeMachineInputSmoke.ps1`; `docs/stardewai-full-handoff/*` | Same decompile boundary as EVD-065: `Object.performObjectDropInAction(probe:true)` checks whether an inventory item can enter a machine, `MachineDataUtility.TryGetMachineOutputRule(...)` selects the matching native rule, and `MachineDataUtility.GetOutputItem(... probe:true)` produces the native output item without applying the live load. EVD-066 remains valid as the reason this must not run inside the HTTP request thread. | `dotnet build src\StardewAI.TransparentBridge\StardewAI.TransparentBridge.csproj --no-restore` PASS with 0 warnings/errors. `dotnet test StardewValleyAICompanion.sln --no-restore` PASS with Core 216, Backend 36. Hidden/silent E runtime smoke PASS: `artifacts\runtime-machine-input-smoke\runtime-machine-input-smoke-20260713-105444\summary.json`; `loadable_input_before=true`, native predicted output `status=available`, source `MachineDataUtility.GetOutputItem(probe:true)`, `(O)262 -> (O)346`, sale price 400, matched rule `Default_Wheat`, effective minutes 1750, load applied/verified, state hash changed. | high for deterministic single-input runtime path | Cache is capped to 64 machine rows and 16 inventory slots per machine. Random multi-output and custom output methods remain explicitly blocked. Broad multi-machine/day strategy still needs downstream planning and more recipe coverage smokes before formal training. |
| EVD-066 | Machine read-side runtime safety correction: HTTP snapshot reads must not run machine input/native output probes. `profile=machine` now narrows farm reads to minimal machine rows, `farm.machines[].loadable_inputs` is intentionally empty with `loadable_input_probe_status=blocked_*`, and the runtime machine input smoke uses the test harness setup result's `input_slot_index` instead of snapshot `loadable_inputs`. | `src/StardewAI.TransparentBridge/State/SnapshotProfileContext.cs`; `src/StardewAI.TransparentBridge/ModEntry.cs`; `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`; `scripts/Invoke-RuntimeMachineInputSmoke.ps1`; `docs/stardewai-full-handoff/*` | Local decompile evidence from EVD-065 remains valid for the game-native machine output path, but runtime testing showed that placing those probes on the HTTP read path caused snapshot requests to time out. The safe architecture is to keep read snapshots non-mutating/lightweight and move expensive/native machine probes to a main-thread runtime command. | `dotnet build src\StardewAI.TransparentBridge\StardewAI.TransparentBridge.csproj --no-restore` PASS with 0 warnings/errors. `powershell -ExecutionPolicy Bypass -File scripts\Invoke-RuntimeMachineInputSmoke.ps1 -StartupTimeoutSeconds 240` PASS: `artifacts\runtime-machine-input-smoke\runtime-machine-input-smoke-20260713-030848\summary.json`, setup/load verified, `machine_minutes_after=1750`, `machine_held_after=(O)346`, state hash changed. `dotnet test StardewValleyAICompanion.sln --no-restore` PASS with Core 216, Backend 36. | high for runtime safety correction | Core still contains native-probe candidate support for future `predicted_output.status=available`, but snapshot currently does not provide it. Next implementation must add a main-thread command or cached main-thread collector for loadable input/native output probes before training can rely on machine input candidates from transparent read state. |
| EVD-065 | Machine input candidates can now consume a transparent native output probe for deterministic machine outputs: `farm.machines[].loadable_inputs[].predicted_output` is produced by the bridge using the game's own `MachineDataUtility.GetOutputItem(... probe:true)` path after `TryGetMachineOutputRule`, and Core uses that native `salePrice()` instead of hand-written preserve/copy-quality/copy-color formulas when the probe reports `status=available`. The bridge also records matched rule id plus rule/effective minutes until ready; Core uses the matched rule id to align additional-consumed item costs with the exact native rule. | `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `docs/stardewai-full-handoff/*` | Local decompile: `MachineDataUtility.TryGetMachineOutputRule(...)` selects the matching output rule; `MachineDataUtility.GetOutputItem(...)` applies `CopyColor`, `CopyQuality`, `CopyPrice`, `PreserveType`, `PreserveId`, price modifiers, and output item fields; `Object.salePrice()` applies game sale-price rules including quality/profit modifiers; `ObjectDataDefinition.CreateFlavoredWine/Jelly/Pickle/AgedRoe/SmokedFish/...` creates preserve-derived output objects. Wiki secondary confirmation: Preserves Jar ignores ingredient quality and uses base ingredient formulas for Jelly/Pickles/Roe; Keg uses fruit/vegetable base-value formulas; Artisan Goods notes most artisan goods ignore input quality except cases such as Ostrich Egg Mayonnaise and Smoked Fish. | Offline validation from the previous pass: `dotnet test tests\StardewAI.Core.Tests\StardewAI.Core.Tests.csproj --no-restore` PASS with Core 216; `dotnet test StardewValleyAICompanion.sln --no-restore` PASS with Core 216 and Backend 36. The 2026-07-13 matched-rule/minutes refinement was not tested because the user was playing. No game runtime smoke was run for native probe yet. | high for compile/candidate wiring, medium until runtime native-probe snapshot is captured | Probe is intentionally conservative: random multi-output rules and custom output methods are blocked rather than probed. Runtime re-smoke should verify the new `predicted_output` field appears in an actual SMAPI snapshot for preserve/copy-quality cases before formal training treats those values as fully covered. |
| EVD-064 | Planting is now exposed as an upstream event-candidate slice only when transparent `current_location.planting_context` approves the exact tile/seed and the maturity-time gate is not negative. `DailyPlanCompiler` converts `plant_seed_tile` into one `plant_seed` step carrying `seed_id`, optional qualified id, and the transparent target tile. | `src/StardewAI.Contracts/Options/OptionContracts.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tests/StardewAI.Core.Tests/DailyPlanCompilerTests.cs` | Reuses EVD-063 decompile boundary for `HoeDirt.plant`, `GameLocation.CanPlantSeedsHere`, season legality, and transparent planting context. This pass did not add new Stardew rule evidence; it wires already-read fields into candidate and plan layers. | No runtime/game smoke was run in this pass because the user was actively playing. Offline validation only: Core filtered tests `CandidateOptionAvailabilityEvaluatorTests|EventCandidateRankerTests|DailyPlanCompilerTests|ActionQueueCompilerTests` PASS with 126 tests; `dotnet build StardewValleyAICompanion.sln --no-restore` PASS with 0 warnings/errors. | high for compile/candidate wiring, medium until runtime re-smoke | Still only single-tile owned-seed planting. Crop layout choice, buy-missing-seeds, fertilizer choice, harvest, exception-map policy, next-day growth/value labels, and visible/runtime regression remain pending. |
| EVD-063 | A minimal single-tile planting executor is runtime-verified in the isolated harness: transparent `current_location.planting_context` must approve the target seed/tile, `executor.plant_seed` calls Stardew's `HoeDirt.plant(seedId, farmer, false)`, consumes one inventory seed, and verifies a crop appears on the target tile. | `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts/Invoke-RuntimePlantSeedSmoke.ps1`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `HoeDirt.cs:536-616` implements `plant`, resolving seed id, checking crop data, location plant rules, `CanPlantSeedsHere`, season legality, season bypasses, paddy auto-water, stats, and speed increases. The compiler only allows `executor.plant_seed` when the transparent planting context has `hard_rule_allows_planting=true` for the exact tile and seed. | Hidden/silent isolated E: plant seed smoke PASS: `artifacts\runtime-plant-seed-smoke\runtime-plant-seed-smoke-20260711-194339\summary.json`. The fixture set an isolated Farm tile to spring-legal, empty HoeDirt with `(O)472` seed available; transparent context reported `planting_context_allowed_before=true`; runtime reported `plant_status=applied`, `plant_verification=verified`, reasons `target_tile_crop_created` and `seed_stack_decreased`, and bridge state hash changed. Core ActionQueueCompilerTests PASS with 63 tests, BackendEndpointTests PASS with 22 tests, solution build 0 warnings/errors. | high | This is a narrow executor-calibration slice. It does not yet expose planning candidates for planting, choose crop layouts, buy missing seeds, apply fertilizer, handle Greenhouse/Ginger Island/Indoor Pot exceptions as training policy, harvest, or record next-day growth/value outcomes. The spring-season setup is an explicit non-training fixture and must not be treated as a game action. |
| EVD-062 | The shop purchase chain is runtime-verified for a safe single-item buy: transparent route connectors reach a shop, whitelisted shop interaction opens the menu, dialogue-shop response reaches `ShopMenu` when required, `menus.shop_stock` selects an executor-enabled item, `executor.buy_shop_item` verifies the purchase, and `executor.close_menu` cleans up the menu. `LiveTrainingLoop` now executes all pending queue items sequentially instead of only `queue.items[0]`, so compiled `OpenShop -> buy_shop_item -> close_menu` queues can reach the purchase and cleanup steps. | `scripts/Invoke-RuntimeRouteToShopSmoke.ps1`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs`; `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `tests/StardewAI.Core.Tests/DailyPlanCompilerTests.cs` | Uses existing shop evidence: `GameLocation.performAction(...)` for whitelisted `OpenShop`/shop dialogue branches, active `ShopMenu.itemPriceAndStock`, `ShopMenu.safetyTimer`, `ShopMenu.ShopId`, `ItemStockInformation`, and `Game1.exitActiveMenu()`/`ShopMenu.readyToClose()` for cleanup. Runtime purchase remains limited to stock entries marked `executor_purchase_enabled=true` by transparent affordability, inventory acceptance, currency, stock, held-item, read-only, callback, and side-effect gates. | Hidden/silent isolated E: route-to-shop purchase smoke PASS: `artifacts\runtime-route-to-shop-smoke\runtime-route-to-shop-smoke-20260711-192207\summary.json`. It traversed `FarmHouse -> Farm -> BusStop -> Town -> Blacksmith`, opened Blacksmith, applied the dialogue shop response, bought `(O)378` copper ore for 150g, reported `buy_status=applied`, `buy_verification=verified`, then closed the menu with `close_menu_status=applied`, `close_menu_verification=verified`, and `final_menu_closed=true`. Focused tests PASS: Core filtered shop/planner/compiler set 73 tests, BackendEndpointTests 22 tests, solution build 0 warnings/errors. | high | This verifies one safe money-currency purchase path in an isolated harness. Broad shop behavior remains constrained: tool upgrades, callback-heavy purchases, trade currency, limited/synced stock, selling, buyback, and non-whitelisted interaction branches still require separate gates and smokes before policy training may rely on them. |
| EVD-061 | Ranked timeline candidates now have a concrete planner output boundary: `DailyPlanCompiler` converts supported candidates into `small_model_plan.v1`, while unsupported domains such as mining/fishing are not emitted as fake executable steps. | `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs`; `src/StardewAI.Backend/Program.cs`; `tests/StardewAI.Core.Tests/DailyPlanCompilerTests.cs`; `tests/StardewAI.Backend.Tests/BackendEndpointTests.cs`; `docs/full-chain-task-planning-roadmap.md` | No new gameplay rule. This is a compiler boundary over existing candidate evidence. It preserves executor honesty by only emitting step kinds already understood by `ActionQueueCompiler`. | `dotnet test tests\StardewAI.Core.Tests\StardewAI.Core.Tests.csproj --no-restore --filter "DailyPlanCompilerTests|EventCandidateRankerTests|ActionQueueCompilerTests"` PASS with 61 tests. `dotnet test tests\StardewAI.Backend.Tests\StardewAI.Backend.Tests.csproj --no-restore --filter BackendEndpointTests` PASS with 21 tests. | high | Next gap is task-level compiler support for farm maintenance/planting, mining, fishing, social, and menu-heavy shop flows. The endpoint can compile supported slices but does not make missing executors complete. |
| EVD-060 | Candidate time windows must survive into ranked event output and be scheduled before execution: same-day shop openings become deferred timeline candidates, while closed-for-day or finish-after-close candidates are excluded upstream. | `src/StardewAI.Contracts/Options/OptionContracts.cs`; `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `src/StardewAI.Core/Training/EventCandidateTimelineScheduler.cs`; `tests/StardewAI.Core.Tests/EventCandidateRankerTests.cs`; `docs/candidate-availability-gating-policy.md` | No new gameplay rule. Reuses EVD-059 transparent `service_time_status` and snapshot `time.time`: `allowed_now`, `allowed_today`, `effective_open_time`, `next_open_time`, `close_time`, and wait-cost fields are read before model scoring. | `dotnet test tests\StardewAI.Core.Tests\StardewAI.Core.Tests.csproj --no-restore --filter "CandidateOptionAvailabilityEvaluatorTests|ActionQueueCompilerTests|EventCandidateRankerTests|TimeBudgetValidatorTests"` PASS with 93 tests. `dotnet test tests\StardewAI.Backend.Tests\StardewAI.Backend.Tests.csproj --no-restore --filter BackendEndpointTests` PASS with 20 tests. | high | This is rank-time scheduling only. Full multi-step day planner still needs integration with queue compilation and TimeBudgetValidator before formal policy training. |
| EVD-059 | Shop endpoint availability is an upstream candidate gate, not a downstream executor failure: owner-at-counter status, service time/door status, and route gate legality are read transparently and unavailable endpoints are excluded before model scoring or route traversal. | `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `scripts/Invoke-RuntimeRouteToShopSmoke.ps1`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `docs/candidate-availability-gating-policy.md` | `LockedDoorWarp` entrance data supplies open/close time, Town Key, friendship, festival, green-rain, and special closures. Shop action endpoints supply `OpenShop`/`Buy` target data. NPC owner/service areas are resolved from current location/NPC state. The candidate layer consumes `owner_service_status` and `service_time_status`; the smoke harness also checks `locations.route_gate_context.action_gates[].allowed_now` before direct traversal. | Core availability tests PASS for owner-present, owner-away, and service-time-blocked interact endpoints. Runtime route-to-shop artifacts: `runtime-route-to-shop-smoke-20260711-115629` Blacksmith passed with entrance-derived 900-1600 gate; `runtime-route-to-shop-smoke-20260711-115721` Carpenter/ScienceHouse filtered because Robin was not at the counter; `runtime-route-to-shop-smoke-20260711-120559` SeedShop endpoint filtered on Wednesday closure; `runtime-route-to-shop-smoke-20260711-120737` SeedShop route stopped at Town with `skip_route_gate_blocked`; `runtime-route-to-shop-smoke-20260711-120828` Blacksmith still passed after route-gate filtering. | high | Purchase side-effect whitelist remains separate. Shop endpoints are controlled only for whitelisted action/menu opening; generic interactions and equipment purchases remain blocked until separately modeled and tested. |
| EVD-056 | `executor.sleep` is implemented as a terminal runtime macro in the training harness: path to a verified bed-adjacent stand tile, step onto the bed `TouchAction=Sleep` tile, confirm `Sleep_Yes`, require a year/season/day transition, and only report success after the post-sleep save menu closes. | `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts/Invoke-RuntimeSleepSmoke.ps1`; `scripts/Invoke-RecoveryRuntimeSmoke.ps1`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `GameLocation.performTouchAction("Sleep", ...)` is the bed touch path used by the game and requires `Game1.player.hasMoved`; the harness now performs an actual bed-tile step before triggering it. `Sleep_Yes` is the dialogue action key that starts sleep. The harness keeps this as a terminal macro and blocks if an active menu is already open, if the current location is not home, if the bed tile is unverified, if the stand tile/path is unavailable, or if the post-sleep menu does not close. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 121 and Backend 34. `dotnet build "tools\StardewAI.RuntimeTestHarness\StardewAI.RuntimeTestHarness.csproj"` PASS with 0 warnings and 0 errors. Hidden/silent E: isolated runtime smoke PASS: `artifacts\runtime-recovery-smoke\runtime-recovery-smoke-20260709-192758\sleep-result.json` records `status=applied`, `primitive_verification_status=verified`, `time.day` 9->10, and `post_sleep_menu_closed`. | high | The macro is training-runtime only and must not target the player's live save. Temporary-bed/Tent Kit sleep remains explicitly outside this executor. |
| EVD-057 | `executor.interact` is no longer a broad disabled placeholder; the runtime harness has a narrow map-action executor shell that only accepts transparent adjacent `Buildings.Action` branches in a small whitelist and blocks missing/unknown interaction requests before side effects. | `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts\Invoke-RuntimeInteractPositiveSmoke.ps1`; `tests/StardewAI.Core.Tests/TrainingExecutionContractTests.cs` | `GameLocation.performAction(string, Farmer, xTile.Dimensions.Location)` handles `Buildings.Action` properties and is narrower than generic `checkAction`. The harness reads the target tile's `Action` property, requires `interaction_kind=map_action`, requires the first action token to match `expected_action_type`, currently whitelists only `OpenShop` and `Buy`, requires adjacency and no active menu, then records menu/location/tile deltas. | `dotnet test "StardewValleyAICompanion.sln" --no-restore` PASS with Core 123 and Backend 34. Hidden/silent E: isolated blocked runtime smoke PASS: `artifacts\runtime-interact-smoke\runtime-interact-blocked-smoke-20260709-195250\summary.json` records `status=passed`, result `status=blocked`, `primitive_kind=interact`, and block reason `interact_target_tile_required`. Positive smoke script ran and safely skipped on the current `FarmHouse` save because no adjacent `OpenShop`/`Buy` endpoint existed: `artifacts\runtime-interact-positive-smoke\runtime-interact-positive-smoke-20260709-200059\summary.json`. | medium-high | Positive applied `OpenShop`/`Buy` runtime smoke is still pending. Generic `checkAction`, `TouchAction`, unsupported action branches, purchase clicks, and menu item selection remain blocked. |
| EVD-058 | `executor.traverse_connector` is a controlled cross-map movement primitive: it walks to a transparent connector tile using the normal collision path, supports boundary/off-map warp exits by walking to the in-map edge then continuing outward, permits location change only when the request declares the expected target location, and verifies the observed target location/optional arrival tile. | `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts\Invoke-RuntimeRouteConnectorSmoke.ps1`; `scripts\Invoke-RuntimeRouteToShopSmoke.ps1`; `tests\StardewAI.Core.Tests\ActionQueueCompilerTests.cs`; `tests\StardewAI.Core.Tests\TrainingExecutionContractTests.cs` | Uses the same collision-safe movement boundary as EVD-002 until the connector tile is reached. Unlike `executor.move_to_tile`, it does not treat all location changes as success; it requires `expected_target_location` and compares the post-warp `Game1.currentLocation.NameOrUniqueName` plus optional `Game1.player.TilePoint` arrival tile against transparent connector data. Boundary warps with coordinates like `x=-1`, `x=width`, `y=-1`, or `y=height` are handled as edge-exit movement rather than hard-blocked target tiles. | Core tests PASS with 125 tests. Backend tests PASS with 34 tests. RuntimeTestHarness build PASS. Hidden/silent E: isolated runtime route connector smoke PASS: `artifacts\runtime-route-connector-smoke\runtime-route-connector-smoke-20260709-202610\summary.json`. Route-to-shop smoke reached Town through verified segments `FarmHouse -> Farm -> BusStop -> Town` and then correctly blocked on transparent Joja door open time: `artifacts\runtime-route-to-shop-smoke\runtime-route-to-shop-smoke-20260709-205532\summary.json`. | high | Positive `OpenShop`/`Buy` runtime smoke still needs a 9:00+ isolated save or reliable background time advancement before entering Joja/Pierre and interacting with shop endpoint. |
| EVD-054 | `executor.close_menu` can be a narrow recovery primitive only when compiler/runtime gates avoid sleep prompts, dialogue/question menus, unknown menu types, and menus not ready to close. | `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts\Invoke-RecoveryRuntimeSmoke.ps1`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs` | `Game1.cs:6128-6131` implements `Game1.exitActiveMenu()` as `activeClickableMenu = null`. `IClickableMenu.cs:899-902` default `readyToClose()` returns true, but concrete menus may override it; `ShopMenu.cs:1255-1262` returns false while `heldItem != null` or animations are active. Runtime harness therefore checks whitelist, rejects `DialogueBox`, checks `menu.readyToClose()`, then calls `Game1.exitActiveMenu()`. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 121 and Backend 34. Hidden/silent E: isolated runtime no-active-menu smoke PASS: `artifacts\runtime-recovery-smoke\runtime-recovery-smoke-20260709-192758\close-menu-result.json` records `status=no_op` and `primitive_verification_status=verified_no_active_menu`. | high | Current whitelist is intentionally small: `GameMenu`, `InventoryMenu`, `QuestLog`, `MapPage`, `ProfileMenu`, `ShopMenu`. Sleep prompts and generic `DialogueBox` are blocked rather than closed; a positive close smoke with a whitelisted menu should be added when a safe menu-opening primitive exists. |
| EVD-055 | Active menus are a compiler-wide bracket gate: while a menu is open, non-menu executor actions such as movement, interaction, waiting, active-item selection, crop maintenance, and sleep macro steps must block until an explicit `close_menu` step is compiled first. | `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs`; `scripts\Invoke-RecoveryRuntimeSmoke.ps1` | `Game1.cs:4274-4278` routes updates to `updateActiveMenu(...)` when `activeClickableMenu != null`. `Game1.cs:4286-4289` only calls `UpdateControlInput(...)` when `activeClickableMenu == null`. This proves an open menu interrupts normal control input/movement and cannot overlap with walking onto the bed `TouchAction=Sleep` tile. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 121 and Backend 34. Hidden/silent E: isolated runtime recovery smoke PASS: `artifacts\runtime-recovery-smoke\runtime-recovery-smoke-20260709-192758\summary.json`. | high | The compiler tracks plan-level menu state with `compiler_context.active_menu_open_before_step`; future menu-opening executors must set menu-open expected effects so bracket tracking remains conservative. |
| EVD-053 | `executor.select_safe_item_slot` is a narrow post-active-item safety primitive: it selects a proven safe toolbar slot and verifies that no active object remains selected, without clicking, using, placing, or opening menus. | `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs`; `artifacts/runtime-select-safe-slot-summary.json` | Uses EVD-051 local decompile evidence: `Farmer.CurrentToolIndex` is the toolbar selector and `Farmer.ActiveObject` exposes the held active object. Compiler gate requires `player.safe_item_context.safe_slot_available=true` or explicit `safe_slot_index` in range 0-11. Runtime harness only sets `Game1.player.CurrentToolIndex` and verifies `CurrentToolIndex` equals the safe slot and `Game1.player.ActiveObject` is null. | Hidden/silent runtime smoke PASS, run `train.20260708122242.1c773dc0`, manifest `E:\StardewAITraining\runs\train.20260708122242.1c773dc0\training-run-manifest.json`. Artifacts: `artifacts/runtime-select-safe-slot-launch-response.json`, `artifacts/runtime-select-safe-slot-before-snapshot.json`, `artifacts/runtime-select-safe-slot-execution.json`, `artifacts/runtime-select-safe-slot-after-snapshot.json`, `artifacts/runtime-select-safe-slot-summary.json`. Runtime result: `status=applied`, `primitive_verification_status=verified`, slot `0 -> 10`, active object null before/after, state hash changed. Tests/build: Core 110, Backend 34, RuntimeTestHarness 0 warnings/errors. | high | Active-item click/place/consume executors are still disabled; this primitive is only the safe-slot post-action boundary they must append later. The runtime smoke selected an empty safe slot from an already-null active object state; a later active-item executor smoke must prove active-object clearing after a real active-item action. |
| EVD-001 | Player tile fields are legitimate transparent inputs and should affect state hashes through `state.player.tile_x` / `state.player.tile_y`. | `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs`, `src/StardewAI.Contracts/State/SnapshotHash.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Character.cs:359-388` documents and implements `Character.Tile` and `Character.TilePoint` from standing pixel / position. `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Character.cs:327-341` implements `Position`. | Historical artifact `artifacts\runtime-episode-applied\runs\runtime-episode-applied\live-snapshots\plan-execution-episode-0001.json` records changed fact `player.tile` from `42,23` to `41,23`. | high | New after-snapshot freshness logic has been build-tested but not re-run against a live E: SMAPI session in this pass. |
| EVD-002 | Collision-safe movement uses Stardew's local collision API rather than direct cross-tile teleporting. | `tools/StardewAI.RuntimeTestHarness/ModEntry.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\GameLocation.cs:2494-2533` implements `GameLocation.isCollidingPosition(...)` using rectangle bounds and farmer bounding boxes. `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Character.cs:1347-1356` implements `Character.GetBoundingBox()`. | `artifacts\runtime-move-smoke\target-41-23-response.json`, `artifacts\runtime-episode-blocked\runs\runtime-episode-applied\live-snapshots\plan-execution-episode-0001.json`, `artifacts\runtime-episode-roomstep\runs\runtime-episode-applied\live-snapshots\plan-execution-episode-0001.json`. | high | Live runtime was not restarted during this pass; relying on prior runtime artifacts plus current build. |
| EVD-003 | Snapshot freshness can be checked with `state_hash` and `game_tick`; stale after snapshots are an output-recording blocker. | `tools/StardewAI.LiveTrainingLoop/Program.cs`, `src/StardewAI.Contracts/Training/TrainingEpisodeContracts.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Game1.cs:1054` declares `Game1.ticks`. `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Game1.cs:1250` declares `Game1.player`; `Game1.cs:1328` declares `Game1.currentLocation`. | Historical applied episode had equal `source_state_hash` and `after_state_hash` despite changed fact, proving the need for after-snapshot freshness recording: `artifacts\runtime-episode-applied\runs\runtime-episode-applied\live-snapshots\plan-execution-episode-0001.json`. | high | Need one live runtime episode after this patch to verify `after_snapshot_fresh=true` and movement-sensitive hash change in new artifacts. |
| EVD-004 | Training output must preserve the full chain from before snapshot through model plan, compiled queue, execution, after snapshot, feature row, and episode labels. | `tools/StardewAI.LiveTrainingLoop/Program.cs`, `src/StardewAI.Contracts/Training/TrainingEpisodeContracts.cs`, `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs` | No new Stardew gameplay rule. Contract-only output recording requirement from task files: `C:\Users\18236\deepseek-worker\handoff\stardewai\tasks\stardewai-full-handoff\TRANSPARENCY_COVERAGE.md:3-16` and `EVIDENCE_REQUIREMENTS.md:11-20`. | Prior artifacts under `artifacts\runtime-plan-loop-final`, `artifacts\runtime-episode-applied`, `artifacts\runtime-episode-blocked`, `artifacts\runtime-episode-roomstep`. | high | Contract fields are build-tested; new episode diagnostic fields need runtime artifact generation. |
| EVD-005 | Player economy, stamina, identity, facing, and key fields read by transparent snapshots are present on local decompiled Stardew types. | `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Farmer.cs:308-309` (`farmName`), `Farmer.cs:598-610` (`maxStamina`, `netStamina`), `Farmer.cs:1267-1302` (`totalMoneyEarned`), `Farmer.cs:1390-1404` (`hasRustyKey`, `hasSkullKey`), `Farmer.cs:1826-1842` (`Stamina`, `MaxStamina`), `Farmer.cs:1863` (`UniqueMultiplayerID`), `Farmer.cs:1989-2016` (`Money`, `FacingDirection`). | Existing snapshots under `artifacts\runtime-episode-applied\runs\runtime-episode-applied\live-snapshots\before-snapshot-0001.json`. | high | No new fields were added in this pass. |
| EVD-006 | Time and current-location model inputs are local Stardew global fields, not guessed values. | `src/StardewAI.TransparentBridge/State/TransparentStateCollector.cs`, `src/StardewAI.TransparentBridge/Adapters/WorldReadAdapter.cs`, `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Game1.cs:761` (`dayOfMonth`), `Game1.cs:766` (`timeOfDay`), `Game1.cs:1328` (`currentLocation`), `Game1.cs:1496` (`currentSeason`), `Game1.cs:1671` (`stats`). | Existing snapshots under `artifacts\runtime-episode-applied\runs\runtime-episode-applied\live-snapshots\before-snapshot-0001.json`. | high | No new fields were added in this pass. |
| EVD-007 | Crop maintenance hard rules must receive season and weather context from transparent state rather than relying on model memory. | `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`, `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`, `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Game1.cs:679-685` declares current weather booleans. `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Game1.cs:1496` exposes `currentSeason`. Wiki secondary checks: `https://stardewvalleywiki.com/Seasons` says seasons last 28 days and most crops/foraging/fish are season-limited; `https://stardewvalleywiki.com/Weather` says weather changes daily and affects fish/villager behavior; `https://stardewvalleywiki.com/Crops` says crops are generally seasonal and unwatered crops do not grow. | Existing hidden/silent runtime snapshots include `time.season` and `time.weather`; final artifacts under `artifacts\hidden-training-final-*`. | high | This pass adds required hard-rule context only; full crop planting season validation needs crop seed/content-data fields before implementation. |
| EVD-008 | Existing crop watering candidates can be derived from local Stardew `HoeDirt` methods instead of model judgement. | `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`, `src/StardewAI.Core/Execution/ActionQueueCompiler.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley.TerrainFeatures\HoeDirt.cs:379-387` implements `needsWatering()` using crop state and `CropData.NeedsWatering`. `HoeDirt.cs:389-394` implements `isWatered()`. `HoeDirt.cs:465-476` implements `readyForHarvest()`. | `artifacts\hidden-training-final-applied\runs\train.20260707074805.c3677324\live-snapshots\before-snapshot-0001.json`; final feature rows record crop maintenance state fields when present. | high | Rain/snow watering behavior should continue to rely on Stardew `HoeDirt.needsWatering()` and `isWatered()` until a separate local decompile audit proves any weather override rule is needed. |
| EVD-009 | Crop planting season legality depends on `Game1.cropData`, `Data\Crops`, location planting rules, and `SeedsIgnoreSeasonsHere()`, so seed/crop catalog facts must be transparent before seed-buying or planting candidates can be trusted. | `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`, `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`, `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Game1.cs:831-832` declares cached crop data from `Data/Crops`. `DataLoader.cs:178-183` loads `Data\Crops`. `Crop.cs:329-350` reads `Game1.cropData` by seed id. `Crop.cs:353-378` implements crop in-season checks with `SeedsIgnoreSeasonsHere()`. `HoeDirt.cs:536-616` implements planting: resolve seed id, `Crop.TryGetData`, `CheckItemPlantRules`, `CanPlantSeedsHere`, `data.Seasons.Contains(season)`, and season ignore override. `GameLocation.cs:657-671` implements `SeedsIgnoreSeasonsHere()` and `CanPlantSeedsHere`; `GameLocation.cs:684-745` implements `CheckItemPlantRules`. Wiki secondary: `https://stardewvalleywiki.com/Crops` states crops are generally seasonal, can be planted only during designated season, and season transition withers invalid crops. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `farm.crop_catalog` is now transparent, but planting executor remains blocked until plantable tile, inventory seed candidate, location override, and executor interaction fields are also covered. |
| EVD-010 | Inventory seed candidates can be transparently derived from `Game1.player.Items`, item identity/category, `Object.SeedsCategory`, and `Crop.ResolveSeedId`/`Game1.cropData` instead of model memory. | `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs`, `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`, `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `I:\StardewValleyAICompanion-decompile\StardewValley\StardewValley\Farmer.cs:1258-1260` exposes player inventory through `Items`. `Item.cs:116`, `Item.cs:168-195` expose `Category`, `ItemId`, and `QualifiedItemId`. `Object.cs:205` defines `SeedsCategory = -74`. `Crop.cs:301-323` implements `ResolveSeedId`, and `Crop.cs:343-350` validates crop seed ids through `Game1.cropData`. `HoeDirt.cs:536-616` consumes the resolved seed id during planting. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `player.seed_inventory` covers inventory seed candidates, but planting still needs transparent tile plantability, location season override state, grow-time constraints, and executor interaction coverage. |
| EVD-011 | Current-location planting feasibility must use Stardew's own `CanPlantSeedsHere`, `CheckItemPlantRules`, `SeedsIgnoreSeasonsHere`, and `HoeDirt` tile state rather than rough location heuristics. | `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs` | `GameLocation.cs:657-671` implements `SeedsIgnoreSeasonsHere()` and `CanPlantSeedsHere(...)`. `GameLocation.cs:684-745` implements `CheckItemPlantRules(...)` over plantable location rules and game-state queries. `HoeDirt.cs:550-592` resolves seed id, checks `CheckItemPlantRules`, checks `CanPlantSeedsHere`, and applies season/indoor-pot/season-ignore logic before planting. `HoeDirt.cs:592-615` blocks seeds outside allowed crop seasons unless season bypass applies. Wiki secondary: `https://stardewvalleywiki.com/Crops` confirms designated crop seasons and withering behavior; Greenhouse/Ginger Island/Indoor Pot exceptions still need dedicated Wiki confirmation before executor enablement. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `current_location.planting_context` is a read-only preview. Planting executor remains blocked until grow-time/fertilizer speed, shop stock, item consumption, UI/executor interaction, and exception-specific Wiki evidence are covered. |
| EVD-012 | Crop mature-before-season-end estimates must account for crop phase days, 28-day seasons, Speed-Gro fertilizer, and Agriculturist, while paddy-water acceleration remains unresolved until separately transparent. | `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs`, `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs` | `Game1.cs:761` declares `dayOfMonth`; `Game1.cs:7795-7814` increments day and rolls season after day 28. `Crop.cs:262-263` loads `CropData.DaysInPhase` into crop `phaseDays`. `Crop.cs:830-872` advances crop phases in `newDay`. `HoeDirt.cs:618-663` implements `applySpeedIncreases`: reset phase days, compute grow-day reduction, add fertilizer speed, paddy 0.25, and Agriculturist 0.1, then decrement phase days over up to three passes. `HoeDirt.cs:1084-1101` maps Speed-Gro `(O)465` to 0.1, Deluxe Speed-Gro `(O)466` to 0.25, and Hyper Speed-Gro `(O)918` to 0.33. `Farmer.cs:143` defines `agriculturist = 5`. Wiki secondary: `https://stardewvalleywiki.com/Crops` confirms crops grow over days and wither on season transition; fertilizer/Agriculturist exception pages still need dedicated Wiki confirmation before executor enablement. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | medium-high | Current estimate intentionally excludes paddy-water acceleration and is marked `without_paddy`; planting remains blocked until paddy and exception pages are covered or explicitly treated as conservative blockers. |
| EVD-013 | Purchase candidates must use the actual `ShopMenu.itemPriceAndStock` entries and `ItemStockInformation` price/stock/trade fields, not inferred shop memory. | `src/StardewAI.TransparentBridge/Adapters/MenuReadAdapter.cs`, `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`, `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `ShopMenu.cs:188-216` exposes `ShopId`, `ShopData`, `forSale`, and `itemPriceAndStock`. `ShopMenu.cs:315-323` constructs menu stock from `Dictionary<ISalable, ItemStockInformation>`. `ShopMenu.cs:1096-1126` reads `itemPriceAndStock` price/stock when purchasing. `ShopMenu.cs:1313-1325` applies stock updates during purchase. `ShopBuilder.cs:16-28` loads shop stock from `Data\Shops`; `ShopBuilder.cs:45-108` resolves item conditions, item queries, base price, available stock, trade items, limited stock mode, synced stock, and actions on purchase. `ItemStockInformation.cs:7-57` defines price, stock, trade item, trade count, limited stock mode, synced key, and actions on purchase. `ISalable.cs:13-24`, `ISalable.cs:42-58` expose item id/display/stack/quality/price/purchase checks. Wiki secondary for individual shop hours/stock still pending. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `menus.shop_stock` covers an already-open shop menu. It does not yet prove shop-opening hours, owner availability, route to shop, festival closures, or safe UI purchase execution. |
| EVD-014 | Shop access context must include `Data\Shops`, current owner rules, current-location NPC presence, and festival store closures before any shop-opening/purchase plan can be trusted. | `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs`, `src/StardewAI.TransparentBridge/ModEntry.cs` | `DataLoader.cs:483-485` loads `Data\Shops`. `ShopBuilder.cs:207-211` returns current owners by evaluating owner conditions with `GameStateQuery`. `Utility.cs:4177-4218` opens a shop by id and optional owner name after loading `Data\Shops` and resolving current owner data. `Utility.cs:4230-4323` opens shops in a location only if a matching owner NPC is present in the allowed area unless `forceOpen` applies. `GameLocation.cs:12715-12722` implements `AreStoresClosedForFestival()` using `Utility.isFestivalDay()` and festival start time. `GameLocation.cs:10322-10324` uses festival closure to lock store doors in valley context. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `locations.shops` now summarizes access context, but exact per-shop door actions, owner-area rectangles, map routes, and Wiki shop-hour confirmation are still needed before shop opening or purchasing can execute. |
| EVD-015 | Shop-opening tiles must be read from current map `Action` properties because `GameLocation.performAction` parses `OpenShop` arguments for direction, open/close time, and owner area; legacy `Buy` actions have hardcoded branches. | `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs` | `GameLocation.cs:9061-9110` handles `Action OpenShop`, parsing shop id, required direction, open time, close time, owner-area rectangle, direction checks, time checks, and `Utility.TryOpenShopMenu`. `GameLocation.cs:10748-10780` handles legacy `Action Buy` and maps legacy shops like Fish, SeedShop, and Sandy. `GameLocation.cs:10832-10850` shows additional hardcoded shop-opening logic for Saloon owner-area checks and island absence fallback. `ShopMenu.cs:1096-1126` and `ShopMenu.cs:1319-1389` show that UI purchase execution depends on click row, currency, stock, trade item, `CanBuyItem`, charge/consume, and action-on-purchase logic. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `current_location.shop_action_tiles` only covers current-map tile action discovery. It does not execute movement, tile action, UI click selection, currency/trade consumption, or exact legacy branch handling. |
| EVD-016 | Purchase execution requires transparent affordability, trade-item, inventory acceptance, and side-effect audit fields before any click executor can be safe. | `src/StardewAI.TransparentBridge/Adapters/MenuReadAdapter.cs` | `ShopMenu.cs:1096-1126` computes clicked row quantity from stock/currency and calls `tryToPurchaseItem`. `ShopMenu.cs:1319-1389` checks `readOnly`, stock, price, trade items, currency, `CanBuyItem`, charges player, consumes trade items, and calls `actionWhenPurchased`. `ShopMenu.cs:1515-1553` implements `HasTradeItem`/`ConsumeTradeItem`, including Qi Gems `(O)858` and Golden Walnuts `(O)73`. `ShopMenu.cs:881-890` and `ShopMenu.cs:1290-1309` map currency balances and charging. `Item.cs:607-610` uses inventory acceptance for base `CanBuyItem`; `Object.cs:7097-7117`, `Tool.cs:863-910`, `PurchaseableKeyItem.cs:138-146`, and `PetLicense.cs:47-53` show `actionWhenPurchased` can mutate state or open menus. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `menus.shop_stock` now includes affordability/trade/inventory audit fields, but purchase executor remains blocked until held-item state, clicked-row coordinates, post-state recording, and side-effect whitelist are implemented and tested. |
| EVD-023 | Purchase executor availability must be an explicit transparent false until side-effect whitelisting and post-state tests exist. | `src/StardewAI.TransparentBridge/Adapters/MenuReadAdapter.cs` | Same local purchase path as EVD-016. Additional risk evidence: `ShopMenu.cs:1022-1025` gates clicks on safety timer/read-only/held item; `ShopMenu.cs:1398-1416` mutates synchronized stock and runs actions on purchase; `ShopMenu.cs:1417-1420` invokes `onPurchase`; `ShopMenu.cs:1506-1512` removes depleted stock entries. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `executor_purchase_enabled=false` is now explicit per shop and per item. This is a safety gate, not executor implementation. |
| EVD-017 | Paddy crop timing must include crop data `IsPaddyCrop`, 3-tile water scan, auto-watering, and +0.25 paddy growth speed when eligible. | `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`, `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs` | `Crop.cs:980-983` implements `isPaddyCrop()` from `CropData.IsPaddyCrop`. `HoeDirt.cs:878-920` implements `hasPaddyCrop()` and `paddyWaterCheck`, excluding indoor pots and scanning radius 3 for water. `HoeDirt.cs:592-608` applies paddy auto-water after planting. `HoeDirt.cs:923-950` auto-waters paddy crops before `crop.newDay`. `HoeDirt.cs:618-663` adds paddy speed boost `0.25` in `applySpeedIncreases`. Wiki secondary: Rice Shoot and Taro Tuber pages confirm 3-tile irrigation, no watering needed when irrigated, and faster growth; Fertilizer/Farming pages confirm speed fertilizer and Agriculturist. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | Paddy fields are transparent read-side only; planting/growth executors remain blocked until post-day growth/output recording and full exception tests exist. |
| EVD-018 | Selling and shipping candidates need transparent item eligibility and price fields from `Item`/`Object`, not model memory. | `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs` | `ShopMenu.cs:1021-1089` and `ShopMenu.cs:1555-1647` sell items using `sellToStorePrice(-1L)`, `sellPercentage`, shop sell filters, and buyback behavior. `ShippingBin.cs:182-195` only accepts active items where `canBeShipped()` is true and player is within range. `ShippingMenu.cs:260-291` values shipped items with `sellToStorePrice(-1L) * Stack`. `Item.cs:530-539` defines default sell/sale price; `Item.cs:617-621` default `canBeShipped()` false. `Object.cs:1624-1632` defines object shipping eligibility; `Object.cs:2792-2804` defines object trash protection; `Object.cs:7171-7254` defines object sell/sale price behavior. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | Inventory now exposes sell/shipping basics, but sell/shipping executors remain blocked until shop sell filters, shipping-bin location/range, protected-item policy, buyback effects, and end-of-day payout recording are covered. |
| EVD-020 | Shop selling and shipping-bin shipping need explicit shop sell filters, sell percentage, buyback context, and bin distance/range fields. | `src/StardewAI.TransparentBridge/Adapters/MenuReadAdapter.cs`, `src/StardewAI.TransparentBridge/Adapters/FarmReadAdapter.cs`, `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs` | `ShopMenu.cs:211-218` stores `categoriesToSellHere`, `tagsToSellHere`, and `sellPercentage`. `ShopMenu.cs:247-255` stores custom `onSell`, `canPurchaseCheck`, and `_isStorageShop`. `ShopMenu.cs:852-879` highlights sellable inventory by category or tag groups. `ShopMenu.cs:1021-1089` and `ShopMenu.cs:1555-1647` execute shop sales using sell filters, safety timer, read-only state, `onSell`, `sellPercentage`, buyback, and tomorrow-resale behavior. `ShippingBin.cs:182-195` requires `canBeShipped()`, a farm, and distance `<= 2` from bin tile. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `menus.sell_context` and `farm.shipping_bins` are read-only. Sell/shipping executors remain blocked until exact click/active-item execution, protected-item policy, buyback/post-state recording, and end-of-day payout tests exist. |
| EVD-022 | Protected-item sell/shipping gates need item runtime type, special item flag, object quest/big-craftable/type flags, and furniture/wallpaper classification. | `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs` | `Item.cs:74` exposes `specialItem`; `Item.cs:558-573` blocks trashing for special items and protected tools; `Item.cs:617-621` defaults `canBeShipped()` false. `Object.cs:364-365` exposes `questItem`; `Object.cs:382-383` exposes `bigCraftable`; `Object.cs:1624-1632` blocks shipping big craftables, quest types, untrashable objects, furniture, and wallpaper; `Object.cs:2792-2804` blocks trashing quest items and object data `CanBeTrashed=false`. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | Protected-item fields are visible, but final never-sell policy still needs candidate policy rules, user-protected item lists, and executor tests before selling/shipping can run. |
| EVD-024 | Auto-sell protection reasons should be explicit and conservative so sell candidates cannot rely on model judgement for risky inventory items. | `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs` | Same protected item sources as EVD-022, plus `Object.cs:7171-7254` sell/sale price behavior and `ShopMenu.cs:1021-1089` sale path using `sellToStorePrice(-1L)`. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `auto_sell_protection_reasons` is a default hard gate. User-defined protected item preferences still need a configurable policy layer before sell execution. |
| EVD-019 | Local routing/shop approach needs Stardew collision probes, not only map coordinates. | `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs`; `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs` | `GameLocation.cs:2358-2365` exposes public `isCollidingPosition` overloads. `GameLocation.cs:2494-2896` implements collision checks over map bounds, animals, buildings, resource clumps, furniture, large terrain features, objects, terrain features, characters, events, farmers, barriers, and tile properties. `PathFindController.cs:180-246` uses a farmer-sized rectangle of `x*64+1,y*64+1,62,62` and `location.isCollidingPosition(..., pathfinding: true)` while expanding four-direction paths. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | medium-high | `current_location.route_context` is a local radius probe. `locations.collision_grid` now emits a compressed current-map grid of blocked/action/warp/door tiles only. Cross-map routing remains blocked until dynamic blocker classes, resolved connector graph, route executor, and tests are covered. |
| EVD-021 | Door/interior-door connectors must be transparent before cross-location or indoor shop routing can be trained. | `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs` | `GameLocation.cs:273-276` declares `doors` and `interiorDoors`. `GameLocation.cs:2146-2154` checks warps then doors. `GameLocation.cs:2156-2192` detects door collision. `GameLocation.cs:2194-2245` resolves door warps including building doors and map `Action` properties. `GameLocation.cs:8222-8249` opens interior doors. `InteriorDoorDictionary.cs:96-120` stores/reset interior door shared state; `InteriorDoorDictionary.cs:142-158` reads door tiles from map property `Doors`. `InteriorDoor.cs:57-83` maps interior door tile action to touch action. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | medium-high | Door fields are read-only. Full door/warp route graph and executor remain blocked until target resolution, locked-door conditions, building indoor targets, and route execution are covered. |
| EVD-025 | Route connectors must be normalized before planner training so the model does not infer warps, doors, or shop endpoints from unrelated fields. | `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs`; `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs` | Same connector evidence as EVD-019 and EVD-021. `GameLocation.cs:2146-2245` handles warp/door collision and action resolution. `GameLocation.cs:8695-9110` implements `performAction` branches including `OpenShop`; `GameLocation.cs:9475` handles `Warp`; `GameLocation.cs:9882` handles legacy `Buy`. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | medium-high | `locations.route_connectors` is a current-map read-only connector index. It does not run route search, resolve every locked/conditional connector, or enable any executor. |
| EVD-026 | Dynamic and static collision participants must be visible before route training so path blockers are not inferred by the model. | `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs` | `GameLocation.cs:247` declares `characters`; `GameLocation.cs:284` declares `resourceClumps`; `GameLocation.cs:286` declares `largeTerrainFeatures`; `GameLocation.cs:452` declares `furniture`; `GameLocation.cs:2494-2896` checks collision participants including characters, resource clumps, furniture, large terrain features, objects, and terrain features. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | medium-high | `locations.route_blockers` exposes current-map blocker classes and bounding boxes. It is not a solved movement graph and does not replace `GameLocation.isCollidingPosition` candidate checks. |
| EVD-027 | Locked/conditional route gates must be evaluated from Stardew APIs before route or shop-opening training. | `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs` | `GameLocation.cs:9537-9545` parses `LockedDoorWarp`; `GameLocation.cs:10319-10378` evaluates Town Key, festival closures, SeedShop Wednesday, Willy hours mail, time windows, friendship, winter, and green-rain overrides. `GameLocation.cs:9547-9564` evaluates `ConditionalDoor` through `GameStateQuery.CheckConditions`. `GameLocation.cs:3653-3708` shows warp actions can require mail. `GameLocation.cs:2469-2470` checks map `BuildConditions`. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `locations.route_gate_context` is read-only gate transparency. It does not cover every hardcoded action branch, does not execute actions, and does not enable route/shop-opening training. |
| EVD-028 | Remaining hardcoded map action branches must be explicitly surfaced as unsupported before route/shop-opening training. | `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs` | `GameLocation.cs:8695-10124` contains many `performAction` branches beyond generic route/shop actions. Prior route evidence covers `OpenShop`, `Warp`, `LockedDoorWarp`, `ConditionalDoor`, `Door`, and legacy `Buy`; other branches must not be inferred by the model. | Runtime regeneration pending because training is paused; compile/build/test evidence is local only in this pass. | high | `locations.route_action_branch_coverage` is an audit surface. Unsupported branches block route/shop-opening training until separately transparent and tested. |
| EVD-029 | Route candidate/compiler gates must consume unsupported action branch coverage without over-blocking unrelated player actions. | `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | Builds on EVD-028. `exploration.visit_location` now requires `locations.route_action_branch_coverage`; the compiler adds `unsupported_route_action_branch_at_target` only when the requested target tile has an unsupported route action branch. | `dotnet build "src\StardewAI.Core\StardewAI.Core.csproj"` passed; `dotnet test "StardewValleyAICompanion.sln"` passed with `StardewAI.Core.Tests` 55 and `StardewAI.Backend.Tests` 31. | high | This is a scoped hard block for route preview/training. Unsupported branches elsewhere on the map do not block unrelated target tiles; path-level blocking still needs a real route path. |
| EVD-030 | Route gates should block unsupported action branches only when the current-map path actually requires them. | `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | Builds on EVD-019 and EVD-028. Compiler-side current-map BFS uses transparent `locations.collision_grid` width/height/notable blocked tiles and `locations.route_action_branch_coverage` unsupported tiles to find a path that avoids unsupported action branches before blocking. | `dotnet build "src\StardewAI.Core\StardewAI.Core.csproj"` passed; `dotnet test "StardewValleyAICompanion.sln"` passed with `StardewAI.Core.Tests` 57 and `StardewAI.Backend.Tests` 31. | high | Current-map only. Cross-map warps/doors, dynamic blockers beyond emitted collision grid, and executor movement remain blocked until graph resolution and runtime tests exist. |
| EVD-031 | Cross-map route preview needs a transparent graph over loaded locations before route training can target other maps. | `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `Game1.cs:433` stores `_locations`; `Game1.cs:1326` exposes `Game1.locations`; `Game1.cs:10226-10268` resolves locations by name. Route edges reuse EVD-019/EVD-021/EVD-027 sources for warps, doors, and action warps. | `dotnet build "src\StardewAI.TransparentBridge\StardewAI.TransparentBridge.csproj"` passed; `dotnet test "StardewValleyAICompanion.sln"` passed with `StardewAI.Core.Tests` 59 and `StardewAI.Backend.Tests` 31. | high | `locations.route_graph` is loaded-location read-side preview only. Compiler uses only `resolved=true` edges for cross-map `target_location`; unresolved edges do not prove reachability. Route executor remains disabled. |
| EVD-032 | Cross-map route preview must validate the current-map segment to the first connector, not just location-level graph reachability. | `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | Builds on EVD-019 and EVD-031. Compiler takes the first resolved route graph edge and uses the transparent current `locations.collision_grid` to verify the player can reach that connector tile while avoiding unsupported action branches. | `dotnet build "src\StardewAI.Core\StardewAI.Core.csproj"` passed; `dotnet test "StardewValleyAICompanion.sln"` passed with `StardewAI.Core.Tests` 61 and `StardewAI.Backend.Tests` 31. | high | Start-segment only. Later segments need per-location collision grids before full cross-map path stitching can be trusted. |
| EVD-033 | Per-location route summaries are needed to distinguish validated current-map segments from pending later-map segments without over-claiming route safety. | `src/StardewAI.TransparentBridge/Adapters/ShopAccessReadAdapter.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | Builds on `Game1.locations` evidence from EVD-031 and map/layer evidence from EVD-019. `locations.route_map_summaries` records loaded location dimensions, connector/action counts, unsupported branch counts, and whether a collision grid is available for segment validation. | `dotnet build "src\StardewAI.TransparentBridge\StardewAI.TransparentBridge.csproj"` passed; `dotnet test "StardewValleyAICompanion.sln"` passed with `StardewAI.Core.Tests` 62 and `StardewAI.Backend.Tests` 31. | high | Full collision grids are still current-location only. Later segment validation remains pending until per-location grids or on-demand probes exist. |
| EVD-034 | Interact must be a narrow transparent primitive because Stardew's action button can trigger many side effects. | `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | `Game1.cs:11146-11473` implements action-button flow including dialogue, tile checks, active object use, and edible handling. `Game1.cs:11502-11517` routes `tryToCheckAt` through `GameLocation.checkAction`. `GameLocation.cs:7639-7939` shows `checkAction` can interact with pets/buildings/NPCs/resource clumps/objects/forage/terrain/furniture/animals and map `Action`. `GameLocation.cs:7868-7888` calls `performAction` for `Buildings` `Action`. | `dotnet test "StardewValleyAICompanion.sln" --no-restore` PASS with Core 123 and Backend 34. | high | `executor.interact` remains limited to adjacent transparent `map_action`. Runtime shell is now present for whitelisted `Buildings.Action` branches (`OpenShop`, `Buy`) per EVD-057; positive runtime smoke and broader side-effect whitelist remain pending. |
| EVD-035 | Runtime primitive results must explicitly record requested effect, observed effect, verification status, and reasons before executor calibration rows can be trusted. | `src/StardewAI.Contracts/Training/TrainingExecutionContracts.cs`; `src/StardewAI.Contracts/Training/TrainingEpisodeContracts.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tools/StardewAI.LiveTrainingLoop/Program.cs`; `tests/StardewAI.Core.Tests/TrainingExecutionContractTests.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | No new Stardew gameplay rule. Uses existing local runtime primitives backed by EVD-001/EVD-002/EVD-003/EVD-005: player tile, facing direction, ticks, and collision-safe movement evidence. Interact adds request-side `interaction_kind` and `expected_action_type` so runtime can reject unsafe broad interactions. | Hidden/silent run `train.20260707152832.4e767523`: applied artifact `artifacts\hidden-training-primitive-verification-applied\runs\train.20260707152832.4e767523\live-snapshots\plan-execution-episode-0001.json` records `primitive_verification_status=verified`, `requested_effect=player.tile=41,23`, `observed_effect=player.tile=41,23`, `after_snapshot_fresh=true`, `state_hash_changed=true`; blocked artifact `artifacts\hidden-training-primitive-verification-blocked\runs\train.20260707152832.4e767523\live-snapshots\plan-execution-episode-0001.json` records `primitive_verification_status=blocked`, reason `target_tile_blocked:999,999`, `after_snapshot_fresh=true`, `state_hash_changed=true`. `dotnet test "StardewValleyAICompanion.sln" --no-restore` PASS with Core 123 and Backend 34. | high | Broad policy/value training remains paused; these rows are executor-calibration only. Interact positive runtime smoke remains pending per EVD-057. |
| EVD-036 | Hard-rule required-field availability should be mapped into candidate options before model scoring so the small model ranks only currently actionable choices. | `src/StardewAI.Contracts/Options/OptionContracts.cs`; `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Backend/Program.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tests/StardewAI.Backend.Tests/BackendEndpointTests.cs` | No new Stardew gameplay rule. This reuses existing `OptionRegistry.RequiredStateFactors` and `Verifier.Verify(...)` over transparent snapshot envelopes; all underlying gameplay field evidence remains in prior EVD rows. | `dotnet test "StardewValleyAICompanion.sln"` PASS with `StardewAI.Core.Tests` 75 and `StardewAI.Backend.Tests` 33. Backend endpoint tests cover `/api/v1/planner/options/availability` and availability-aware `/api/v1/planner/baseline/rank-options`. | high | First pass is field-availability plus explicit preview/executor-disabled gating. Parameter-specific route/interact value gates remain compiler second-pass until safely extracted/shared. |
| EVD-037 | Parameter-bound route and interact hard-rule gates should be available before model scoring without duplicating compiler logic or enabling executors. | `src/StardewAI.Contracts/Options/OptionContracts.cs`; `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tests/StardewAI.Backend.Tests/BackendEndpointTests.cs` | No new Stardew gameplay rule. Reuses `ActionQueueCompiler.Compile(...)` as a non-executing synthetic dry-run probe over the same decompile-backed route/interact gates documented in EVD-029 through EVD-034. | `dotnet test "StardewValleyAICompanion.sln"` PASS with `StardewAI.Core.Tests` 78 and `StardewAI.Backend.Tests` 34. Tests cover parameterized `exploration.visit_location` target-branch block and `executor.interact` missing-target/valid-preview cases without `queue_global_compiler_block`. | high | Dry-run probe only merges item-level compiler block reasons. Runtime `executor.interact` now has a minimal whitelisted shell per EVD-057, but availability should remain conservative until positive smoke passes; route executor remains disabled. |
| EVD-038 | Buy/sell hard-rule value gates should prune economic candidates before model scoring while keeping purchase/sell/shipping executors disabled. | `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs` | No new Stardew gameplay rule. Consumes transparent fields already evidenced by EVD-013/EVD-016/EVD-018/EVD-020/EVD-022/EVD-023/EVD-024: `menus.shop_stock`, `player.inventory`, `menus.sell_context`, and `farm.shipping_bins`. | `dotnet test "StardewValleyAICompanion.sln"` PASS with `StardewAI.Core.Tests` 82 and `StardewAI.Backend.Tests` 34. Tests cover buy preview candidate, buy no-value block, sell preview candidate, and protected inventory block. | high | First economic pass is option-level, not item-level output. It does not enable purchase, sell, or shipping execution. |
| EVD-039 | Economic availability should expose concrete item-level buy/sell candidates so the event scorer can rank actions at item/slot granularity instead of coarse options. | `src/StardewAI.Contracts/Options/OptionContracts.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs` | No new Stardew gameplay rule. Uses the same transparent fields and evidence as EVD-038. | `dotnet test "StardewValleyAICompanion.sln"` PASS with `StardewAI.Core.Tests` 82 and `StardewAI.Backend.Tests` 34. Tests assert `economic_candidates[]` for buy/sell available and blocked items. | high | Candidate list is for scoring/preview only. Purchase, sell, and shipping executors remain disabled. |
| EVD-040 | Availability-aware event ranking should rank concrete economic candidates by `candidate_id` so the model can score item/slot actions, not only coarse option IDs. | `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `src/StardewAI.Backend/Program.cs`; `tests/StardewAI.Core.Tests/EventCandidateRankerTests.cs` | No new Stardew gameplay rule. Uses `economic_candidates[]` from EVD-039 and existing baseline option scores. | `dotnet test "StardewValleyAICompanion.sln"` PASS with `StardewAI.Core.Tests` 84 and `StardewAI.Backend.Tests` 34. Tests cover ranking available economic candidates and skipping blocked candidates. | medium-high | Baseline ranking heuristic only; not a trained event model yet. Runtime economic executors remain disabled. |
| EVD-041 | Non-economic event ranking exposes concrete watering candidates from transparent crop state before model scoring, without relying on model memory for tile selection. Those candidates can compile into targeted `farm.maintain_crops` execution for one crop tile. | `src/StardewAI.Contracts/Options/OptionContracts.cs`; `src/StardewAI.Contracts/Training/TrainingFeatureContracts.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `src/StardewAI.Core/Training/DailyPlanCompiler.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `scripts/Invoke-RuntimeTargetedWateringSmoke.ps1`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tests/StardewAI.Core.Tests/EventCandidateRankerTests.cs`; `tests/StardewAI.Core.Tests/DailyPlanCompilerTests.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs` | No new Stardew gameplay rule. Reuses EVD-008: `farm.crops[].needs_watering` is derived from local decompiled `HoeDirt.needsWatering()` and compiler crop maintenance uses the same field. Runtime targeted watering verifies the requested tile state update rather than choosing an arbitrary crop. | Hidden/silent isolated E: targeted watering smoke PASS: `artifacts\runtime-targeted-watering-smoke\runtime-targeted-watering-smoke-20260711-192559\summary.json`. The isolated save had no natural watering target, so explicit fixture `debug.setup_watering_target` created `farm.crops[64,15].needs_watering=true`; `farm.maintain_crops` then applied only tile `64,15`, reported `watering_status=applied`, `watering_verification=verified`, `watered_count=1`, and `target_crop_water_state_updated`. | high | This verifies one targeted watering path in an isolated harness. Broad crop automation still excludes planting, harvesting, fertilizer, multi-tile batching policy, and post-day growth/value recording until separately modeled and tested. |
| EVD-042 | Route connector event candidates should expose current-map connector tiles before model scoring, while recognizing that safe tile movement exists but cross-map/action triggering still needs controlled executor boundaries. | `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tests/StardewAI.Core.Tests/EventCandidateRankerTests.cs` | No new Stardew gameplay rule. Reuses route connector evidence from EVD-025 and compiler route gates from EVD-029 through EVD-033. Movement executor boundary is backed by `RuntimeTestHarness/ModEntry.cs`, which uses `GameLocation.isCollidingPosition`, NPC soft-obstacle replanning, removable-obstacle clearance, and primitive tile verification. | `dotnet test "StardewValleyAICompanion.sln"` PASS with `StardewAI.Core.Tests` 89 and `StardewAI.Backend.Tests` 34. Tests cover `route_connector_tile` event candidates, candidate-level compiler block reasons, and ranking. | high | Candidate points to the connector tile movement step only. Cross-map route execution, warp/door/shop triggering, and `executor.interact` remain disabled/controlled until side-effect and post-state verification exist. |
| EVD-043 | Interact endpoint event candidates should expose shop/action endpoints and route-to-adjacent previews before model scoring, without enabling the broad Stardew action-button executor. | `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tests/StardewAI.Core.Tests/EventCandidateRankerTests.cs` | No new Stardew gameplay rule. Reuses shop action tile evidence from EVD-015, broad interaction risk evidence from EVD-034, and route-to-adjacent gates from EVD-029 through EVD-033. | `dotnet test "StardewValleyAICompanion.sln" --no-restore` PASS with Core 123 and Backend 34. Tests cover `interact_endpoint` candidate extraction, menu/branch block reasons, route-to-adjacent dry-run use, and ranking. | high | Candidate is preview/scoring only until positive runtime smoke closes the whitelisted `OpenShop`/`Buy` path. Runtime shell exists per EVD-057 but generic action-button execution remains blocked. |
| EVD-044 | Recovery stabilize candidates should expose safe wait/refresh and blocked close-menu/home/sleep recovery choices before model scoring, without enabling unverified recovery executors. | `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `src/StardewAI.Core/Training/EventCandidateRanker.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `tests/StardewAI.Core.Tests/EventCandidateRankerTests.cs` | No new Stardew gameplay rule. Reuses transparent `time.time`, `player.energy`, `menus.active_menu`, existing recovery compiler intent, and already verified `executor.wait_ticks` primitive boundary. | `dotnet test "StardewValleyAICompanion.sln"` PASS with `StardewAI.Core.Tests` 95 and `StardewAI.Backend.Tests` 34. Tests cover available `recovery_refresh_plan`, blocked late-night close/home/sleep candidates, and ranking. | high | Only refresh/wait-style recovery is available as a candidate. Close-menu, return-home, and sleep remain blocked until dedicated executors and post-state verification exist. |
| EVD-045 | Home/bed targets for recovery must be transparent and bed-specific route gated before return-home or sleep-adjacent candidates can move beyond generic blocked placeholders. | `src/StardewAI.TransparentBridge/Adapters/CurrentLocationReadAdapter.cs`; `src/StardewAI.TransparentBridge/Adapters/MenuReadAdapter.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs` | `Utility.cs:5118-5120` implements `Utility.getHomeOfFarmer(Farmer)` through `Game1.RequireLocation<FarmHouse>(who.homeLocation.Value)`. `Farmer.cs:904-905` declares `homeLocation`. `FarmHouse.cs:609-611` implements `GetPlayerBedSpot()` as bed spot or entry fallback. `FarmHouse.cs:628` implements `getEntryLocation()`. `BedFurniture.cs:199-201` places wake-up position at `FarmHouse.GetPlayerBedSpot()`. `GameLocation.cs:2282-2290` uses `BedFurniture.IsBedHere(...)` to decide whether wake-up is valid. `BedFurniture.cs:473-486` implements `IsBedHere(...)` using the bed tile property. `BedFurniture.cs:489-509` shows furniture beds expose Bed property on their sleep tile, which may not be an ordinary walk target. `Game1.cs:11502-11523` shows `tryToCheckAt` requires target tile within radius 1 and faces toward the grab tile. `GameLocation.cs:3762-3766` shows `Action Sleep` creates the `Sleep` question dialogue. `GameLocation.cs:4431-4459` stores `lastQuestionKey` when creating question dialogues. `GameLocation.cs:12357-12359` shows `Sleep_Yes` calls `startSleep()`. Candidate gating therefore confirms `bed_tile_has_bed=true`, requires no active menu and no active object, finds a reachable adjacent stand tile, dry-runs route/collision to the stand tile, derives `face_direction` to the bed tile, and marks `Sleep_Yes_not_executed`. | `dotnet test "tests\StardewAI.Core.Tests\StardewAI.Core.Tests.csproj"` PASS with 100 tests. Tests cover available already-home bed-adjacent preview with facing/prompt metadata, bed tile collision not blocking when an adjacent stand tile is reachable, blocked preview when no adjacent stand tile is available, blocked preview when a menu is open, and blocked preview when an active object is held. | high | Read-side target and movement-only candidate preview. Bed tile collision is not treated as a sleep blocker. Sleep prompt is only expected metadata; `Sleep_Yes` and day transition remain disabled. Cross-map return-home remains blocked until route graph/segment validation proves the route. |
| EVD-046 | Runtime snapshot must prove the home/bed and menu sleep-prompt transparency fields exist in the actual hidden/silent SMAPI bridge, not just unit fixtures. | `artifacts/runtime-home-menu-snapshot.json`; `artifacts/runtime-home-menu-world-snapshot.json`; `artifacts/runtime-home-menu-field-summary.json`; `artifacts/runtime-recovery-availability.json`; `WORKER_NOTES.md`; `docs/stardewai-full-handoff/test-results.txt` | Hidden/silent isolated SMAPI launch through backend launcher produced a Bridge `snapshot.v1`. Runtime field summary shows `player_location=FarmHouse`, `home_available=true`, `current_location_is_home=true`, `bed_tile=43,23`, `bed_tile_has_bed=true`, `sleep_executor_enabled=false`, `menus.active_menu.last_question_key` property present, `menus.active_menu.is_sleep_prompt` property present with `false`, and `player.active_object_qualified_id=null`. Raw snapshot ingest accepted state hash `d34227e975124b6eb2e7650ee8bd3d88fd2f33f6c656d82914192c6bc576bc2a`. | Runtime smoke PASS for field shape. Backend detached with WMI/CIM to avoid OpenCode pipe hang; launcher manifest `E:\StardewAITraining\runs\train.20260708091900.1217b2fa\training-run-manifest.json` records hidden window and disabled sound. | high | Runtime time was not late-night, so recovery availability emitted refresh-plan rather than bed-return candidates. No sleep executor was enabled and `Sleep_Yes` was not executed. |
| EVD-047 | Sleep prompt state must be explicit and separately gated from route-to-bed and sleep-confirmation execution. | `src/StardewAI.TransparentBridge/Adapters/MenuReadAdapter.cs`; `src/StardewAI.Core/OptionRegistry/CandidateOptionAvailabilityEvaluator.cs`; `tests/StardewAI.Core.Tests/CandidateOptionAvailabilityEvaluatorTests.cs`; `WORKER_NOTES.md`; `docs/stardewai-full-handoff/test-results.txt` | Local decompile evidence from EVD-045 remains the behavioral source: `GameLocation.cs:3762-3766` creates the `Sleep` question dialogue, `GameLocation.cs:4431-4459` stores `lastQuestionKey`, and `GameLocation.cs:12357-12359` maps `Sleep_Yes` to `startSleep()`. The bridge now exposes `menus.sleep_prompt_context` with `prompt_open`, `last_question_key`, `confirm_action_key=Sleep_Yes`, `can_confirm_sleep=false`, and `confirm_executor_enabled=false`. Candidate gating blocks prompt-confirm state with `sleep_confirm_executor_disabled`. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 101 and Backend 34. Core tests cover blocking `recovery_return_home` when sleep prompt is already open. | high | Prompt-open verification is transparent; `Sleep_Yes` remains disabled and must not be learned or inferred by the model. |
| EVD-048 | Sleep should be a single compiler-owned terminal semantic action, not a model-learned sequence, because every day must end in sleep to avoid collapse and dirty training outcomes. | `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs`; `WORKER_NOTES.md`; `docs/stardewai-full-handoff/test-results.txt` | Uses EVD-045/EVD-047 plus corrected local evidence: `BedFurniture.cs:489-516` exposes `Bed` and `TouchAction=Sleep` on the bed sleep tile; `GameLocation.cs:4222-4230` checks the player's current tile `TouchAction` and calls `performTouchAction`; `GameLocation.cs:3762-3766` opens the `Sleep` question and requires `Game1.player.hasMoved`; `Sleep_Yes` calls `startSleep()`. Compiler maps plan step kind `sleep` to `executor.sleep`, requires `compiler_context.is_terminal_step=true`, and expands to `move_to_bed_adjacent`, `step_onto_sleep_touch_tile`, and `confirm_sleep_yes`. Runtime implementation and smoke evidence are recorded in EVD-056. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 121 and Backend 34. Tests cover terminal sleep macro expansion, non-terminal sleep blocking, direct sleep action blocking without terminal plan context, and recovery auto-sleep gates. Hidden/silent E: isolated runtime smoke PASS is recorded in EVD-056. | high | Normal farmhouse sleep is now runtime-verified for the isolated training harness. Temporary-bed/Tent Kit sleep remains separate and blocked. |
| EVD-049 | Tent Kit / temporary-bed sleep is a separate special-case path and must not be mixed into the normal farmhouse sleep macro until it has dedicated transparent fields and gates. | `docs/stardewai-full-handoff/evidence.md`; `WORKER_NOTES.md`; future transparent fields pending | `Object.cs:6269-6332` handles `(O)TentKit` placement: location must be outdoors, tomorrow must not be a festival day for the map, and a direction-derived 3x2 rectangle must pass `location.isAreaClear(rectangle)` before adding `new Tent(...)`. `Tent.cs` implements the deployed tent terrain feature and sleep interaction path. `GameLocation.cs:12360-12366` handles `SleepTent_Yes` by setting `Game1.player.isInBed.Value=true`, `Game1.player.sleptInTemporaryBed.Value=true`, hiding the farmer, and calling `startSleep()`. `GameLocation.cs:2282-2290` accepts `sleptInTemporaryBed` as a wake-up validity exception, `Game1.cs:9056` resets it on a new day, and `Farmer.cs` declares/uses `sleptInTemporaryBed`. Wiki secondary `https://stardewvalleywiki.com/Tent_Kit` confirms one-time use, outdoors-only placement, selected 3x2 area, blocked indoors/not enough space/next-day festival, sleep/save, 6am same-location wake-up, and tent disappearance. | Local decompile and Wiki review only; no code enabled for tent sleep. | high | Tent placement/use executor, inventory candidate, terrain-feature target, festival/map checks, same-location wake-up recording, and temporary-bed post-state remain blocked. This path may later support special non-home or multi-day chains, but not through normal `executor.sleep`. |
| EVD-050 | Late-night `recovery.stabilize_day` can compile directly to the same compiler-owned sleep macro when transparent home/bed state proves a safe target, so the small model does not need to output low-level sleep steps. | `src/StardewAI.Core/OptionRegistry/OptionRegistry.cs`; `src/StardewAI.Core/Execution/ActionQueueCompiler.cs`; `tools/StardewAI.RuntimeTestHarness/ModEntry.cs`; `tests/StardewAI.Core.Tests/ActionQueueCompilerTests.cs`; `docs/stardewai-full-handoff/test-results.txt` | Uses corrected EVD-048/EVD-056 touch-action evidence. `recovery.stabilize_day` requires sleep prompt/player tile/location fields. For `time.time >= 2200`, compiler validation requires a transparent `SleepTarget` and no already-open sleep prompt; compilation appends `move_to_bed_adjacent`, `step_onto_sleep_touch_tile`, and `confirm_sleep_yes`. Active object state is no longer a sleep gate because sleep prompt is triggered by walking onto `TouchAction=Sleep`, not pressing action with a held item. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 121 and Backend 34. Tests cover late-night recovery auto-sleep expansion, blocking when the sleep target is unavailable, no active-object sleep gate, and blocking when a sleep prompt is already open. Hidden/silent E: isolated runtime smoke PASS is recorded in EVD-056. | high | Runtime execution and day-transition output recording are now verified for the normal current-home farmhouse bed macro. Cross-map return-home remains blocked; this pass only auto-appends normal home bed sleep when the current transparent snapshot can prove the target. |
| EVD-051 | Active-item executors should switch to a safe toolbar slot immediately after click/place/consume actions, instead of relying on later sleep/recovery gates to catch a held object. | `src/StardewAI.TransparentBridge/Adapters/PlayerReadAdapter.cs`; future active-item executor/compiler files pending | `Farmer.cs:1787-1804` exposes `CurrentItem`, which returns null when stowed/out of range and otherwise returns `Items[currentToolIndex]`. `Farmer.cs:1808-1822` exposes `CurrentToolIndex`; setting it unstows and calls `CurrentItem.actionWhenStopBeingHeld(...)` when switching away. `Farmer.cs:1686-1695` shows `ActiveObject` is the active item as an object. `Game1.cs:11349-11380` shows active objects can perform use/place actions with side effects. The bridge now emits `player.current_tool_index` and `player.safe_item_context` with `active_object_selected`, `safe_slot_available`, `safe_slot_index`, and policy `prefer_empty_slot_then_tool_slot`. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 108 and Backend 34. Build proves the transparent field compiles; runtime field validation pending. | high | No active-item executor is enabled yet. The next executor contract must append `select_safe_item_slot` after active-object click/place/consume actions and verify `player.active_object_qualified_id=null` or selected safe slot in after-snapshot. |
| EVD-052 | AI-control runs need deterministic Stardew options for movement, toolbar selection, item stowing, and hidden-window execution; those settings must be transparent and opt-in rather than silently changing a human player's options. | `src/StardewAI.TransparentBridge/Models.cs`; `src/StardewAI.TransparentBridge/ModEntry.cs`; `src/StardewAI.TransparentBridge/Adapters/OptionsReadAdapter.cs`; `docs/stardewai-full-handoff/test-results.txt`; `artifacts/runtime-options-snapshot-2.json`; `artifacts/runtime-options-field-summary-2.json` | Wiki secondary `https://stardewvalleywiki.com/Options` confirms `Auto Run`, `Item Stowing`, `Gamepad Mode`, `Pause When Game Window Is Inactive`, `Use Controller-Style Menus`, and `Invert Toolbar Scroll Direction` can alter controls. Wiki `https://stardewvalleywiki.com/Controls` confirms Use Tool / Check Action, Switch Item, Shift Toolbar, Run, and inventory slots 1-12. Local evidence: `Options.cs` declares `autoRun`, `pauseWhenOutOfFocus`, `invertScrollDirection`, `stowingMode`, `gamepadMode`, and `snappyMenus`; defaults include auto-run true, stowing off, gamepad auto, snappy menus true, pause out of focus true. `Options.allowStowing` derives behavior from `stowingMode`. `Game1.cs:11545-11569` shows `invertScrollDirection` flips toolbar movement and tool cycling is over slots 0-11. `Game1.cs:11656` and `11684` show `allowStowing` can turn Use Tool into stow behavior. `Game1.cs:3902` shows pause-on-unfocused can pause single-player. `Game1.cs:3587` and `InputState.cs:78` show gamepad mode affects gamepad controls. Runtime correction: options can be loaded after `GameLaunched`, so the bridge converges settings on `UpdateTicked` when opt-in is enabled. | `dotnet test "StardewValleyAICompanion.sln"` PASS with Core 108 and Backend 34. Hidden/silent runtime smoke PASS: `runtime-options-field-summary-2.json` shows `auto_run=true`, `stowing_mode=Off`, `allow_stowing=false`, `gamepad_mode=ForceOff`, `gamepad_controls=false`, `snappy_menus=false`, `snappy_menus_effective=false`, `invert_toolbar_scroll_direction=false`, `pause_when_out_of_focus=false`, `ai_requested=true`, and `player.safe_item_context.safe_slot_available=true`. | high | `ApplyAiControlSettings=false` by default. Runtime smoke used isolated E: runtime with `ApplyAiControlSettings=true`, hidden window, and disabled sound. Runtime processes were stopped afterward and ports 5108/8765 were clear. |
