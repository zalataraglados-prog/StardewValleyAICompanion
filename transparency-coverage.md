# Transparency Coverage

| field or output id | consumer | required for training | transparent source path or reason unavailable | evidence claim id | runtime snapshot/artifact path | output recording path | status |
|---|---|---|---|---|---|---|---|
| `executor.sleep` home-bed target edge selection | executor, episode_writer | yes for terminal sleep calibration | `current_location.home_context`, `locations.collision_grid`, and bed `TouchAction=Sleep` context select a reachable bed-adjacent stand tile different from the current tile when possible; runtime still steps onto the native sleep touch tile before confirming | EVD-056, EVD-069 | `artifacts\runtime-sleep-edge-a-summary.json` | `training_execution_result.v1.primitive_kind=sleep`, `primitive_verification_status`, `primitive_verification_reasons`, `after_snapshot_path` | runtime_verified |
| native sleep prompt opened during touch-tile movement | executor, episode_writer | yes for sleep runtime edge handling | Runtime observes active native `DialogueBox` sleep prompt and stops movement before entering prompt confirmation; no model-memory assumption | EVD-056, EVD-069 | `artifacts\runtime-sleep-edge-a-summary.json` | `training_execution_result.v1.primitive_verification_reasons`, `status`, `after_state_hash` | runtime_verified |
| `farm.machines[].loadable_inputs[].predicted_output_net_value` ranking signal | ranker, daily_plan | yes for machine input/output ordering | Transparent machine cache records native deterministic predicted output when safe; ranker consumes `predicted_output_net_value` and `predicted_output_total_value` from candidate `expected_effect` | EVD-065, EVD-067, EVD-069 | Runtime machine input/native output smoke already covers field presence: `artifacts\runtime-machine-input-smoke\runtime-machine-input-smoke-20260713-122831\summary.json`; ranking covered by unit tests | `availability_policy_prediction.v1.ranked_event_candidates[].score`, `rank`, `expected_effect` | covered_for_scoring / offline_tested |
| `runtime_queue_replan_after_snapshot` | executor, episode_writer, trainer audit | yes for multi-item daily-plan runtime execution | `LiveTrainingLoop` polls a fresh after snapshot, posts it to backend snapshot ingest, rebuilds ranked daily-plan candidates for the after-state hash, compiles a replacement queue, records `queue_replan_*` fields plus replan artifact paths, and caps attempts with `max_queue_item_attempts` | EVD-069 | Runtime smoke pending for the new replan path; build and solution tests passed in this pass | `execution-*.json.queue_replan_applied`, `queue_replan_state_hash`, `queue_replan_queue_id`, `queue_replan_item_count`, `queue_replan_*_path`, aggregate `max_queue_item_attempts`, `step_results[]` | covered_for_recording / build_verified / runtime_replan_smoke_pending |
| `machine_stand_tile_availability` | candidate_generator, ranker, daily_plan, action_compiler | yes for machine output/input event candidates | Adjacent stand tile selector excludes every `farm.machines[]` tile and requires collision-grid plus movement compiler probe acceptance before output/input candidates become available | EVD-070 | Unit tests PASS for adjacent-machine rows and all-adjacent-machine blocked candidates | `availability.options[].event_candidates[].block_reasons`, `expected_effect move_to_adjacent=...` | covered_for_candidate / offline_tested |
| `runtime_queue_replan_semantic_identity` | executor, episode_writer, trainer audit | yes for blocked-item daily-plan replanning | Stable semantic identity uses option id, command type, semantic normalized parameters, and steps, excluding random `queue_item_id`; replanned queues are filtered against attempted semantic keys | EVD-070 | Deterministic unit test PASS for no duplicate regenerated blocked/completed effects and surviving different remaining item | `execution-*.json.queue_item_semantic_key`, `queue_replan_trigger_semantic_key`, `queue_replan_remaining_before_filter`, `queue_replan_remaining_after_filter`, aggregate `planned_item_count` | covered_for_recording / offline_tested / runtime_replan_smoke_pending |
| `runtime_queue_replan_decision` | executor, episode_writer, trainer audit | yes for blocked-item daily-plan replanning | Deterministic decision helper stops stale blocked daily-plan continuations, replans only blocked+fresh daily-plan continuation, and skips replans for continuable, continue-disabled, executor-override, or non-daily-plan cases | EVD-071 | Decision tests PASS for blocked+fresh, blocked+stale, continuable, continue-disabled, non-daily-plan, and executor override | `execution-*.json.queue_replan_applied`, `queue_replan_stop_reason`, `queue_replan_skip_reason`, `queue_replan_trigger_reason` | covered_for_recording / offline_tested / runtime_replan_smoke_pending |
| `runtime_effective_execution_binding` | episode_writer, trainer audit | yes for replanned queue execution | Each execution result embeds `effective_queue_id`, `effective_queue_item`, `effective_before_state_hash`, `effective_before_snapshot_path`, and `effective_before_snapshot`; training row/episode writers prefer those fields instead of falling back to the original queue/state | EVD-071 | Build/test validation; runtime smoke pending for corrected branch | `training_feature_row.v1.source_state_hash`, `queue_id`, `state_features`; `plan_execution_episode.v1.source_state_hash`, `queue_id`, `before_snapshot_path`, `option_id` | covered_for_recording / build_verified / runtime_replan_smoke_pending |
| `machine_stand_tile_route_preview` | candidate_generator, ranker, daily_plan | yes for machine output/input event candidates when route data exists | Machine stand-tile probe uses existing `exploration.visit_location` route path validation over `locations.collision_grid`; lightweight snapshots without route data retain movement-parameter probe fallback | EVD-071 | Unit test PASS where nearest unoccupied stand tile is route-unreachable and another adjacent stand tile is selected | `availability.options[].event_candidates[].expected_effect move_to_adjacent=...`, `block_reasons` | covered_for_candidate / offline_tested |
| `six_machine_family_mainline` | candidate_generator, ranker, daily_plan, action_compiler, trainer audit | yes for standard deterministic machine training slice | `farm.machines[]`, `machine_data.output_rules[]`, `loadable_inputs[]`, `predicted_output`, `held_item`, `ready_for_harvest`, and `minutes_until_ready` drive Preserves Jar, Keg, Mayonnaise Machine, Cheese Press, Furnace, and Charcoal Kiln without per-family executor branches | EVD-072 | `MachineFamilyMainlineTests` PASS for all six families; full solution no-restore PASS Core 239/Backend 36 | `availability.options[].event_candidates[]`, `ranked_event_candidates[]`, `small_model_plan.v1.steps[].parameters`, `action_queue.v1.items[].normalized_command.parameters`, `blocking_reasons` | covered_for_candidate_plan_queue / offline_tested |
| `fishing.target_casting_power` | candidate_generator, action_compiler, executor, trainer | yes for native cast calibration | Derived from compiled stand/bobber distance and vanilla max-distance formula; executor observes native peak/release power instead of assigning cast state directly | EVD-073 | Runtime validation pending under active user-play constraint | `candidate.parameters.target_casting_power`, `training_execution_result.changed_facts`, compact numeric feature `fishing.target_casting_power` | static_covered / runtime_pending |
| `fishing.max_cast_requested` and `fishing.max_cast_observed` | candidate_generator, action_compiler, executor, trainer | yes for maximum-power visible casts | Candidate generation prefers reachable maximum casts; compiler verifies metadata against geometry; executor records whether native charge visibly reached max before release | EVD-073 | Runtime validation pending under active user-play constraint | `candidate.parameters.max_cast_requested`, `changed_facts`, compact boolean features | static_covered / runtime_pending |
| `fishing.bobber_bar_tick_count` and `fishing.bobber_bar_in_bar_ratio` | executor, trainer | yes for BobberBar control calibration | Runtime records every controlled `BobberBar` update driven through `Game1.oldMouseState`; no one-frame synthetic state is success evidence | EVD-073 | Runtime validation pending under active user-play constraint | `primitive_verification_reasons`, `changed_facts`, compact numeric features | static_covered / runtime_pending |
| `fishing.terminal_progress` and `fishing.terminal_result` | executor, trainer | yes for fail-closed catch verification | Normal fish requires BobberBar `handledFishResult` at success progress; junk/special path is separate and cannot be labeled BobberBar success | EVD-073 | Runtime validation pending under active user-play constraint | `primitive_verification_reasons`, `changed_facts`, compact categorical/numeric features | static_covered / runtime_pending |
| `fishing.hook_attempt_count` | executor, trainer audit | yes for repeated-hook race detection | Runtime latches before native `DoFunction` on a nibble, records the single hook attempt, and blocks impossible junk/special pull plus BobberBar observations | EVD-073 | Runtime validation pending under active user-play constraint | `primitive_verification_reasons`, `changed_facts`, cast diagnostics | static_covered / runtime_pending |
| `fishing.action_idle_cleanup_complete` | executor, trainer | yes for trustworthy verified rows | Executor waits for rod/menu/player idle cleanup before success; unverified or blocked attempts do not append calibration rows | EVD-073 | Runtime validation pending under active user-play constraint | `primitive_verification_reasons`, `changed_facts`, compact boolean feature | static_covered / runtime_pending |
| `required_verified_actions` | live_loop, smoke_scripts, trainer audit | yes for repeated visible action lists | `--required-verified-actions` counts only applied primitive-verified runtime rows; blocked/retry attempts remain progress diagnostics | EVD-073 | Runtime validation pending under active user-play constraint | progress log `verified_actions`, report `required_verified_actions`, dataset row count | static_covered / runtime_pending |
| reconciled executable capabilities | evaluator, ranker, daily_plan, action_compiler, executor, trainer | yes | `recovery.stabilize_day`, `farm.process_machines`, `economy.buy_supplies`, `exploration.visit_location`, `executor.interact`, `executor.sleep`, `executor.pickup_debris`, `executor.collect_machine_output`, and `executor.load_machine_input` are backed by existing compiler/runtime paths | EVD-074 | prior per-executor runtime artifacts remain authoritative | `OptionAvailability.ExecutorEnabled=true`, no stale executor-disabled reason | offline_tested |
| deliberately incomplete capabilities | evaluator, ranker, trainer safety gate | yes to prevent unsafe targets | `economy.sell_items`, social, quest, and mining remain executor-disabled | EVD-074 | not applicable | `OptionAvailability.ExecutorEnabled=false` plus existing block reasons | fail_closed / offline_tested |
| `current_location.home_context.sleep_executor_enabled` | candidate generator, daily plan, trainer | yes for terminal recovery | Bridge reports `true` for the implemented compiler-owned sleep macro; `sleep_prompt_context.confirm_executor_enabled` remains `false` for standalone prompt takeover | EVD-075 | existing isolated sleep runtime artifact | home context and sleep prompt context fields | source_contract_tested / runtime_executor_verified |

No new required model input field is missing or unavailable after this slice. Broad policy training remains blocked outside the narrow runtime-calibration coverage described here and in `docs\stardewai-full-handoff\transparency-coverage.md`.
